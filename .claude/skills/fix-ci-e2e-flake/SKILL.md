---
name: fix-ci-e2e-flake
description: 诊断并修复本仓库（VSCode 范式桌面编辑器）在 CI 上偶发、但本地稳定通过的 Playwright e2e 失败（flake）。当用户提到 “CI e2e 偶发失败 / flaky / 本地跑没问题但 CI 挂了 / e2e 不稳定 / @p0 @p1 spec 偶尔红”，或贴出 Playwright 的 toHaveCount / toBeVisible / timeout 报错、`expect(locator)` call log 时使用。聚焦“区分真回归 vs 环境/写法 flake”的通用流程；具体哪个 spec、哪条断言由 agent 当场判断。
disable-model-invocation: true
---

# 修复 CI 偶发 e2e 失败（flake）

本仓库 e2e 用 Playwright + `_electron` 启动真实 Electron，通过 `window.__E2E__` 探针调服务。CI 偶发、本地稳过的失败**绝大多数不是产品 bug，而是断言写法不够鲁棒，或 CI 环境噪音（extension host 崩溃、进程启动慢、定时器竞态）**。核心套路：**判定真回归 vs flake → 读 call log 的“失败形态” → 把断言收敛到“被测对象本身” → 本地验证 happy path 不破 → 经验追加到案例库/速记**。

> ⚠️ 第一原则：**不要为了让 CI 变绿而削弱对被测行为的覆盖**。鲁棒化 = 排除背景噪音干扰，被测断言强度不变。只能靠放宽真正的被测断言才能过 → 它可能是真回归，别盖住。

## 判定流程

1. **真回归 vs flake**：本地 `--repeat-each=5` 能否复现（本地稳过+CI 偶发→flake；本地也挂→回归）；同 commit 重跑能过=flake，每次必挂=回归/结构性缺口；`git log -p` 看失败 spec 是否刚改过；核对 CI 堆栈绝对路径（`D:\a\...` 是 runner 路径）语义对得上你改的目录。
2. **读 call log 失败形态**：count **波动**=背景元素间歇出现（噪音污染全局 count）；count/received **稳定停错值**=被测对象自身没就位（真回归/定时器没触发/fire-once 空转）；`waiting for locator` 恒 0=渲染没发生/探针没触发/选择器错；timeout 且无元素=往前看前置步骤。
3. **已知噪音源**：extension host 偶发崩溃（`ExtensionHostClientService._handleCrash` 发背景 toast + error 日志）；renderer 定时器竞态（auto-hide/auto-read 在 CI 晚几百 ms）。
4. **最小且鲁棒的修复**（优先对齐同文件已鲁棒化的兄弟断言）：噪音污染**列表**→`.filter({hasText:'<被测唯一文案>'})` 收敛；噪音污染**全局单值/一次性状态**→从源头禁用无关子系统（先 grep 确认无 spec 依赖）；定时器/异步竞态→`expect.poll`/`toHaveCount({timeout})`，少用固定 `waitForTimeout`+硬断言；纯环境型→别强改产品，记录案例库。
5. **验证**：`pnpm --filter @universe-editor/editor exec playwright test e2e/specs/<spec>.ts [--repeat-each=5]`；全量 `pnpm e2e`（输出多，只截错误）。本地无法复现 CI 噪音是常态——目标是“鲁棒化没破坏 happy path”。
6. **沉淀**：把“失败形态→根因→修法”追加到案例库，并新增/更新一条速记（与案例号互相引用）。这是本 skill 长期价值所在。

## 速记（判定信号速查，先扫这里再读对应案例）

1. call log count **波动**=背景噪音；count **卡死错值**=更像真回归。先分清。
2. ext host CI 偶发崩溃，污染一切对通知做全局 count 的断言——按文案过滤。
3. 鲁棒化 ≠ 放宽被测断言。别用“变绿”掩盖真回归。
4. 异步/定时器用 `toHaveCount({timeout})`/`expect.poll`，少用固定 `waitForTimeout`+硬断言。
5. 同文件内有的步骤已加固（按文案过滤/PO 封装）、有的还裸（全局 count/裸 evaluate），后者是遗留薄弱点，优先对齐复用兄弟方法。
6. CI 堆栈路径是 runner 路径（`D:\a\...`），语义对齐即可，别因路径不同误判改错 checkout。
7. 本地无法复现 CI 噪音是常态；本地验证目标是“没破坏 happy path”。
8. 污染**列表**（可 filter）→改 spec 收敛；污染**全局单值/一次性状态**（bell badge、`_hasRevealed`）或**产品一次性动作**→spec 层无法隔离，从源头禁用噪音子系统/修产品（先 grep）。同一噪音源可同时打挂多个无关 spec。（案例 2/11）
9. **retry 救得回=瞬时竞态（flake）；救不回=结构性（test 超时击穿/真回归/产物缺口）**。分类第一信号。
10. spec poll/service retry 预算远大于全局 test `timeout`（30s）时，**test 超时才是真天花板**，宽 poll 是摆设。冷启动 spec 用 `test.slow()`；`workers/expect.timeout` 按 `process.env['CI']` 分档。（案例 3）
11. 本地手跑单 LSP spec 前确保产物链齐：`pnpm ext:build`+`extension-host/dist`+`vendor/typescript-language-server`。缺→符号空/探针缺=产物问题非回归。正规跑法根 `pnpm e2e`。
12. **本地稳过+CI 每次必挂+received 空+retry 救不回=伪 flake**（CI 漏装运行时产物，尤其**非 pnpm workspace 的 vendor**）。鉴别捷径：同类功能“A 能跑 B 不能”不对称（md LSP ✓/ts LSP ✗）。修 CI 只精准 `npm --prefix vendor/<x> ci`（e2e job 无 `submodules: recursive`）。（案例 4）
13. `expect.poll` 里“盲按按键”（`keyboard.press`）危险：按键落**当前焦点**，UI 未就位时打进编辑器 TEXTAREA 污染被测对象自身，加宽窗口无效。正解：按键**门控在“目标已聚焦”前置**（探针读状态）。（案例 5）
14. 报错 `Execution context was destroyed ...navigation`=harness 时序 flake 非断言失败。凡“fire 后页面 reload/导航再 `page.evaluate`”的 PO 步骤都要鲁棒（捕获→重等 `domcontentloaded`+`__E2E__`→重试）。见 `WorkbenchPO._evaluateWhenRestored`。（案例 6）
15. config 已按 CI 分档 `expect.timeout` 时，spec 硬编码 `{timeout:5000}` 盖回本地值→分档失效；received 稳定空 `""`+等 Monaco 首帧→删局部 timeout 继承 config。**反向坑**：`openWorkspace`-then-`openFileUri` 的 spec 冷启动重，`activeEditorLanguageId` 首 poll 吃 CI 默认 10s 在 Windows 仍不够→显式提到 `{timeout:20000}`。只改触发 flake 的谓词。**此型反复复发**（gotoSymbol→markdownLsp→markdownEditing），命中信号=`openWorkspace`+`openFileUri` 冷启动 spec 里首个 `activeEditorLanguageId` poll 无显式 timeout 而其后 `hasCommand` poll 已带 15000ms；修时**整文件扫同型兄弟一并加固**。（案例 7、25、31）
16. 报错 `EBUSY/EPERM/ENOTEMPTY rmdir`、栈在 `finally`/teardown=Windows 文件锁清理竞态非断言失败。根因：spec tmp workspace 的 `fs.rm` 时 Electron app 未关（`await use(app)` 后才 `closeApp`），句柄/watcher 占目录。修：`fs.rm` 加 `maxRetries:10,retryDelay:200`。**`ENOTEMPTY` 子型**（删的目录冒出 `<id>.json.tmp`）是主进程 storage debounced 原子写“持续写”竞态，`rmSync` 内建 maxRetries 救不回，须手写循环重试整个 `rmSync`（`for`+`await sleep`）。共享 fixture `sharedApp.seedUserData` 同吃（reload 不拆主进程 storage）。（案例 8、17）
17. `runCommand(动作)`+`expect.poll(只读状态)` 分离范式，动作 fire-once 且依赖异步就位前置→received 卡死初值+加宽无效（命令在就位前 fire→静默 no-op，poll 不重 fire）。修：把动作放进 poll 谓词，抽 PO helper。（案例 9）
18. 报错 `Test timeout of 30000ms exceeded`（非 poll message）+received 初值=test 级超时击穿。重 spec 前置开销大，poll 窗口 ≥ 全局 test timeout 则 test 天花板先到。常因裁剪兄弟 spec 漏抄 `test.setTimeout`。鉴别环境慢 vs 产物缺口：Ubuntu✓/Windows✗=纯 Windows 慢；纯 Ubuntu✗ 也可只是 CI 2 核+2 workers 抢占放大冷启动。**裸 `electron.launch` 的 restore/persistence spec 是重灾区**：命中信号=spec 走 `launchWithState` 冷启动却既无 `test.slow()`/`setTimeout` 又有 poll 窗口逼近或等于 30s。修=补 `test.slow()`（抬到 90s）+poll 抬到 CI 分档以上；修时整体扫同目录所有裸 launch spec 有无同型漏保护（案例 32：layoutPersistence 漏 slow、vscodeKeybindings 有 30s poll 却无 slow）。（案例 10、32）
19. 同 flaky spec 二进宫、形态从 `Test timeout` 变 poll 窗口超时+received 恒初值=上轮只治天花板，真根因还在，八成产品 bug。查链路：“响应事件去操作异步启动的资源”时若资源 in-flight 启动 Promise 没被 await，启动期事件被静默丢弃。修：操作前 `await Promise.allSettled([...启动 Promise])`；修产品+回归单测。（案例 11）
20. 报错 `Target page/context/browser has been closed`（区别速记 14）先验 main 是否真崩：Windows 退出码 `3221225477`=`0xC0000005` native 段错误。决定性三组对照：①`--workers=1` 重复 ②`--workers=6` 重复 ③多实例+禁用嫌疑子系统。单实例不崩、多实例才崩=并发放大的跨进程竞态（常第三方 native 库），进程内串行化无效。正解：触发用例 `@serial` 隔离到 `--workers=1`。（案例 12、16）
21. 报错「Worker teardown timeout」+所有测试 pass=关 app 链路卡死。①graceful close 挂：`app.close()`→`before-quit` veto 链某 participant 在 headless 弹无人应答 modal→永不 resolve；②强杀不彻底：超时只 SIGKILL main PID，Windows 杀父不杀子→子进程孤儿占句柄。修：Fix A quit-chain 上会弹 modal 的 participant 按 `isE2E` 短路放行；Fix B `closeApp` 超时改 `taskkill /pid <pid> /T /F`。（案例 13）
22. `page.evaluate` 里“滚动后等固定 N 帧 raf 再读布局量”危险：虚拟化列表经 `ResizeObserver` 异步测量，固定帧数不够→读到过渡瞬时值污染被测对象。鉴别：`--workers=1 --repeat-each=N` 全过、`--workers=4` 全挂（received 单调爬升=测量收敛被采样）=竞态 flake 非回归。注意 `--repeat-each` 默认吃 config workers（本仓库 4），必须显式 `--workers=1` 对照。修：改“等布局量连续 K 帧不变再采”的 `settle()`。（案例 14）
23. 滚动恢复用 `frac=scrollTop/max` 当落点代理脆弱：①分母漂移（下方懒测量使 max 增大、frac 缓慢漂）；②RAF 过冲（restore 收敛前 frac 在顶/底震荡含 1，单边 `poll(readFrac).toBeGreaterThan(0.15)` 被过冲帧 frac=1 立刻满足提前返回）；③**虚拟模式像素中点是双峰的**——`scrollTop=(H-h)/2` 因上方行 estimate 高度≠真实高度，同一像素落到两条截然不同消息(如 m11 frac≈0.43 或 m22 frac≈0.92)，忠实恢复到 m22 就 >0.85 误判。正解：断言锚定消息索引（复用 `captureAnchor` 语义解析 `m:m<idx>`）；**能定位到具体消息时优先相对不变量“恢复后顶部消息==切走前那条(settle 收敛后取值,±2 容差)”而非绝对带**——相对不变量对双峰中点免疫且仍守护重置到顶/跳到底两端。（案例 15、34）
24. 同类“滚动恢复”spec 有的加了 echo-settle/高度收敛门控、有的没加→没加的并发下偶发。虚拟模式贴底靠 `scrollToBottomStable` 10 帧 RAF 上限，echo 仍涌入/scrollHeight 还涨时上限耗尽停在离底几百 px。修：scroll 前 `poll(getAcpMessages().length).toBe(<full>)`+两帧 scrollHeight 相等门控；单次贴底改 `poll` 反复直到 distance<30。（案例 15）
25. spec 用 `mkdtemp(tmpdir())` 造真 git 仓库、断言依赖“spec 路径 ⋈ git 内部路径精确匹配”→仅 CI Windows 确定性失败（received 恒定、retry 救不回=伪 flake=真 bug）。根因：CI Windows `os.tmpdir()` 是 8.3 短路径（`RUNNER~1`），`git rev-parse --show-toplevel` 总返回长路径→`relative(长,短)` 破碎→`git show` exit 128→`getHeadContent` 恒 null。修：`realpathSync.native(mkdtempSync(...))` 规范成长形（本地 no-op）。复现捷径：COM `GetFolder(dir).ShortPath` 取 8.3 名当 cwd。（案例 18）
26. 诊断探针别污染被测断言——给“被多断言共用的既有探针”追加调试后缀会让每个用它的断言都失败、最早的先挂、误导定位。铁律：诊断只走新增独立探针方法或 `window.__X_DBG__` 数组+独立 probe 读回。`console.info` 太重会改时序（heisenbug），优先轻量内存数组累积事件序列由 probe 一次读回。（案例 19）
27. e2e agent fixture（`test-fixtures/*.cjs`）写盘 `os.tmpdir()` 且文件名只含 agent 自颁发 sessionId（每进程都从 `sd-1` 起）→整机共享路径被并发 worker/孤儿 agent 互相覆写。修：写盘路径加进程唯一 token（`${process.pid}-${randomBytes(4)}`）。改 `.cjs` 无需 rebuild，产品 contribution 改动要 build。（案例 19）
28. “迟到的 fs 事件把 diff modified 侧从磁盘刷回、覆盖未保存 live 编辑”是真产品竞态。凡“响应 fs 变更刷新可能有 live 编辑缓冲的视图”都要先查 live model：`MonacoModelRegistry.peek(uri)` 有值用 `model.getValue()`，无才读盘。别用 `isDirty` 判断（React effect 异步更新留竞态窗口）。同源 `ExternalChangeWatcher._refreshChangedDiffEditors`、`SessionChangesDiffSyncContribution._sync`。（案例 19）
29. e2e 用 `dispatchEvent(new Event('scroll'))` 合成“用户滚动”危险：不带 wheel/pointerdown/keydown，凡产品靠这些事件区分“程序化 vs 用户接管”的逻辑都识别不到被吞。决定性鉴别：`--workers=1 --repeat-each=N` 确定性全挂（非并发放大）+`git stash`+build 回 HEAD 仍挂（既有脆弱点被某提交激活）。修：测试用真实 `WheelEvent`/`mouse.wheel`/键盘表达用户接管。别改产品用位置差异判接管（会把 scroll-anchoring 漂移误判）。（案例 20）
30. `sharedApp`（worker 单实例、reload 复位）里 `window.reload()` 不拆主进程状态——上测试 `openWorkspace` 的文件夹被 session-restore 灌回本测试 active group（含 ghost tab），污染 tab 数类断言（形态因上个 spec 是谁而飘忽）。修：`resetWindow` 在 `whenRestored` 后确定性 `closeWorkspace()` 拆掉 ghost groups。（案例 21）
31. “关闭编辑器后紧接着对同一文件做 model 操作”有 model-dispose 竞态：`closeAllEditors` 同步返回但 React 卸载→dispose 异步，返回后 `peek(uri)` 仍可能拿到即将 dispose 的孤儿 model→按 model 存活分“改内存 vs 落盘”的代码走 live 分支只改内存、孤儿 dispose 后丢失。修：对“静默文件级改写”从源头保证落盘（apply 加 `persistToDisk` 始终读写盘+`setValue` 同步残留 model）。（案例 22）
32. 一次“启动/性能优化”重构改变就位时序会同时放大多个无关 spec 的“就位前”竞态；且该提交往往自补部分探针却漏改同类路径——漏改处即回归。多 spec 同提交后一起变红、形态各异，但 `git show <opt-commit>` 能读到“workbench mounts before Monaco”这类自述时序变更。判定：`git show --stat` 看波及面，逐个失败对照“同类路径它修了没”。（案例 23）
33. provider 拉取 `.then(...)` 缺 `.catch()`→冷启动 reject 掐断 retry 链（保活 `_maybeRetry` 只在 `.then` 调），结果恒空且 retry 救不回=伪 flake=真 bug。修：`.then().catch()` 双分支都调 `_maybeRetry` 保活，catch 加 `_currentModel!==model` 早退。（案例 23）
34. `.catch()` 接得住 reject，接不住“pull 永久 pending（既不 resolve 也不 reject）”——凡“从 then/catch 调度下一次重试”的结构，一次 hang 掐死全链，比速记 33 更隐蔽。底层 web-worker RPC/动态 import 冷启动永久排队时触发。不对称鉴别：Windows✓/Ubuntu headless✗。修：给单次 pull 加超时竞速（`Promise.race` timeout-reject）保证必 settle。（案例 24）
35. 报 `Target page ... closed`（速记 20）的 spec **未必是肇事者**：`sharedApp` 是 worker 级共享 Electron，`resetWindow` 每 test 前 `closeWorkspace()`→若同 worker 前个 test 开过 workspace 则触发 parcel `unsubscribe`，多 worker 并发崩，报错落在恰好在 reset 的**下一个受害 spec**（纯 renderer 的 output 也会背锅，崩点飘忽）。要隔离**加害者=`sharedApp ∩ openWorkspace` 的 test**（全打 `@serial`），受害者不用改。判别：`for f in $(grep -l fixtures/sharedApp *.spec.ts);do grep -q openWorkspace $f&&echo $f;done`。（案例 26）
36. 就位门控只 await `whenRestored`（=mount 时机）不够：renderer 有**在 mount 后 fire-and-forget 的 workspace-state reconcile**（`main.tsx` `Promise.all([...reconcileFromStorage()])`，内含 500ms fallback timer），它以**破坏性 `set(...)`+`_seedDefaults()` 回默认值**收尾。spec 若在 reconcile 落地前就 `click` 把状态切离默认（如 SideBar 切到 Search）→迟到的 reconcile 覆盖回默认（Explorer），received **恒卡默认值 5s 不恢复**（速记 1：稳定卡值≠噪音；速记 17：fire-once 动作被后台异步终态覆盖）。反例免疫：断言**默认态本身**的 spec（reconcile seed 与之一致）不会翻车。修：动作前 `await workbench.waitForBootstrapFocusSettled()`（probe 的 `bootstrapFocusSettled` 在同一 reconcile `Promise.all` 的 `.finally` resolve，保证 reconcile 已 settle）。（案例 27）
37. **本机 Windows** 裸 `electron.launch`（自带 `launchWithState`，带 `--inspect=0 --remote-debugging-port=0` 二次启动 Electron）的 restore/persistence/relaunch 类 `@p1` 偶发/稳定报 `electron.launch: Process failed to launch!`（`<launched> pid=...` 后仍判失败、exitCode=9）=**本机环境对 Electron 二次启动+CDP 连接的限制,非回归**;走 fixture 的 `@p0` 不受影响。判别：`git stash` 清改动复跑仍挂=环境（失败集合 ⊆ 裸启动 `@p1` 且报此串→别当回归追,以 CI ubuntu+windows 为准）。**真 bug**：Claude Code shell 设 `ELECTRON_RUN_AS_NODE=1`→Electron 当纯 Node 跑、不认 `--remote-debugging-port`→所有子进程继承后报 `bad option`;凡新写自定义 `electron.launch`,`env` 里先解构去掉它：`const { ELECTRON_RUN_AS_NODE: _ignored, ...inheritedEnv } = process.env`。（案例 28）

38. Windows CI 的 `%TEMP%` 可能含 DOS 8.3 段（如 `RUNNER~1`）：`pathToFileURL(path)` 将 `~` 编为 `%7E`，但产品 `URI.file(path).toString()` 保持 `~`。若 E2E 探针返回平台 URI 字符串、预期却用 Node URL，received 稳定仅差 `%7E`/`~`、retry 救不回，是**断言表示域不一致**，不是时序或产品回归。修：预期也用 `URI.file(path).toString()`；先审计 `pathToFileURL` 的其它用法，只有比较 URI 字符串的断言须改。（案例 29）

39. `@regression`+本机 `--workers=1 --repeat-each=N` **确定性**失败（非并发放大）+received 恒初值+retry 救不回=伪 flake=真 bug/fixture 失真，别当环境噪音。**复合根因要层层剥**：一次修复只让通过率从 0→部分，说明还有第二、三层，继续加诊断别收手。本例三层叠加：①**盘符大小写**——SCM 默认 changelist 路径经内核 `norm` 存**小写盘符**(`c:/`)，fake-p4 却用大小写敏感字符串精确匹配磁盘路径(大写 `C:/`)→匹配空；真实 p4 在 Windows 大小写不敏感，是 fixture 保真度不足（修 fake-p4 匹配走 `normPath`/depotFile 身份比较）。②**fixture 架构失真**——fake-p4 把 state.json 写在**被 watch 的 workspace 目录内**(`.p4fake/`)，每次 mutate 触发 workspace watcher→额外 `refreshReconcilePaths` 与被测操作交织；真实 p4 状态在服务器不在工作区（修：state 移出 workspace 到独立 tmpdir）。③**真产品竞态**——`refresh` 与 `refreshReconcilePaths` 无互斥，两条 pass 读改共享 reconcile 状态，watcher 触发的"文件仍 opened→scan 结果被过滤成空"的 pass 若**最后完成**，其 `mergeReconcile`（path 在 scanned 却不在 fresh）把文件删回去→组恒空。真实使用(p4 edit→改文件触发 watcher→点 Move to Reconcile)同样踩，修：引入串行链 `_runSerial`，所有 refresh 类操作串行，每 pass 见前一 pass 已提交态。诊断法：fake CLI 侧 append 时间戳日志到固定文件看命令时序+targets 数；产品侧临时 DBGX 写同类日志看 `_setReconcileFiles` 谁最后 clobber（速记 26：用独立日志别污染既有探针，完事删干净）。（案例 30）

40. 多条**业务无关** spec 同轮随机挂在 fixture `waitForFunction(__E2E__)`，trace 显示 Electron/window 很快创建、页面纯黑、probe 30s 恒无，retry 又恢复——先查 **probe 前 bootstrap RPC 永久 pending**，别把它当慢启动加 timeout。本例 dead-frame gate 在 `did-start-navigation` 关闸，renderer 却能在 `dom-ready` 前发首个 NLS RPC；main 同步响应被 gate 丢弃，bootstrap 永久卡死。入站 IPC 本身就是新 frame 存活的强信号，必须先重开 gate 再分发请求，让同步响应可返回。（案例 33）

41. 报错 `extension host may only execute _workbench.* commands ... not "<ext.cmd>"`=**扩展命令冷启动激活竞态**：spec 门控用的信号（如 SCM `getScmSourceControlCount()>0`）在扩展 `activate()` **早期**就绪，而 contributed 命令 handler 在**同一 `activate()` 后段**才注册；两者之间 fire 该命令→renderer 转发给 host→host 无 handler 转回 renderer→被 `MainThreadCommands.$executeCommand` 拒（只准 `_workbench.*`）。同 `smoke.markdownEditing` 的 `runEdit` 一型。修：门控改**轮询任一只读 contributed 命令直到不再被拒**（handler 同步一次性注册，一个就绪则全就绪），抽 helper 放进 `expect.poll`（捕获→匹配该错串 return false 继续 poll，其它 throw）。命中信号=spec 只 poll `getScmSourceControlCount` 却紧接着裸 `runCommand('<ext>.<cmd>', ...)`。修时整文件扫同型 test 一并加固。（案例 35）

42. **拖拽 spec 只等 `data-testid` host 可见就 drop 属就位门控不足**：内嵌 Monaco 输入框的 host `div` 立即渲染，但 drop 处理里的 `insertRef()`/句柄方法在 Monaco 实例 mount 完成前**静默 no-op**（`if (!ed||!model||!tracker) return`）。Ubuntu 慢启动下 drop 在 Monaco 就位前 fire→received 恒 0（速记 1：稳定卡值≠噪音；速记 17：fire-once 动作 no-op，drop 不幂等不能放进 poll 反复 fire）。**键盘型 spec 免疫**（`focusInput`+poll `editorTextFocus` 契约键=Monaco `onDidFocusEditorText` 设，天然门控就位）；**drop 型绕过焦点**直接 dispatch 到 host div，故独有此薄弱点。同文件里 drop 前有额外 await（如 `setInputFiles`）的兄弟 test 偶然给了缓冲不易翻车、无缓冲的稳定挂。修：门控加 `expect(host.locator('.monaco-editor').first()).toBeVisible()`（= `m.editor.create` 返回+句柄 refs 就位），并把固定 `waitForTimeout+硬断言` 换 `expect.poll`。（案例 36）
43. **命令在 workspace-swap 的异步 re-pin 窗口里 fire→打到仍钉旧（空）workspace 的 host→scan 空→received 恒初值**：ext host 在 launch 时把 `workspace.rootPath` 钉死，workspace 变更靠 `_onWorkspaceChanged` **异步** `_restart` 重启重钉；若某命令在“stop 旧 host→spawn 新 host”这段窗口 fire（如 markdown 改链接 flush 与 `openWorkspace` 撞同一 `onDidRunFileOperation` 事件轮），会读到旧 host（`$findMarkdownFiles` 的 `root` 仍 undefined→返回 `[]`）→空 edit。**本地几乎测不到**（重连极快，窗口 <1 帧），Windows CI 的 Electron-as-node spawn 慢把窗口撑到秒级。**先前的 blind-retry（案例 22/速记 31 之外新加的 `EMPTY_EDIT_RETRIES`）治标不治本**：只要重连比 retry 预算慢就仍挂。真修=产品侧**同步 arm re-pin barrier**——`onDidChangeWorkspace` 回调**首个 await 前**同步记下 `_repinning` Promise，命令入口（`activateByEvent`/`executeContributedCommand`）改 await `_whenReady()`（先 drain barrier 再 `start()`），命令必落在重钉后的 host。复现捷径=临时探针在 `openWorkspace` **前**强制 `activateByEvent('onLanguage:markdown')` 连上初始（空）workspace（制造 `hasConn=true` 分支）+在 `_restart` 的 `host.stop` 后注入 `setTimeout` 撑宽窗口。（案例 37）
44. **spec 的等待窗口 < 产品对该异步链路的容忍窗口 → “产品仍在合法等待、spec 已先放弃”**：`runCommand`+poll(`activeEditorTypeId`) **先通过**（打开 editor/读内存不依赖扩展 provider），但紧接着的 iframe/DOM 元素 `toBeVisible` 恒 `element(s) not found`（速记 1：稳定卡值≠波动噪音）——该元素由 `CustomEditorHost` 在扩展宿主子进程**激活+provider 注册 RPC 往返**回来后才 mount，产品内部对这条链路容忍到 **15s**（`setTimeout(...,15000)` 才 `setFailed`），spec 却只等 10s。Windows CI 的 Electron-as-node spawn 慢（速记 43）把往返推过 10s。命中信号=同一条链路的**兄弟 spec 用更宽 timeout**（如 `smoke.swarmSpreadsheetDiff` 走同 `openWebviewDiff`→iframe 用 15000，本 spec 10000=遗留薄弱点，速记 5/15）。修：把 iframe/marker 等待对齐**产品内部容忍窗口**（10000→15000）+给扩展宿主冷启动 test 补 `test.slow()` 防累计击穿 30s 全局 timeout（速记 10/18）；修时整文件扫同型兄弟 test 一并加固（本例第二个 `trust.grant` relaunch test 更重，同改）。（案例 38）

## 案例库

> 每条：现象+根因 → 修法+锚点。教训已并入对应速记；此处保留判别信号与锚点路径。新经验往下追加。

**案例 1（速记 1/2）**：`smoke.notification.spec.ts` `@p0` `notification-toast-item` 全局 `toHaveCount(0)` CI 偶发超时、count 1↔2 波动——ext host 崩溃发背景 Warning toast 污染全局 count。修：`.filter({hasText:'This is a test notification.'})` 收敛再 count。锚：噪音源 `ExtensionHostClientService._handleCrash`；toast `NotificationsToast.tsx`。

**案例 2（速记 8）**：同轮挂 `smoke.notification` bell badge `"1"` + `smoke.output` `panelVisible` false——同噪音源 `_handleCrash`（未读 toast→unreadCount；崩溃 error→`ErrorLogAutoRevealContribution._hasRevealed` 一次性置真）污染全局单值/一次性状态，spec 层无法 filter。修：`ExtensionsContribution._boot` 开头 `isE2E` 短路 return（先 grep 确认无 spec 依赖 ext host/SCM）。锚：`isE2E=window[E2E_PROBE_ENABLED_KEY]`（`shared/e2e/contract.ts`）。

**案例 3（速记 9/10）**：`smoke.outline` `@p1` 硬失败（超时非拿 `[]`）——全局 test `timeout:30_000` 拦腰截断 outline poll 累计 ~115s（`OutlineService` retry 180s），CI tsserver 冷启动+4 Electron 抢 2 核。修：冷启动 spec `test.slow()`；`expect.timeout=CI?10_000:5_000`；`workers=CI?2:4`；editorResolver 改“先 poll 目标项可见再 Enter”。锚：`playwright.config.ts`；`OutlineService.ts`（`PULL_RETRY_BUDGET_MS`）；`extensions/typescript/src/extension.ts`（缺 `UNIVERSE_TSLS_CLI` 不激活）。

**案例 4（速记 12）**：`smoke.outline` `@p1` Windows+Ubuntu 都总挂、received 恒 `[]`——outline 唯一依赖 ts LSP，ts 扩展硬依赖 `UNIVERSE_TSLS_CLI`→`vendor/typescript-language-server`（非 pnpm workspace，CI 只 `pnpm install`+`build` 从未装它）。修：e2e job 加 `npm --prefix vendor/typescript-language-server ci`（不能调 `vendor-install.mjs`——它遍历 submodule 而 e2e job 无 `submodules: recursive`）。锚：`.github/workflows/ci.yml`；`scripts/release/vendor-install.mjs`。

**案例 5（速记 13）**：`smoke.peekNavigation` `@p1`「Enter follows the reference」Ubuntu 偶发、received 稳定 `…/a.md`——`expect.poll` 盲按 `keyboard.press('Enter')`，peek 打开前焦点在编辑器 TEXTAREA，Enter 在链接处插换行破坏 link→peek 永不打开。修：加探针 `isReferencePeekFocused()`（`document.activeElement?.closest('.ref-tree')`），只在聚焦时按。锚：`PeekNavigationContribution.ts`（`.ref-tree`）。

**案例 6（速记 5/14）**：`smoke.disposableLeak` `@p1` Ubuntu 偶发 `Execution context was destroyed`、栈在 `WorkbenchPO.waitForRestartRestore()` 末步裸 `evaluate(whenRestored)`——`restart`→`win.reload()` 导航未 commit 时裸 evaluate 与上下文切换重合被销毁。修：抽 `_evaluateWhenRestored()`（捕获→重等→重试 ≤3）两处复用。锚：`WorkbenchPO.ts`；`windowActions.ts`（`RestartEditorAction`）。

**案例 7（速记 15）**：`smoke.gotoSymbol` `@p1` Windows 偶发、`activeEditorLanguageId` poll received 恒 `""`——首步硬编码 `{timeout:5000}` 盖回 CI 分档（config 已 CI 10s）。修：删这 4 个 spec 的 `{timeout:5000}` 继承 config 默认。锚：`smoke.{gotoSymbol,peekNavigation,markdownLsp,markdownPreview}.spec.ts`；`playwright.config.ts:11`。

**案例 8（速记 16）**：`smoke.editorTabDnD` `@p1` 本地 Windows 偶发 `EBUSY rmdir`、栈在 `finally` 的 `fs.rm(tmpDir)`——app 未关（`await use(app)` 后才 `closeApp`），pinned 句柄+watcher 占目录，`force:true` 不重试 EBUSY。修：`fs.rm` 加 `maxRetries:10,retryDelay:200`。锚：`electronApp.ts`（`await use(app)` 后才 `closeApp`）。

**案例 9（速记 17）**：`smoke.commandPalette` `@p0` 前置 `expect.poll(getContextKey('editorFocus')).toBe(true)` received 恒 false——`focusActiveEditorGroup` fire-once 依赖 Monaco `FileEditorRegistry.register`（model 异步后），冷启动命令在 register 前 fire→no-op，poll 不重 fire。修：把 fire 放进 poll 谓词，抽 PO helper `focusActiveEditorGroup()`。锚：`editorActions.ts`（`FocusActiveEditorGroupAction.run`）；`FileEditor.tsx`（`applyModel` register）。

**案例 10（速记 18）**：`smoke.aiCommitMessage.spec.ts` `@p1` 仅 Windows CI `Test timeout of 30000ms`、received 恒 0——与兄弟 `@p0` 逐行相同但漏抄 `test.setTimeout(120_000)`，poll 窗口 30_000==全局 timeout→摆设，Windows 慢击穿。修：加 `test.setTimeout(120_000)`、poll→60_000。锚：基准 `aiCommitMessage.generate.spec.ts`；`playwright.config.ts:6`。

**案例 11（速记 8/19）**：案例 10 续集，形态变 poll 自己 60s 窗口超时、`getScmSourceControlCount()` 恒 0——git `activate` fire-once，`openWorkspace` 依赖 host relaunch 重新 activate，但 `_connect` 在 `await host.start` 期间 `this._trusted` 未赋值→启动期 swap 落此窗口事件被丢弃→host pin 空 workspace。修产品：`_onWorkspaceChanged` 开头 `await Promise.allSettled([_startingTrusted,_startingRestricted])` 再读 tier+回归单测。锚：`ExtensionHostClientService.ts`（`_onWorkspaceChanged`）；`extensions/git/src/extension.ts`。

**案例 12（速记 20）**：`smoke.simpleFileDialog` `@p1`「OK switches workspace」本地全量偶发 `Target page...has been closed`——main 退出码 `0xC0000005` native 段错误；三组对照证 `@parcel/watcher` windows backend 多进程并发重订阅（切 workspace）native 竞态，进程内串行化无效。修：`@serial` 隔离到 `--workers=1`（`pnpm e2e`/`ci.yml` 各拆并行趟+串行趟）。锚：`smoke.simpleFileDialog.spec.ts`；`fileWatcherMainService._subscribe`。

**案例 13（速记 21）**：仅 Windows CI「Worker teardown timeout」+所有测试 pass——①`SessionShutdownParticipant._maybeVeto` 有 running session 时弹 headless 无人应答 modal→`app.close()` 永不 resolve；②超时 SIGKILL 只杀 main PID，Windows 子进程孤儿占句柄。修：Fix A `_maybeVeto` 算出 running 后 `isE2E` 短路 `return false`；Fix B `closeApp` 超时 `taskkill /pid <pid> /T /F`。锚：`SessionShutdownParticipant.ts`；`e2e/fixtures/electronApp.ts`（`forceKillTree`）。

**案例 14（速记 22）**：`smoke.agentsScrollbarStable` `@p1` 仅 Windows `maxJump≈1.27`（阈值 0.25）、采样单调爬升到稳定——每段滚动只等固定 2 帧 raf 读 `scrollHeight`，@tanstack `ResizeObserver` 异步测量未完，采到过渡瞬时值。判定：`--workers=1 --repeat-each=5` 5/5 过、`--workers=4` 全挂=并发放大。修：换 `settle()`（scrollHeight 连续 4 帧不变或耗尽 120 帧）。锚：`ChatBody.tsx`（`estimateRow`/`useVirtualizer`）。

**案例 15（速记 23/24）**：①`smoke.agentsScrollRestore` `@p1` `finalFrac=1`（期望 <0.85）；②`smoke.vscodeKeybindings` `@p1` 末步裸 `whenRestored` `Execution context destroyed`；sibling `agentsVirtualScrollRestore` scene1 贴底 distance 卡 ~342。根因：①`poll(readFrac).toBeGreaterThan(0.15)` 被 RAF 过冲帧 frac=1 立刻满足；②案例 6 同型；sibling 缺 echo-settle 门控。修：①断言改锚定消息索引落绝对中部带 [4,12]、poll 连续 ≥600ms 在带内；②导出 `evaluateWhenRestored(page)` 复用；sibling 补 echo-settle+两帧 scrollHeight 门控。锚：`ChatBody.tsx`（`captureAnchor`/`scrollToBottomStable`）；`WorkbenchPO.ts`（导出 `evaluateWhenRestored`）。

**案例 16（速记 20）**：`smoke.folderDragNewWindow`+`smoke.outline` `@p1` 本地 `--workers=4` 偶发 `Target page...has been closed`——撞案例 12 同款 parcel watcher 跨进程崩溃（`openWorkspace`→`ExplorerTreeService._setRoot`→`fileWatcher.watch`→parcel `subscribe`；folderDrag 一次 test 两次 subscribe 高危）。`--workers=1` 12/12 过=并发放大。修：两 spec `@serial`。锚：`ExplorerTreeService._setRoot`→`fileWatcherMainService._subscribe`。

**案例 17（速记 9/16）**：`smoke.multiFileDragEditor.spec.ts` `@p1` 本地全量偶发、断言已过、报 `ENOTEMPTY rmdir workspaces` 栈在 `sharedApp.seedUserData`——`openWorkspace` 改主进程状态违反 `sharedApp` 契约，`resetWindow` 删 workspaces 瞬间主进程又写 `.tmp`（storage debounced 原子写）→父 rmdir 撞 ENOTEMPTY，`rmSync` 内建 maxRetries 只覆盖叶子救不回。修：`seedUserData` 改 async，手写 `for(attempt<10)`+`await sleep(200)` 循环重试整个 `rmSync`。锚：`sharedApp.ts`（`seedUserData`）；`src/main/storage.ts`（`writeAll` tmp→rename）。

**案例 18（速记 12/25）**：`smoke.dirtyDiffPeek.spec.ts` `@p1` 仅 CI Windows、poll received 恒 false（test 级已 120s）——CI Windows `os.tmpdir()` 8.3 短路径喂 openWorkspace，`git --show-toplevel` 返长路径→`relative(长,短)` 破碎→`git show` exit 128→`getHeadContent` 恒 null→regions 空。不对称：兄弟 aiCommitMessage 只验 SCM count>0 能过。修：`realpathSync.native(mkdtempSync(...))` 规范长形。锚：`extensions/git/src/repository.ts`（`getHeadContent` `relative(this.root,absPath)`）；`DirtyDiffContribution._getHead`（缓存 null）。

**案例 19（速记 26/27/28）**：sessionChanges「diff 跟随修改自动更新」新 spec `--workers=4 --repeat-each=6` 批量挂、received 恒 agent 值——三层叠加：①`sessionDiffAgent.cjs` 写盘共享 tmp（sessionId `sd-1` 每进程重复）被覆写→加 `${process.pid}-${randomBytes(4)}`；②`setActiveEditorText` 裸 evaluate model 未就位 no-op→放进 `expect.poll`；③迟到 fs 事件 `_refreshChangedDiffEditors` 无条件读盘盖回 live 编辑→live-model 优先（`peek` 有值用 `model.getValue()`，别用 isDirty）。诊断坑：改既有共用探针污染全断言、`console.info` heisenbug→用独立 probe+`window.__DIFF_DBG__` 数组。锚：`sessionDiffAgent.cjs`；`ExternalChangeWatcher._refreshChangedDiffEditors`；`SessionChangesDiffSyncContribution._sync`。

**案例 20（速记 29）**：`smoke.mermaidPreviewSwitch.spec.ts` `@p1` 全量新暴露、切回预览 scrollTop 恒 0——测试裸 `dispatchEvent(new Event('scroll'))` 不带 wheel，被 `useMarkdownPreviewScrollRestore` 的 `restoringRef` guard 吞（只有 wheel/pointer/key 触发 `stop()` 退出 restore）；提交 `f7405724` 新增 reveal-line 分支+mermaid 同步渲染拉长 600ms 定时器激活既有脆弱点。判定：`--workers=1 --repeat-each=5` 确定性全挂+`git stash`+build 回 HEAD 仍挂。修：测试滚动前补真实 `WheelEvent`（产品零改动）。弯路：改产品用位置差异判接管会把 scroll-anchoring 漂移误判、打破切回。锚：`useMarkdownPreviewScrollRestore.ts`（`restoringRef`+`stop()`+`RESTORE_WINDOW_MS=600`）；激活提交 `f7405724`。

**案例 21（速记 30）**：`smoke.historyNavigation.spec.ts:15` `@p0` 本地全量偶发、goBack 后多一个 ghost pinned tab——`sharedApp` `window.reload()` 不拆主进程 workspace，上个 spec `openWorkspace` 的文件夹被 `WorkspaceRestoreContribution._restore` 灌回本测试 active group。修：`resetWindow` 在 `whenRestored` 后加 `closeWorkspace()`（切 no-workspace scope→`clearAll()`）+新探针。锚：`sharedApp.ts`（`resetWindow`）；`workspaceMainService.ts`（`closeFolder`）。

**案例 22（速记 31）**：`smoke.markdownMoveStaleDiagnostic.spec.ts:37` `@p1` 本地全量偶发（~1/5）、移动 B 后 A 链接没落盘、received 恒旧值——`closeAllEditors` 同步返回但 model dispose 异步，move 触发 bulk edit 时 `peek(A)` 返回即将 dispose 的孤儿 model→走 live 分支只改内存、孤儿 dispose 后丢失。无扰动探针（`__peekModelAlive`）抓到孤儿存活+磁盘旧值。修产品：`FileBulkEditService.apply` 加 `persistToDisk`（始终读写盘+`setValue` 同步残留 model），`_flush` 传 true。锚：`fileBulkEditService.ts`（`apply` persistToDisk）；`MarkdownUpdateLinksOnRenameContribution.ts`。

**案例 23（速记 32/33）**：一次启动优化提交同时打挂 4 spec（jsonOutline/outlineKeyboard 恒空、markdownEditing 命令被拒、markdownPreview languageId 恒空）——`git show <opt-commit>` 自述「workbench mounts before Monaco」，自补部分探针漏改同类。三类根因：(a) `OutlineService._recomputeSymbols` 拉取缺 `.catch()`→worker 冷启动 reject 断 retry 链（产品 bug）；(b) `runCommand(toggleTask)` 未注册时抛异常击穿 `expect.poll`（Playwright poll 对回调抛异常不重试）；(c) markdownPreview pure-preview `openWorkspace` 冷启动 10s poll 不够。修：(a) `.then().catch()` 双分支 `_maybeRetry`+注入 ILoggerService；(b) `runEdit` try/catch 仅 `/extension host may only execute/` 时 return undefined；(c) poll `{timeout:20000}`。守护测试：OutlineService REJECT 保活用例。锚：`OutlineService.ts`；`smoke.markdownEditing.spec.ts`（`runEdit`）；`smoke.markdownPreview.spec.ts`。

**案例 24（速记 34）**：案例 23 续集，补 `.catch()` 后 jsonOutline/outlineKeyboard 仍 Ubuntu 稳定挂 `[]`——`_maybeRetry` 只在 pull settle 后排下一次，JSON provider 底层 worker RPC/动态 import 冷启动永久 pending→then/catch 都不触发→链死。不对称：Windows✓/Ubuntu headless✗；TS/markdown outline 走 LSP 有自带超时不吃此路径。修：抽 `_pullWithTimeout`，`provideDocumentSymbols` race `PULL_TIMEOUT_MS=5000` timeout-reject 保证必 settle。守护测试：provider HANGS（`new Promise(()=>{})`）用例。锚：`OutlineService.ts`（`_pullWithTimeout`）；`JsonLanguageFeaturesContribution._provideDocumentSymbols`。

**案例 25（速记 15）**：`smoke.markdownLsp.spec.ts:59` `@p1` 仅 CI Windows、首 `activeEditorLanguageId` poll received 恒 `""`——`openWorkspace`-then-`openFileUri` 冷启动，首 poll 吃 config CI 默认 10s 在 Windows 不够（后续 poll 都已带 10s，唯首 poll 漏改，同案例 23c 未对齐兄弟）。修：首 poll 加 `{timeout:20000}`。锚：`smoke.markdownLsp.spec.ts`；对齐基准 `smoke.markdownPreview.spec.ts:1197/1238`。

**案例 26（速记 20/35）**：`smoke.output.spec.ts` `@p1`（纯 renderer，自己不 openWorkspace）本地全量偶发 `Target page ... has been closed`（**非** `Execution context destroyed`，故不被 `sharedApp.ts:105` catch 吞），栈在 `resetWindow` 的 `runCommand('clearHistory')`——whenRestored/closeWorkspace 都已过说明整个 page/主进程死了。根因同案例 12/16 parcel `0xC0000005`，但传播链是 sharedApp 模型：`sharedApp` worker 级共享 Electron，`resetWindow` 每 test 前无条件 `closeWorkspace()`→若同 worker 前个 test 开过 workspace，reload restore 它、reset `unwatch`→parcel `unsubscribe`，多 worker 并发 subscribe/unsubscribe 崩，output 只是恰好在 reset 撞上濒死进程的受害者（崩点飘忽、谁排在前谁背锅）。本地 4 轮(含全量 108 passed)复现不到=该 native 竞态低频。修：隔离加害者而非受害者——把主趟里所有 `sharedApp ∩ openWorkspace` 的 test 打 `{tag:'@serial'}`（workspace 2/editorTabDnD 2/inlineCompletion t1/nes t2/promptDragHighlight 2/simpleFileDialog 追加 2），主趟遂无 sharedApp test 进 workspace 状态→`closeFolder` 的 `if(_current===null)return` 让 closeWorkspace 变 no-op→主趟永不 parcel op，受害者自然不崩（主趟 108→98，@serial 趟接手，覆盖不变）。已在 @flaky/@regression 独立趟的 explorerDnD/explorerRowHeight/multiFileDragEditor/multiFilePromptDrag 天然不在主趟无需改。锚：`sharedApp.ts`（`resetWindow`→`closeWorkspace`）；`fileWatcherMainService._teardown`→`unsubscribe`；`workspaceMainService.closeFolder`。

**案例 27（速记 1/17/36）**：`smoke.activityBar.spec.ts:14` `@p0`「switches between Explorer and Search」`--repeat-each=16` 偶发 2 挂、`data-active-view-container` 期望 `search` received 恒 `explorer`（13 采样全 explorer、卡满 5s 不恢复）——`click(SEARCH)` 是 fire-once 把 SideBar 切离默认，但 renderer 在 **mount 后 fire-and-forget** 跑 `viewsService.reconcileFromStorage()`（`main.tsx` `Promise.all([...])`，`ViewsService.reconcileFromStorage` 内含 `INITIAL_LOAD_TIMEOUT_MS=500` fallback timer），无持久 views 时以破坏性 `_loadFromStorage`→`_seedDefaults()` 收尾回默认 Explorer。`sharedApp.resetWindow` 只 await `whenRestored`（=`Workbench.tsx` mount 时机，早于 reconcile），故迟到的 reconcite 在 click 后覆盖回 Explorer。免疫反例：`smoke.workspace.spec.ts:55` 断言的是 openWorkspace 后的**默认 Explorer**（与 reveal contribution + reconcile seed 三方一致），迟到不翻车，无需改。修：spec body 首行 `await workbench.waitForBootstrapFocusSettled()`（probe `bootstrapFocusSettled` 在同一 reconcile `Promise.all` 的 `.finally` resolve）。验证 16/16 绿。锚：`main.tsx:751`（reconcile `Promise.all`）；`ViewsService.ts`（`reconcileFromStorage`/`_seedDefaults`）；`WorkbenchPO.waitForBootstrapFocusSettled`。

**案例 28（速记 37）**：本机 Windows `pnpm e2e` 里 `smoke.editorRestore`/`smoke.outputRestore`/`smoke.layoutPersistence`/`smoke.agentOnboarding`/`smoke.agentsEmptySessionRestore` 等**裸 `electron.launch`**（自带 `launchWithState` 二次启动 Electron，带 `--inspect=0 --remote-debugging-port=0`）的 restore/relaunch 类 `@p1` 偶发/稳定报 `electron.launch: Process failed to launch!`（常 `<launched> pid=...` 后仍判失败、exitCode=9）——两类根因叠加：①**环境**：本机对 Electron 二次启动+CDP 连接的限制，`git stash` 清改动复跑仍挂即证与业务无关，走 fixture 的 `@p0` 全过、CI(ubuntu+windows) 亦过→**别当回归追,以 CI 为准**；②**真 bug**：Claude Code shell 设 `ELECTRON_RUN_AS_NODE=1`→Electron 当纯 Node 跑不认 `--remote-debugging-port`→子进程继承后报 `bad option: --remote-debugging-port=0`。修：所有自定义 `electron.launch` 的 `env` 先解构去掉该变量再展开：`const { ELECTRON_RUN_AS_NODE: _ignored, ...inheritedEnv } = process.env`（`fixtures/electronApp.ts`/`sharedApp.ts` 及各 spec 内 `launchWithState` 已统一）;本地全量偶发 launch 争用时 `--workers=2` 可稳。附：ext host 启动崩溃通知污染精确 count 断言→`smoke.notification.spec.ts` 触发前 `clearAll` 清噪声+文案 `filter` 替代 `toHaveCount(1)`。锚：`e2e/fixtures/electronApp.ts`（`inheritedEnv` 解构）；各 restore/relaunch spec 的 `launchWithState`。

**案例 29（速记 38）**：`smoke.search.spec.ts` `@regression` 仅 Windows CI 稳定失败，active editor 已正确打开目标文件，但预期 `file:///C:/Users/RUNNER%7E1/...`、received `file:///C:/Users/RUNNER~1/...`，retry 不改变差异——Windows runner 的 temp root 含 8.3 `~` 路段；Node `pathToFileURL` 与平台 `URI.file` 的序列化规则不同。修：断言使用 `URI.file(target).toString()`，保持和 E2E probe 的 `EditorInput.id` 同一 URI 表示域。审计 `pathToFileURL` 后，`smoke.folderDragNewWindow` 只把其作为 DnD 输入且最终对比 fsPath，无需变更。锚：`smoke.search.spec.ts`；`renderer/e2e/probe.ts`（`getActiveEditorUri`）；`platform/base/uri.ts`（`URI.file`）。

**案例 31（速记 15）**：案例 25 第三次复发——`smoke.markdownEditing.spec.ts:247`「operates on non-final lines and CRLF documents (regression)」`@p1 @regression` 仅 CI Windows，首 `activeEditorLanguageId` poll received 恒 `""`（10s 超时）——同 `openWorkspace`-then-`openFileUri` 冷启动，首 poll 无显式 timeout 继承 config CI 默认 10s，Windows 首帧 mount 不够；紧随的 `hasCommand` poll 反而都带了 15000ms，首 poll 是漏改薄弱点。本文件 4 个 test 全同型（line 89/259/326/357）。修：4 处首 poll 统一 `{timeout:20000}`（`replace_all`）。教训=修一处必整体扫同文件同型兄弟一并加固。锚：`smoke.markdownEditing.spec.ts`；基准 `smoke.markdownPreview.spec.ts`。

**案例 30（速记 39）**：`smoke.perforceChangelist.spec.ts:144` `@p1 @regression`「perforce.moveToReconcile lands the file in Changes to Reconcile」本机 `--workers=1 --repeat-each=N` **确定性**失败(0/5)、received 恒 `["default"]`(default 组 `hideWhenEmpty:false` 永远可见，真正失败是 reconcile 组恒空被隐藏)——三层复合根因，逐层剥开：**①盘符大小写**：失败路径 `moveToReconcile({scmResourceGroupId:'default'})` 靠 `pathsInChangelist('default')` 返回内核 `norm(clientFile)`=**小写盘符** `c:/`，喂给 `refreshReconcilePaths→reconcile -n <path>`，fake-p4 `targetsFromArgs` 用**大小写敏感** `toPosix(f)===toPosix(abs)` 精确匹配磁盘路径(大写 `C:/`)→匹配空；sibling(line 236)传显式 `perforce.file()`(大写)故过。真实 p4 Windows 大小写不敏感=fixture 失真。修：fake-p4 匹配改走 `normPath`(小写盘符归一)+`toDepotFile` 身份比较。**②fixture 架构失真**：fake-p4 把 `state.json` 写在**被 watch 的 workspace 目录内**(`.p4fake/state.json`)，每次 `saveState`(edit/revert 都写)触发 workspace watcher→额外 `refreshReconcilePaths([state.json])` 与被测操作交织；真实 p4 状态在服务器不在客户端工作区。修：`seedWorkspace` 把 state 移到独立 `mkdtempSync(tmpdir(),'ue2-p4-state-')`。**③真产品竞态**(剥完前两层仍 4/8 挂)：`refresh`(有 `_refreshing` 单飞)与 `refreshReconcilePaths`(无互斥)并发读改共享 `_reconcileFiles`/`_openedPaths`；watcher 触发的"文件仍 opened→其 `reconcile -n` 结果被 `_openedPaths` 过滤成空"的 pass 若**最后完成**，其 `mergeReconcile`(path 在 scanned 却不在 fresh)把文件删回→组恒空(诊断日志实锤:`_setReconcileFiles n=1` 后被 `n=0` clobber)。真实使用(p4 edit→改文件触发 watcher→点 Move to Reconcile)同样踩=真 bug。修：加串行链 `_reconcileChain`+`_runSerial(task)`，`refresh`/`refreshReconcilePaths` 全走它，每 pass 见前一 pass 已提交态；回归单测 `clientReconcilePersist.test.ts`「serializes overlapping reconcile passes」(受控 spawn 挂起首个 scan 断言第二个 scan 不早启)——临时 `task()` 直通验证禁串行时红。诊断法：fake-p4 append 时间戳日志到固定文件看 command/targets 时序；client.ts 临时 `DBGX` 写同类日志看谁最后 clobber `_setReconcileFiles`(速记 26 独立日志，完事删净)。验证 12/12 绿+耗时腰斩(竞态重试消失)。锚：`extensions/perforce/src/client.ts`(`_runSerial`/`refreshReconcilePaths`/`_refreshReconcile` cheap-path)；`apps/editor/e2e/fixtures/fake-p4.mjs`(`normPath`/`targetsFromArgs`)；`fixtures/perforceApp.ts`(`seedWorkspace` state 外置)；`workspaceWatcher.ts`(recursive watch)。

**案例 32（速记 10/18）**：案例 10 复发——`smoke.layoutPersistence.spec.ts:67`「sidebar width is restored from storage on launch」`@p1` 仅 Ubuntu CI `Test timeout of 30000ms exceeded`（**非** poll message，是 test 天花板击穿）——裸 `electron.launch` 冷启动重前置（seed workspace-scoped state→等 main workspace hydration→reconcile 落地 service poll→Allotment DOM 命令式收敛 poll），但**漏抄 `test.slow()`/`setTimeout`**（兄弟 markdown 系列全 `test.slow()`、aiCommit 系列 `setTimeout(120_000)` 都有，此 spec 独漏），且两 poll 各硬编码 `timeout:5000` 盖回 config CI 分档 10s（案例 7 反模式）；Ubuntu 2 核+2 workers 抢占下 launch+reconcile+DOM 收敛累计 >30s。修：`test.slow()`+两 poll 抬到 `15_000`。整体评估同款：`vscodeKeybindings` 两 test 有 `timeout:30_000` poll 但无 test 级保护（poll 窗口==全局 timeout→摆设，速记 10），一并补 `test.slow()`；`editorRestore`/`outputRestore`/`agentOnboarding`/`agentsEmptySessionRestore`/`viewMove` poll 窗口 ≤8s 单发不击穿 30s 暂不动。验证本地 3/3 绿（各 2–3s，本地无 CI 抢占）。锚：`smoke.layoutPersistence.spec.ts`；`smoke.vscodeKeybindings.spec.ts`；`e2e/playwright.config.ts:6`（global timeout 30s）/`:11`（expect CI 10s）。

**案例 33（速记 40）**：同一 Ubuntu shard 随机报 1 failed + 5 flaky，受害者横跨 `swarmReview/gotoSymbol/markdownPreview/windows/workspaceSettings`；全部未进入 test body，首窗口或第二窗口卡 `waitForFunction(__E2E__)`，截图纯黑，retry 多数恢复。trace 显示 launch/firstWindow 很快，随后 probe 恒无；提交边界是最后绿色 commit 后新增 `ElectronProtocol._frameAlive`。根因：main 在 `loadURL` 前建 protocol，`did-start-navigation` 关 gate；renderer ES module 在 `dom-ready` 前执行 `initializeRendererNls()` 发 IPC，`ChannelServer` 同步生成响应却被 `send()` 静默丢弃，renderer 的无超时 Promise 永久 pending，probe/React 均不会安装。修产品：`onIncoming` 路由到 `ElectronProtocol.acceptMessage`，先 `_frameAlive=true` 再 `Emitter.fire`，保证同一调用栈里的响应可发送；保留 `render-process-gone` 后无入站时的 dead-frame 防反馈保护。守护单测钉住「main-frame navigation 后、dom-ready 前收到首个 IPC 即恢复」。不要加 fixture timeout掩盖永久死锁。验证本轮 6 条 `--workers=2 --repeat-each=3` 共 18/18 绿。锚：`apps/editor/src/main/ipc/electronProtocol.ts`；`main.tsx initializeRendererNls/installE2EProbeIfEnabled`；`electronProtocol.test.ts`。

**案例 34（速记 23）**：案例 15 遗漏的 sibling——`smoke.agentsVirtualScrollRestore.spec.ts:27`「stays bottom-pinned and restores mid position ... when virtualized」`@p1 @regression` 仅 Ubuntu CI 偶发，场景 2 末尾 `expect(finalFrac).toBeLessThan(0.85)` received `0.97`。兄弟主 spec `smoke.agentsScrollRestore` 早已按案例 15 改成锚定消息索引，此虚拟化 sibling 场景 2 仍裸用 `frac=scrollTop/max`+3s 窗口是遗留薄弱点（速记 5）。诊断：加 `readTopIndex`（解析 `data-slot-key` 尾数，产品 message id 皆 `m<n>` 单调递增）+临时打印，`--repeat-each` 实测发现**恢复完全忠实**（切走前 anchor=11→恢复≈10-11 frac≈0.42；anchor=22→恢复=22 frac≈0.92，每次稳定），**非产品 bug**；flake 真因=像素中点 `(scrollHeight-clientHeight)/2` 在虚拟模式**双峰**落到 m11 或 m22（上方行 estimate 高度≠真实，即案例 15 estimate 坐标系伪影），落 m22 时忠实恢复 frac≈0.92 触发 `<0.85` 误判。绝对中部带无法同时覆盖 m11 和 m22（m22≈贴底，带宽到能含它就不再守护“跳到底”）。修：改**相对不变量**——`settleIndex()`（连续 4 帧同值）记切走前 anchor，恢复后 poll 连续 ≥600ms 落 `[anchor-2, anchor+2]`，仍守护重置到顶(→0/1)与坐标系丢失跳到底(→末尾)。验证 6/6 绿。锚：`smoke.agentsVirtualScrollRestore.spec.ts`；基准 `smoke.agentsScrollRestore.spec.ts`（`readTopIndex`/中部带）；`ChatBody.tsx`（`captureAnchor`/`slotKey`）；`acpSession.ts`（`_appendChunk`/`_appendMessage` 的 `m${++_msgCounter}`）。

**案例 35（速记 41）**：`smoke.perforceChangelist.spec.ts:58`「a default-changelist file row exposes "Move to Changelist" via right-click」`@p1 @regression` 本机 Windows 偶发，`page.evaluate` 报 `extension host may only execute _workbench.* commands ... not "perforce.edit"`（栈落 `WorkbenchPO.runCommand`）——扩展命令冷启动激活竞态：perforce `activate()` **早期** `PerforceClient.create`→`scm.createSourceControl`（`client.ts:219`）就建好 source control，spec 的 `getScmSourceControlCount()>0` 门控随即满足；但 contributed 命令 handler（`perforce.edit` 等）在**同一 `activate()` 后段**的 `context.subscriptions.push(...)` 同步批量注册（`extension.ts:231+`）。两者之间 fire `perforce.edit`→renderer 转发 host→host 无 handler 转回 renderer→`MainThreadCommands.$executeCommand` 拒（非 `_workbench.*`）。同 `smoke.markdownEditing` 的 `runEdit` 一型。修：加 `waitForPerforceCommands(workbench)` helper，`expect.poll` 轮询只读的 `perforce.refresh` 直到不再抛该错串（handler 一次性注册，一个就绪则全就绪），插到每个 fire `perforce.*` 的 test 的 `workbench.view.scm` 之后（5 处）。验证 `--repeat-each=3` 共 21/21 绿。同目录 `smoke.perforceCollectChanges`（靠文件改动+watcher 驱动，只在 row 可见后 click，天然等激活）、`smoke.perforceGraph`（`perforce-graph.view` 是 renderer Action2 恒有 handler）均不脆弱。锚：`apps/editor/e2e/specs/smoke.perforceChangelist.spec.ts`（`waitForPerforceCommands`）；`extensions/perforce/src/extension.ts`（activate 命令注册）；`MainThreadCommands.ts:74`（拒绝点）；`ExtensionPointTranslator.ts`（bootstrap proxy → `onCommand:` 激活）。

**案例 36（速记 42）**：`smoke.multiFilePromptDrag.spec.ts:113`「a CR-separated text/uri-list inserts one @mention per file」`@p1 @regression` 仅 Ubuntu CI，`expect(count).toBe(3)` received 恒 0（稳定卡值非波动，速记 1）——`openSessionWithPrompt` 只等 `acp-prompt-drop-host`（host `div` 立即渲染）就 drop，但 drop 处理里 `el.insertRef()` 依赖内嵌 Monaco 句柄（`PromptMonacoEditor.tsx:264` `if (!ed||!model||!m||!tracker) return`），Monaco 未 mount 完时静默 no-op；Ubuntu 慢启动下 drop 在 Monaco 就位前 fire→0 个 pill，drop fire-once 不幂等（速记 17）。同文件第一个 test（OS drag）因 `setInputFiles` 额外 await 给 Monaco 缓冲不易翻车；第二个 drop 前无缓冲稳定挂。键盘型兄弟 `smoke.agentsPromptHistory`（poll `editorTextFocus` 契约键=Monaco `onDidFocusEditorText` 设，天然门控就位）、`smoke.promptDragHighlight`（只 `dragover` 测 `dropActive` state 不碰句柄）均免疫——drop 型绕过焦点直接 dispatch 到 host div 是独有薄弱点。修：`openSessionWithPrompt`（两 test 共用）加 `expect(prompt.locator('.monaco-editor').first()).toBeVisible()` 门控编辑器就位，并把 `waitForTimeout(400)`+硬断言换 `expect.poll`（速记 4）。验证本地 `--repeat-each=3` 6/6 绿。锚：`smoke.multiFilePromptDrag.spec.ts`（`openSessionWithPrompt`）；`PromptMonacoEditor.tsx`（`insertRef` no-op 守卫）；`PromptInput.tsx:1052`（`onPromptDrop` `if(!el)return`）。

**案例 37（速记 43）**：`smoke.markdownRenameLinks.spec.ts:30`「rewrites a link in another file when its target is renamed」`@p1` 仅 Windows CI 偶发，`readWorkspaceFileText(a.md)` `toContain('./c.md')` received 恒原文 `./b.md`（15s 超时）——同族 `smoke.markdownMoveStaleDiagnostic` 先前也报同型（先加了 `EMPTY_EDIT_RETRIES` blind-retry，仍复发→说明治标）。真根因=**workspace-swap 的异步 re-pin 竞态**：idle `LanguageServicePrewarmContribution` 的 `runWhenIdle`→`activateByEvent('onLanguage:markdown')` 在 CI 抢占下可能**先于** `openWorkspace` 把 ext host 连到初始（空）workspace（`hasConn=true` 分支）；随后 `_onWorkspaceChanged` 异步 `_restart('workspace')` 重钉，而 rename 的改链接 flush 撞在“`host.stop` 旧 host→spawn 新 host”窗口里 fire→`executeContributedCommand('markdown.getRenameFileEdits')` 落到仍钉 `root=none` 的旧 host，其 `mdFsBridge.$findMarkdownFiles` 因 `root` undefined 直接 `return []`→无文档→空 edit。本地重连 <1 帧测不到，Windows Electron-as-node spawn 慢撑到秒级。排障关键：DBG 打印证明 happy path 恒 `hasConn=false`（`_connect` 干净重钉），强制 `hasConn=true`+2s stop 延迟才暴露；`console.error` 在 renderer 走 page console listener 可抓，ext host 侧 `console.error` 落子进程 stderr→主日志 `[stderr N]` 不进 page console。修：`ExtensionHostClientService` 加**同步 arm 的 re-pin barrier**——`onDidChangeWorkspace` 回调改非 async（首 await 前同步记 `_repinning=Promise`，`.finally` 清），新增 `_whenReady()`（drain barrier 再 `start()`），`activateByEvent`/`executeContributedCommand` 从 `await this.start()` 改 `await this._whenReady()`。retry 作为 defense-in-depth 保留（barrier 关掉 swap 窗口，retry 兜住 flush 抢在 swap 事件之前的首次尝试）。回归单测「blocks a command racing a workspace swap until the host is re-pinned」（fake host `stop` 挂起撑宽窗口，断言命令不早 resolve 且最终落 h2）。验证两 spec `--repeat-each=3` 6/6 绿+加 2s 延迟+0 retry 仍绿。锚：`ExtensionHostClientService.ts`（`_repinning`/`_whenReady`/`_onWorkspaceChanged`+`_repin`）；`extensions/markdown/src/mdFsBridge.ts`（`$findMarkdownFiles` `if(!root)return[]`）；`LanguageServicePrewarmContribution.ts`（idle prewarm 触发早连）；`MarkdownUpdateLinksOnRenameContribution.ts`（`_getRenameEditsWithRetry`）。

**案例 38（速记 44）**：`smoke.webviewDiff.spec.ts:90`「opens a webview diff via _workbench.openWebviewDiff and renders both sides」`@p1 @regression` 仅 Windows CI 偶发，前置 poll(`getActiveEditorTypeId()==='webviewDiff'`, 10s)**已通过**，紧接着 `expect(locator('[data-testid="webview-frame"]')).toBeVisible({timeout:10000})` 恒 `element(s) not found`（稳定卡值非波动，速记 1）——iframe 由 `CustomEditorHost` 在 `webviewService.openPanel()` 拿到 panel（=扩展宿主子进程激活 + `registerCustomEditorProvider` RPC **往返回来**）后才 mount `WebviewElement`；打开 `WebviewDiffInput`（读内存字节）不依赖 provider 故 poll 快速通过，但 iframe 就位要等 provider 注册往返，产品内部对这条链路容忍 **15s**（`CustomEditorHost.tsx:99-101` `setTimeout(...,15000)` 才 `setFailed`）。spec 只等 10s < 15s→“产品仍在合法等待、spec 已先放弃”。Windows CI 的 Electron-as-node spawn 慢（速记 43）把往返推过 10s。命中信号=同走 `openWebviewDiff`→iframe 链路的兄弟 `smoke.swarmSpreadsheetDiff.spec.ts` 用的是 `{timeout:15000}`，本 spec 10000=遗留薄弱点（速记 5/15）。修：iframe `toBeVisible` + marker `toHaveText` 从 10000 对齐产品 15s 容忍窗口（15000）+两个 test 补 `test.slow()`（扩展宿主冷启动，两 iframe 15s 等待链可能累计逼近 30s 全局 timeout，速记 10/18）；整文件扫同型——第二个 `native Explorer compare` test 更重（`openWorkspace`→Restricted→`trust.grant` relaunch host，速记 43）同改。验证本地 `smoke.webviewDiff` 2/2 绿（2.7s/4.3s，本地无 CI 抢占）。锚：`smoke.webviewDiff.spec.ts`；`CustomEditorHost.tsx`（`tryOpen`/15000 `setFailed`）；`WebviewService.openPanel`（无 provider 返 undefined）；对齐基准 `smoke.swarmSpreadsheetDiff.spec.ts`（15000）。

## 关键参考路径
- `apps/editor/e2e/specs/` —— 所有 e2e spec；`@p0` 阻塞 CI，`@p1` 次级
- `apps/editor/e2e/fixtures/electronApp.ts` —— `workbench` fixture、`runCommand`/`waitForRestored`/`statusBar` 封装、`closeApp`
- `apps/editor/src/renderer/e2e/probe.ts` —— `window.__E2E__` 探针
- `apps/editor/src/renderer/services/extensions/ExtensionHostClientService.ts` —— ext host 崩溃→通知（CI 噪音主源）

## 其它
- 后续用本 skill，发现新经验，需同步更新本文件
- 修复了某个e2e本身测试稳固性的问题，那么需要整体评估一下所有e2e是否也存在同样的问题，并一并修复
