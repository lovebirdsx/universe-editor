# 案例库（fix-ci-e2e-flake）

> 单编号自包含：每条 = **信号**（判别特征，一行）+ 现象/根因 → 修法+锚点。
> `../SKILL.md` 的「案例索引」是按信号速查的一行索引；命中后按案例号来本文件查阅。
> 新经验：本文件追加一条案例（信号行必填）+ SKILL.md 索引补一行。

**案例 1 — ext host 崩溃 toast 污染全局 count**
信号：toast/通知类全局 `toHaveCount` CI 偶发，count **波动**（1↔2）而非卡死。
`smoke.notification` toast 全局 `toHaveCount(0)` CI 偶发——ext host 崩溃发背景 Warning toast 污染全局 count。修：`.filter({hasText:'This is a test notification.'})` 收敛再 count。锚：`ExtensionHostClientService._handleCrash`；`NotificationsToast.tsx`。

**案例 2 — 同一噪音源污染全局单值/一次性状态**
信号：同轮挂多个业务无关断言（bell badge 恒 `"1"`、`panelVisible` 恒 false）=同一背景噪音源打挂一片。
同轮挂 bell badge `"1"` + `smoke.output` `panelVisible` false——同噪音源 `_handleCrash`（未读 toast→unreadCount；崩溃 error→`ErrorLogAutoRevealContribution._hasRevealed` 一次性置真）污染全局单值/一次性状态，spec 层无法 filter。修：`ExtensionsContribution._boot` 开头 `isE2E` 短路 return（先 grep 确认无 spec 依赖 ext host/SCM）。锚：`isE2E=window[E2E_PROBE_ENABLED_KEY]`（`shared/e2e/contract.ts`）。

**案例 3 — poll 预算被全局 test timeout 拦腰截断**
信号：硬失败形态是 `Test timeout`（非拿到 `[]`），而 spec 内 poll/service retry 累计预算远大于全局 `timeout:30_000`——宽 poll 是摆设，test 超时才是真天花板。
`smoke.outline` `@p1` 硬失败（超时非拿 `[]`）——全局 test `timeout:30_000` 拦腰截断 outline poll 累计 ~115s（`OutlineService` retry 180s），CI tsserver 冷启动+4 Electron 抢 2 核。修：冷启动 spec `test.slow()`；`expect.timeout=CI?10_000:5_000`；`workers=CI?2:4`；editorResolver 改“先 poll 目标项可见再 Enter”。锚：`playwright.config.ts`；`OutlineService.ts`（`PULL_RETRY_BUDGET_MS`）。

**案例 4 — CI 漏装非 workspace 的 vendor 产物（伪 flake）**
信号：**本地稳过+CI 每次必挂+received 恒空+retry 救不回**=伪 flake（CI 漏装运行时产物）；鉴别捷径=同类功能“A 能跑 B 不能”不对称（md LSP ✓/ts LSP ✗）。
`smoke.outline` Windows+Ubuntu 都总挂、received 恒 `[]`——outline 唯一依赖 ts LSP，ts 扩展硬依赖 `vendor/typescript-language-server`（非 pnpm workspace，CI 只 `pnpm install`+`build` 从未装它）。修：e2e job 加 `npm --prefix vendor/typescript-language-server ci`（不能调 `vendor-install.mjs`——它遍历 submodule 而 e2e job 无 `submodules: recursive`）。锚：`.github/workflows/ci.yml`；`scripts/release/vendor-install.mjs`。

**案例 5 — expect.poll 里盲按按键污染被测对象**
信号：`expect.poll` 谓词里 `keyboard.press`，received 稳定停错值且被测对象内容被改（链接被 Enter 打断）——按键落**当前焦点**，UI 未就位时打进编辑器 TEXTAREA。
`smoke.peekNavigation` Ubuntu 偶发、received 稳定 `…/a.md`——`expect.poll` 盲按 `keyboard.press('Enter')`，peek 打开前焦点在编辑器 TEXTAREA，Enter 在链接处插换行破坏 link→peek 永不打开。修：加探针 `isReferencePeekFocused()`（`document.activeElement?.closest('.ref-tree')`），只在聚焦时按。锚：`PeekNavigationContribution.ts`（`.ref-tree`）。

**案例 6 — fire 后页面导航再 evaluate 撞上下文销毁**
信号：报错 `Execution context was destroyed ... navigation`=harness 时序 flake 非断言失败；凡“fire 后页面 reload/导航再 `page.evaluate`”的 PO 步骤都要鲁棒。
`smoke.disposableLeak` Ubuntu 偶发 `Execution context was destroyed`、栈在 `WorkbenchPO.waitForRestartRestore()` 末步裸 `evaluate(whenRestored)`——`restart`→`win.reload()` 导航未 commit 时裸 evaluate 与上下文切换重合被销毁。修：抽 `_evaluateWhenRestored()`（捕获→重等 `domcontentloaded`+`__E2E__`→重试 ≤3）两处复用。锚：`WorkbenchPO.ts`；`windowActions.ts`（`RestartEditorAction`）。

**案例 7 — spec 局部硬编码 timeout 盖回 CI 分档（反复复发型）**
信号：config 已按 CI 分档 `expect.timeout`，spec 硬编码 `{timeout:5000}` 盖回本地值→分档失效；received 稳定空 `""`+等 Monaco 首帧。**命中信号=冷启动 spec 里首个 `activeEditorLanguageId` poll 无显式 timeout 而其后 `hasCommand` poll 已带 15000ms；修时整文件扫同型兄弟一并加固**。
`smoke.gotoSymbol` Windows 偶发、`activeEditorLanguageId` poll received 恒 `""`——首步硬编码 `{timeout:5000}` 盖回 CI 分档（config 已 CI 10s）。修：删这 4 个 spec 的 `{timeout:5000}` 继承 config 默认。**反向坑**：`openWorkspace`-then-`openFileUri` 的 spec 冷启动重，首 poll 吃 CI 默认 10s 在 Windows 仍不够→显式提到 `{timeout:20000}`，只改触发 flake 的谓词。锚：`smoke.{gotoSymbol,peekNavigation,markdownLsp,markdownPreview}.spec.ts`；`playwright.config.ts:11`。

**案例 8 — teardown 时 Windows 文件锁 rmdir 竞态**
信号：报错 `EBUSY/EPERM rmdir`、栈在 `finally`/teardown=Windows 文件锁清理竞态非断言失败（ENOTEMPTY 子型见案例 17）。
`smoke.editorTabDnD` 本地 Windows 偶发 `EBUSY rmdir`、栈在 `finally` 的 `fs.rm(tmpDir)`——app 未关（`await use(app)` 后才 `closeApp`），pinned 句柄+watcher 占目录，`force:true` 不重试 EBUSY。修：`fs.rm` 加 `maxRetries:10,retryDelay:200`。锚：`electronApp.ts`。

**案例 9 — runCommand(fire-once) 与 expect.poll(只读) 分离**
信号：`runCommand(动作)`+`expect.poll(只读状态)` 分离范式，动作 fire-once 且依赖异步就位前置→received 卡死初值+加宽窗口无效（命令在就位前 fire→静默 no-op，poll 不重 fire）。
`smoke.commandPalette` `@p0` 前置 `expect.poll(getContextKey('editorFocus')).toBe(true)` received 恒 false——`focusActiveEditorGroup` fire-once 依赖 Monaco `FileEditorRegistry.register`（model 异步后），冷启动命令在 register 前 fire→no-op。修：把 fire 放进 poll 谓词，抽 PO helper `focusActiveEditorGroup()`。锚：`editorActions.ts`；`FileEditor.tsx`（`applyModel` register）。

**案例 10 — test 级超时击穿（漏抄兄弟的 test.setTimeout）**
信号：报错 `Test timeout of 30000ms exceeded`（非 poll message）+received 初值=test 级超时击穿：重 spec 前置开销大，poll 窗口 ≥ 全局 test timeout 则天花板先到，常因裁剪兄弟 spec 漏抄 `test.setTimeout`。**裸 `electron.launch` 的 restore/persistence spec 是重灾区**（命中信号=`launchWithState` 冷启动却既无 `test.slow()` 又有 poll 窗口逼近 30s）；修时整体扫同目录裸 launch spec 有无同型漏保护。
`smoke.aiCommitMessage.spec.ts` 仅 Windows CI `Test timeout of 30000ms`、received 恒 0——与兄弟 `@p0` 逐行相同但漏抄 `test.setTimeout(120_000)`，poll 窗口 30_000==全局 timeout→摆设，Windows 慢击穿。修：加 `test.setTimeout(120_000)`、poll→60_000。锚：基准 `aiCommitMessage.generate.spec.ts`。

**案例 11 — 响应事件操作 in-flight 启动中的资源（真产品 bug）**
信号：同 flaky spec 二进宫、形态从 `Test timeout` 变 poll 窗口超时+received 恒初值=上轮只治天花板，真根因还在，八成产品 bug。
案例 10 续集，形态变 poll 自己 60s 窗口超时、`getScmSourceControlCount()` 恒 0——git `activate` fire-once，`openWorkspace` 依赖 host relaunch 重新 activate，但 `_connect` 在 `await host.start` 期间 `this._trusted` 未赋值→启动期 swap 落此窗口事件被丢弃→host pin 空 workspace。查链路要点：“响应事件去操作异步启动的资源”时若资源 in-flight 启动 Promise 没被 await，启动期事件被静默丢弃。修产品：`_onWorkspaceChanged` 开头 `await Promise.allSettled([_startingTrusted,_startingRestricted])` 再读 tier+回归单测。锚：`ExtensionHostClientService.ts`（`_onWorkspaceChanged`）。

**案例 12 — parcel watcher 多 worker native 崩溃 → @serial**
信号：报错 `Target page/context/browser has been closed`，先验 main 是否真崩：Windows 退出码 `3221225477`=`0xC0000005` native 段错误；**单实例（`--workers=1`）稳过、多实例才崩**=并发放大的跨进程竞态，进程内串行化无效。
`smoke.simpleFileDialog`「OK switches workspace」本地全量偶发 `Target page...has been closed`——决定性三组对照：①`--workers=1` 重复 ②`--workers=6` 重复 ③多实例+禁用嫌疑子系统，证 `@parcel/watcher` windows backend 多进程并发重订阅（切 workspace）native 竞态（常第三方 native 库），非产品 bug、真实用户不受影响。修：`@serial` 隔离到 `--workers=1`（`pnpm e2e`/`ci.yml` 各拆并行趟+串行趟）。锚：`smoke.simpleFileDialog.spec.ts`；`fileWatcherMainService._subscribe`。

**案例 13 — Worker teardown timeout：quit 链 veto / 强杀不彻底**
信号：报错「Worker teardown timeout」+所有测试 pass=关 app 链路卡死（无 fixture 名变体的排查法见案例 45）。
仅 Windows CI「Worker teardown timeout」+所有测试 pass——①graceful close 挂：`app.close()`→`before-quit` veto 链某 participant 在 headless 弹无人应答 modal→永不 resolve（`SessionShutdownParticipant._maybeVeto` 有 running session 时弹 modal）；②强杀不彻底：超时只 SIGKILL main PID，Windows 杀父不杀子→子进程孤儿占句柄。修：Fix A quit-chain 上会弹 modal 的 participant 按 `isE2E` 短路放行（`_maybeVeto` 算出 running 后 `return false`）；Fix B `closeApp` 超时 `taskkill /pid <pid> /T /F`。锚：`SessionShutdownParticipant.ts`；`e2e/fixtures/electronApp.ts`（`forceKillTree`）。

**案例 14 — 固定帧数 raf 采样读到虚拟化测量瞬时值**
信号：`--workers=1 --repeat-each=N` 全过、`--workers=4` 全挂（received 单调爬升=测量收敛被采样）=竞态 flake 非回归。注意 `--repeat-each` 默认吃 config workers（本仓库 4），必须显式 `--workers=1` 对照。
`smoke.agentsScrollbarStable` 仅 Windows `maxJump≈1.27`（阈值 0.25）、采样单调爬升到稳定——每段滚动只等固定 2 帧 raf 读 `scrollHeight`，@tanstack `ResizeObserver` 异步测量未完，采到过渡瞬时值。修：换 `settle()`（scrollHeight 连续 4 帧不变或耗尽 120 帧）。锚：`ChatBody.tsx`（`estimateRow`/`useVirtualizer`）。

**案例 15 — 滚动恢复用 frac 当落点代理脆弱 + echo 未收敛就贴底**
信号：滚动恢复断言被 RAF 过冲帧提前满足（frac=1 瞬间触顶/触底），或虚拟模式贴底停在离底几百 px。
①`smoke.agentsScrollRestore` `finalFrac=1`（期望 <0.85）——`poll(readFrac).toBeGreaterThan(0.15)` 被 RAF 过冲帧 frac=1 立刻满足；`frac=scrollTop/max` 当落点代理有三重脆弱：分母漂移（下方懒测量使 max 增大）、RAF 过冲、**虚拟模式像素中点是双峰的**（`scrollTop=(H-h)/2` 因上方行 estimate 高度≠真实高度，同一像素落到两条截然不同消息，如 m11 frac≈0.43 或 m22 frac≈0.92，忠实恢复到 m22 就 >0.85 误判）。②`smoke.vscodeKeybindings` 末步裸 `whenRestored` `Execution context destroyed`=案例 6 同型；sibling `agentsVirtualScrollRestore` scene1 贴底 distance 卡 ~342=缺 echo-settle 门控（`scrollToBottomStable` 10 帧 RAF 上限，echo 仍涌入/scrollHeight 还涨时上限耗尽）。修：①断言改锚定消息索引落绝对中部带 [4,12]、poll 连续 ≥600ms 在带内；②导出 `evaluateWhenRestored(page)` 复用；sibling 补 scroll 前 `poll(getAcpMessages().length).toBe(<full>)`+两帧 scrollHeight 相等门控，单次贴底改 `poll` 反复直到 distance<30。锚：`ChatBody.tsx`（`captureAnchor`/`scrollToBottomStable`）；`WorkbenchPO.ts`。

**案例 16 — parcel 崩溃同型：folderDrag/outline**
信号：同案例 12（`Target page...has been closed` + 多 worker 才崩）。
`smoke.folderDragNewWindow`+`smoke.outline` 本地 `--workers=4` 偶发——撞案例 12 同款 parcel watcher 跨进程崩溃（`openWorkspace`→`ExplorerTreeService._setRoot`→`fileWatcher.watch`→parcel `subscribe`；folderDrag 一次 test 两次 subscribe 高危）。`--workers=1` 12/12 过=并发放大。修：两 spec `@serial`。锚：`ExplorerTreeService._setRoot`→`fileWatcherMainService._subscribe`。

**案例 17 — ENOTEMPTY：主进程 storage 原子写 vs fixture 清目录**
信号：报错 `ENOTEMPTY rmdir`、删的目录冒出 `<id>.json.tmp`=主进程 storage debounced 原子写“持续写”竞态，`rmSync` 内建 maxRetries 救不回。
`smoke.multiFileDragEditor` 本地全量偶发、断言已过、报 `ENOTEMPTY rmdir workspaces` 栈在 `sharedApp.seedUserData`——`openWorkspace` 改主进程状态违反 `sharedApp` 契约，`resetWindow` 删 workspaces 瞬间主进程又写 `.tmp`→父 rmdir 撞 ENOTEMPTY，`rmSync` 内建 maxRetries 只覆盖叶子救不回。修：`seedUserData` 改 async，手写 `for(attempt<10)`+`await sleep(200)` 循环重试整个 `rmSync`。共享 fixture `sharedApp.seedUserData` 同吃。锚：`sharedApp.ts`（`seedUserData`）；`src/main/storage.ts`（`writeAll` tmp→rename）。

**案例 18 — CI Windows 8.3 短路径破碎 git 相对路径（伪 flake=真 bug）**
信号：spec 用 `mkdtemp(tmpdir())` 造真 git 仓库、断言依赖“spec 路径 ⋈ git 内部路径精确匹配”→仅 CI Windows 确定性失败（received 恒定、retry 救不回）。
`smoke.dirtyDiffPeek` 仅 CI Windows、poll received 恒 false（test 级已 120s）——CI Windows `os.tmpdir()` 是 8.3 短路径（`RUNNER~1`），`git rev-parse --show-toplevel` 总返回长路径→`relative(长,短)` 破碎→`git show` exit 128→`getHeadContent` 恒 null→regions 空。不对称：兄弟 aiCommitMessage 只验 SCM count>0 能过。修：`realpathSync.native(mkdtempSync(...))` 规范成长形（本地 no-op）。复现捷径：COM `GetFolder(dir).ShortPath` 取 8.3 名当 cwd。锚：`extensions/git/src/repository.ts`（`getHeadContent` `relative(this.root,absPath)`）；`DirtyDiffContribution._getHead`（缓存 null）。

**案例 19 — sessionChanges 三层复合（fixture 共享 tmp / 裸 evaluate no-op / 迟到 fs 事件盖 live 编辑）**
信号：新 spec `--workers=4 --repeat-each=N` 批量挂、received 恒 agent 写入值=多层叠加，须层层剥。
sessionChanges「diff 跟随修改自动更新」新 spec 批量挂——三层叠加：①`sessionDiffAgent.cjs` 写盘共享 tmp（sessionId `sd-1` 每进程从 1 起重复）被并发 worker/孤儿 agent 覆写→写盘路径加进程唯一 token `${process.pid}-${randomBytes(4)}`（改 `.cjs` 无需 rebuild，产品 contribution 改动要 build）；②`setActiveEditorText` 裸 evaluate model 未就位 no-op→放进 `expect.poll`；③“迟到的 fs 事件把 diff modified 侧从磁盘刷回、覆盖未保存 live 编辑”是真产品竞态——凡“响应 fs 变更刷新可能有 live 编辑缓冲的视图”都要先查 live model：`MonacoModelRegistry.peek(uri)` 有值用 `model.getValue()`，无才读盘，别用 `isDirty` 判断（React effect 异步更新留竞态窗口）。诊断坑：**给被多断言共用的既有探针追加调试后缀会让每个用它的断言都失败、最早的先挂、误导定位**——诊断只走新增独立探针方法或 `window.__X_DBG__` 数组+独立 probe 读回；`console.info` 太重会改时序（heisenbug），优先轻量内存数组累积事件序列由 probe 一次读回。锚：`sessionDiffAgent.cjs`；`ExternalChangeWatcher._refreshChangedDiffEditors`；`SessionChangesDiffSyncContribution._sync`。

**案例 20 — 测试合成 scroll 事件被产品“用户接管”判定吞掉**
信号：`--workers=1 --repeat-each=N` **确定性**全挂（非并发放大）+`git stash`+build 回 HEAD 仍挂=既有脆弱点被某提交激活，别当环境噪音。
`smoke.mermaidPreviewSwitch` 全量新暴露、切回预览 scrollTop 恒 0——测试裸 `dispatchEvent(new Event('scroll'))` 不带 wheel/pointerdown/keydown，凡产品靠这些事件区分“程序化 vs 用户接管”的逻辑都识别不到被吞（`useMarkdownPreviewScrollRestore` 的 `restoringRef` guard 只有 wheel/pointer/key 触发 `stop()` 退出 restore）；提交 `f7405724` 新增 reveal-line 分支+mermaid 同步渲染拉长 600ms 定时器激活既有脆弱点。修：测试滚动前补真实 `WheelEvent`（产品零改动）。弯路：改产品用位置差异判接管会把 scroll-anchoring 漂移误判、打破切回。锚：`useMarkdownPreviewScrollRestore.ts`（`restoringRef`+`stop()`+`RESTORE_WINDOW_MS=600`）。

**案例 21 — sharedApp 的 window reload 不拆主进程 workspace（ghost tab）**
信号：`sharedApp` spec 莫名多出 ghost pinned tab，形态因同 worker 上个 spec 是谁而飘忽。
`smoke.historyNavigation` `@p0` 本地全量偶发、goBack 后多一个 ghost pinned tab——`sharedApp` `window.reload()` 不拆主进程 workspace，上个 spec `openWorkspace` 的文件夹被 `WorkspaceRestoreContribution._restore` 灌回本测试 active group。修：`resetWindow` 在 `whenRestored` 后确定性 `closeWorkspace()`（切 no-workspace scope→`clearAll()`）拆掉 ghost groups+新探针。锚：`sharedApp.ts`（`resetWindow`）；`workspaceMainService.ts`（`closeFolder`）。

**案例 22 — 关闭编辑器后紧接 model 操作撞 dispose 竞态**
信号：“关闭编辑器后紧接着对同一文件做 model 操作”，修改静默丢失（~1/5 偶发）——`closeAllEditors` 同步返回但 React 卸载→dispose 异步，`peek(uri)` 拿到即将 dispose 的孤儿 model。
`smoke.markdownMoveStaleDiagnostic` 本地全量偶发、移动 B 后 A 链接没落盘、received 恒旧值——按 model 存活分“改内存 vs 落盘”的代码走 live 分支只改内存、孤儿 dispose 后丢失。无扰动探针（`__peekModelAlive`）抓到孤儿存活+磁盘旧值。修产品：`FileBulkEditService.apply` 加 `persistToDisk`（始终读写盘+`setValue` 同步残留 model），`_flush` 传 true。锚：`fileBulkEditService.ts`（`apply` persistToDisk）。

**案例 23 — 一次启动优化提交同时放大多个无关 spec 的就位竞态**
信号：多 spec 同提交后一起变红、形态各异，但 `git show <opt-commit>` 能读到“workbench mounts before Monaco”这类自述时序变更——`git show --stat` 看波及面，逐个失败对照“同类路径它修了没”，漏改处即回归。
一次启动优化提交同时打挂 4 spec（jsonOutline/outlineKeyboard 恒空、markdownEditing 命令被拒、markdownPreview languageId 恒空）。三类根因：(a) `OutlineService._recomputeSymbols` 拉取 `.then(...)` 缺 `.catch()`→冷启动 reject 掐断 retry 链（保活 `_maybeRetry` 只在 `.then` 调），结果恒空且 retry 救不回=伪 flake=真 bug；(b) `runCommand(toggleTask)` 未注册时抛异常击穿 `expect.poll`（Playwright poll 对回调抛异常不重试）；(c) markdownPreview pure-preview `openWorkspace` 冷启动 10s poll 不够。修：(a) `.then().catch()` 双分支都调 `_maybeRetry` 保活，catch 加 `_currentModel!==model` 早退+注入 ILoggerService；(b) `runEdit` try/catch 仅 `/extension host may only execute/` 时 return undefined；(c) poll `{timeout:20000}`。守护测试：OutlineService REJECT 保活用例。锚：`OutlineService.ts`；`smoke.markdownEditing.spec.ts`（`runEdit`）。

**案例 24 — pull 永久 pending 掐死 retry 链（then/catch 都不触发）**
信号：补了 `.catch()` 仍挂——`.catch()` 接得住 reject，接不住“永久 pending（既不 resolve 也不 reject）”；凡“从 then/catch 调度下一次重试”的结构，一次 hang 掐死全链。不对称鉴别：Windows✓/Ubuntu headless✗。
案例 23 续集，jsonOutline/outlineKeyboard 仍 Ubuntu 稳定挂 `[]`——`_maybeRetry` 只在 pull settle 后排下一次，JSON provider 底层 web-worker RPC/动态 import 冷启动永久排队时 then/catch 都不触发→链死；TS/markdown outline 走 LSP 有自带超时不吃此路径。修：抽 `_pullWithTimeout`，`provideDocumentSymbols` race `PULL_TIMEOUT_MS=5000` timeout-reject 保证必 settle。守护测试：provider HANGS（`new Promise(()=>{})`）用例。锚：`OutlineService.ts`（`_pullWithTimeout`）。

**案例 25 — 案例 7 复发：冷启动首 poll 漏带大 timeout**
信号：`openWorkspace`-then-`openFileUri` 冷启动 spec，首 `activeEditorLanguageId` poll 无显式 timeout 继承 config CI 默认 10s 不够，而其后 poll 都已带 10s+——首 poll 是漏改薄弱点。
`smoke.markdownLsp` 仅 CI Windows、首 `activeEditorLanguageId` poll received 恒 `""`。修：首 poll 加 `{timeout:20000}`。锚：基准 `smoke.markdownPreview.spec.ts:1197/1238`。

**案例 26 — sharedApp 模型下 parcel 崩溃的受害者飘忽**
信号：报 `Target page ... closed`（案例 12 型）的 spec **未必是肇事者**：崩点飘忽、谁排在前谁背锅，纯 renderer 的 spec 也会背锅。
`smoke.output`（纯 renderer，自己不 openWorkspace）本地全量偶发 `Target page ... has been closed`（**非** `Execution context destroyed`，故不被 `sharedApp.ts:105` catch 吞），栈在 `resetWindow` 的 `runCommand('clearHistory')`——根因同案例 12/16 parcel `0xC0000005`，但传播链是 sharedApp 模型：`resetWindow` 每 test 前无条件 `closeWorkspace()`→若同 worker 前个 test 开过 workspace，reload restore 它、reset `unwatch`→parcel `unsubscribe`，多 worker 并发 subscribe/unsubscribe 崩，output 只是恰好在 reset 撞上濒死进程的受害者。修：隔离**加害者**（`sharedApp ∩ openWorkspace` 的 test 全打 `{tag:'@serial'}`）而非受害者——主趟遂无 sharedApp test 进 workspace 状态→`closeFolder` 的 `if(_current===null)return` 让 closeWorkspace 变 no-op→主趟永不 parcel op，覆盖不变。判别：`for f in $(grep -l fixtures/sharedApp *.spec.ts);do grep -q openWorkspace $f&&echo $f;done`。锚：`sharedApp.ts`（`resetWindow`→`closeWorkspace`）；`fileWatcherMainService._teardown`→`unsubscribe`。

**案例 27 — mount 后 fire-and-forget 的 reconcile 覆盖用户刚切的状态**
信号：fire-once click 把状态切离默认（如 SideBar→Search）→received **恒卡默认值**不恢复；断言**默认态本身**的 spec 免疫。
`smoke.activityBar` `@p0`「switches between Explorer and Search」`--repeat-each=16` 偶发 2 挂、`data-active-view-container` 期望 `search` received 恒 `explorer`（13 采样全 explorer、卡满 5s 不恢复）——`click(SEARCH)` 是 fire-once 把 SideBar 切离默认，但 renderer 在 **mount 后 fire-and-forget** 跑 `viewsService.reconcileFromStorage()`（含 `INITIAL_LOAD_TIMEOUT_MS=500` fallback timer），无持久 views 时以破坏性 `_loadFromStorage`→`_seedDefaults()` 收尾回默认 Explorer。`sharedApp.resetWindow` 只 await `whenRestored`（=mount 时机，早于 reconcile），故迟到的 reconcile 在 click 后覆盖回 Explorer。免疫反例：`smoke.workspace` 断言 openWorkspace 后的**默认 Explorer**（与 reveal + reconcile seed 三方一致）。修：spec body 首行 `await workbench.waitForBootstrapFocusSettled()`（probe 在同一 reconcile `Promise.all` 的 `.finally` resolve，保证已 settle）。锚：`main.tsx:751`；`ViewsService.ts`（`reconcileFromStorage`/`_seedDefaults`）。

**案例 28 — 本机 Windows 裸 electron.launch 二次启动失败（环境，不打 tag）**
信号：**本机 Windows** 裸 `electron.launch`（自带 `launchWithState`，带 `--inspect=0 --remote-debugging-port=0` 二次启动 Electron）的 restore/persistence/relaunch 类 `@p1` 偶发/稳定报 `electron.launch: Process failed to launch!`（`<launched> pid=...` 后仍判失败、exitCode=9）；`git stash` 清改动复跑仍挂=环境，失败集合 ⊆ 裸启动 `@p1` 且报此串→别当回归追，以 CI 为准（CI 正常通过，故**不打 `@flaky`**，否则削弱 CI 覆盖）。
本机 Windows `pnpm e2e` 里 `smoke.editorRestore`/`smoke.outputRestore`/`smoke.layoutPersistence`/`smoke.agentOnboarding`/`smoke.agentsEmptySessionRestore` 等裸启动 `@p1` 报 launch 失败——两类根因叠加：①**环境**：本机对 Electron 二次启动+CDP 连接的限制，走 fixture 的 `@p0` 全过、CI 亦过；②**真 bug**：Claude Code shell 设 `ELECTRON_RUN_AS_NODE=1`→Electron 当纯 Node 跑、不认 `--remote-debugging-port`→所有子进程继承后报 `bad option`；凡新写自定义 `electron.launch`，`env` 里先解构去掉它：`const { ELECTRON_RUN_AS_NODE: _ignored, ...inheritedEnv } = process.env`（`fixtures/electronApp.ts`/`sharedApp.ts` 及各 spec 内 `launchWithState` 已统一）。本地全量偶发 launch 争用时 `--workers=2` 可稳。附：ext host 启动崩溃通知污染精确 count 断言→`smoke.notification` 触发前 `clearAll` 清噪声+文案 `filter` 替代 `toHaveCount(1)`。锚：`e2e/fixtures/electronApp.ts`（`inheritedEnv` 解构）。

**案例 29 — URI 断言的表示域不一致（%7E vs ~）**
信号：URI 字符串断言 received 稳定仅差 `%7E`/`~`、retry 不改变差异=断言表示域不一致，不是时序或产品回归。
`smoke.search` `@regression` 仅 Windows CI 稳定失败，active editor 已正确打开目标文件，但预期 `file:///C:/Users/RUNNER%7E1/...`、received `file:///C:/Users/RUNNER~1/...`——Windows runner 的 `%TEMP%` 含 8.3 `~` 路段；Node `pathToFileURL(path)` 将 `~` 编为 `%7E`，但产品 `URI.file(path).toString()` 保持 `~`。修：预期也用 `URI.file(target).toString()`，保持和 E2E probe 的 `EditorInput.id` 同一 URI 表示域；先审计 `pathToFileURL` 的其它用法，只有比较 URI 字符串的断言须改。锚：`renderer/e2e/probe.ts`（`getActiveEditorUri`）；`platform/base/uri.ts`（`URI.file`）。

**案例 30 — @regression 确定性失败的复合根因要层层剥**
信号：`@regression`+本机 `--workers=1 --repeat-each=N` **确定性**失败（非并发放大）+received 恒初值+retry 救不回=伪 flake=真 bug/fixture 失真，别当环境噪音；一次修复只让通过率 0→部分=还有第二、三层，继续加诊断别收手。
`smoke.perforceChangelist`「perforce.moveToReconcile lands the file in Changes to Reconcile」`@p1 @regression` 本机确定性失败(0/5)、received 恒 `["default"]`(default 组 `hideWhenEmpty:false` 永远可见，真正失败是 reconcile 组恒空被隐藏)——三层复合根因：**①盘符大小写**：`pathsInChangelist('default')` 返回内核 `norm(clientFile)`=**小写盘符** `c:/`，fake-p4 `targetsFromArgs` 用**大小写敏感**精确匹配磁盘路径(大写 `C:/`)→匹配空；sibling 传显式 `perforce.file()`(大写)故过。真实 p4 Windows 大小写不敏感=fixture 失真。修：fake-p4 匹配改走 `normPath`+`toDepotFile` 身份比较。**②fixture 架构失真**：fake-p4 把 `state.json` 写在**被 watch 的 workspace 目录内**(`.p4fake/`)，每次 `saveState` 触发 workspace watcher→额外 `refreshReconcilePaths` 与被测操作交织；真实 p4 状态在服务器不在工作区。修：state 移到独立 `mkdtempSync`。**③真产品竞态**(剥完前两层仍 4/8 挂)：`refresh`(有单飞)与 `refreshReconcilePaths`(无互斥)并发读改共享 `_reconcileFiles`/`_openedPaths`；watcher 触发的"文件仍 opened→scan 结果被过滤成空"的 pass 若**最后完成**，其 `mergeReconcile` 把文件删回→组恒空(诊断日志实锤:`_setReconcileFiles n=1` 后被 `n=0` clobber)。真实使用(p4 edit→改文件触发 watcher→点 Move to Reconcile)同样踩=真 bug。修：加串行链 `_runSerial`，refresh 类操作全走它，每 pass 见前一 pass 已提交态；回归单测「serializes overlapping reconcile passes」。诊断法：fake CLI 侧+产品侧各 append 时间戳日志到固定文件，看命令时序+谁最后 clobber（独立日志别污染既有探针，完事删净）。锚：`extensions/perforce/src/client.ts`(`_runSerial`)；`e2e/fixtures/fake-p4.mjs`(`normPath`)；`fixtures/perforceApp.ts`(state 外置)。

**案例 31 — 案例 7 第三次复发（markdownEditing）**
信号：同案例 7/25——冷启动首 `activeEditorLanguageId` poll 无显式 timeout，紧随的 `hasCommand` poll 反而都带了 15000ms。
`smoke.markdownEditing`「operates on non-final lines and CRLF documents (regression)」仅 CI Windows，首 `activeEditorLanguageId` poll received 恒 `""`（10s 超时）——同案例 23c 未对齐兄弟。本文件 4 个 test 全同型。修：4 处首 poll 统一 `{timeout:20000}`（`replace_all`）。教训=修一处必整体扫同文件同型兄弟一并加固。锚：基准 `smoke.markdownPreview.spec.ts`。

**案例 32 — 案例 10 复发：裸 launch spec 漏抄 test.slow()**
信号：裸 `electron.launch` 冷启动重前置 spec 既无 `test.slow()` 又有 poll 窗口逼近 30s（命中信号），且局部硬编码 timeout 盖回 CI 分档。
`smoke.layoutPersistence`「sidebar width is restored from storage on launch」仅 Ubuntu CI `Test timeout of 30000ms exceeded`——裸 `electron.launch` 冷启动重前置（seed workspace state→hydration→reconcile 落地→Allotment DOM 收敛），但**漏抄 `test.slow()`**（兄弟 markdown 系列全有，此 spec 独漏），且两 poll 各硬编码 `timeout:5000` 盖回 config CI 分档 10s（案例 7 反模式）；Ubuntu 2 核+2 workers 抢占下累计 >30s。修：`test.slow()`+两 poll 抬到 `15_000`。整体评估同款：`vscodeKeybindings` 两 test 有 `timeout:30_000` poll 但无 test 级保护（poll 窗口==全局 timeout→摆设），一并补 `test.slow()`；`editorRestore`/`outputRestore`/`agentOnboarding`/`agentsEmptySessionRestore`/`viewMove` poll 窗口 ≤8s 单发不击穿 30s 暂不动。锚：`smoke.layoutPersistence.spec.ts`；`playwright.config.ts:6`/`:11`。

**案例 33 — probe 前 bootstrap RPC 被 dead-frame gate 永久丢弃（已修复）**
信号：多条**业务无关** spec 同轮随机挂在 fixture `waitForFunction(__E2E__)`，trace 显示 Electron/window 很快创建、页面纯黑、probe 30s 恒无，retry 又恢复——先查 probe 前 bootstrap RPC 永久 pending，别把它当慢启动加 timeout。
同一 Ubuntu shard 随机报 1 failed + 5 flaky，受害者横跨 `swarmReview/gotoSymbol/markdownPreview/windows/workspaceSettings`；全部未进入 test body，首窗口或第二窗口卡 `waitForFunction(__E2E__)`，截图纯黑，retry 多数恢复。提交边界是最后绿色 commit 后新增 `ElectronProtocol._frameAlive`。根因：main 在 `loadURL` 前建 protocol，`did-start-navigation` 关 gate；renderer ES module 在 `dom-ready` 前执行 `initializeRendererNls()` 发 IPC，`ChannelServer` 同步生成响应却被 `send()` 静默丢弃，renderer 的无超时 Promise 永久 pending，probe/React 均不会安装。修产品：`onIncoming` 路由到 `ElectronProtocol.acceptMessage`，先 `_frameAlive=true` 再 `Emitter.fire`，保证同一调用栈里的响应可发送（入站 IPC 本身就是新 frame 存活的强信号，必须先重开 gate 再分发请求）；保留 `render-process-gone` 后无入站时的 dead-frame 防反馈保护。守护单测钉住「main-frame navigation 后、dom-ready 前收到首个 IPC 即恢复」。锚：`apps/editor/src/main/ipc/electronProtocol.ts`；`electronProtocol.test.ts`。

**案例 34 — 虚拟模式像素中点双峰：换相对不变量锚断言**
信号：虚拟化滚动恢复断言 `finalFrac` 超阈值，但加诊断实测**恢复完全忠实**=落点代理（像素中点）双峰问题，非产品 bug。
`smoke.agentsVirtualScrollRestore`「stays bottom-pinned and restores mid position ... when virtualized」仅 Ubuntu CI 偶发，场景 2 末尾 `expect(finalFrac).toBeLessThan(0.85)` received `0.97`——兄弟主 spec 早已按案例 15 改成锚定消息索引，此虚拟化 sibling 场景 2 仍裸用 `frac=scrollTop/max`+3s 窗口是遗留薄弱点。诊断：加 `readTopIndex`（解析 `data-slot-key` 尾数）+临时打印，`--repeat-each` 实测发现恢复完全忠实（anchor=11→恢复≈10-11 frac≈0.42；anchor=22→恢复=22 frac≈0.92）；flake 真因=像素中点在虚拟模式**双峰**落到 m11 或 m22（上方行 estimate 高度≠真实），落 m22 时忠实恢复 frac≈0.92 触发 `<0.85` 误判。绝对中部带无法同时覆盖两者（带宽到能含 m22 就不再守护“跳到底”）。修：改**相对不变量**——`settleIndex()`（连续 4 帧同值）记切走前 anchor，恢复后 poll 连续 ≥600ms 落 `[anchor-2, anchor+2]`，仍守护重置到顶与跳到底；**能定位到具体消息时优先相对不变量而非绝对带**，相对不变量对双峰中点免疫。锚：`smoke.agentsVirtualScrollRestore.spec.ts`；基准 `smoke.agentsScrollRestore.spec.ts`；`acpSession.ts`（`m${++_msgCounter}`）。

**案例 35 — 扩展命令冷启动激活竞态（host 拒非 _workbench.* 命令）**
信号：报错 `extension host may only execute _workbench.* commands ... not "<ext.cmd>"`；命中信号=spec 只 poll 激活早期信号（如 SCM count>0）却紧接着裸 `runCommand('<ext>.<cmd>')`；修时整文件扫同型 test 一并加固。
`smoke.perforceChangelist`「a default-changelist file row exposes "Move to Changelist" via right-click」本机 Windows 偶发——perforce `activate()` **早期** `PerforceClient.create`→`scm.createSourceControl` 就建好 source control，spec 的 `getScmSourceControlCount()>0` 门控随即满足；但 contributed 命令 handler（`perforce.edit` 等）在**同一 `activate()` 后段**才同步批量注册。两者之间 fire `perforce.edit`→renderer 转发 host→host 无 handler 转回 renderer→被 `MainThreadCommands.$executeCommand` 拒（只准 `_workbench.*`）。修：加 `waitForPerforceCommands(workbench)` helper，`expect.poll` 轮询只读的 `perforce.refresh` 直到不再抛该错串（handler 一次性注册，一个就绪则全就绪），插到每个 fire `perforce.*` 的 test 的 `workbench.view.scm` 之后（5 处）。同目录 `smoke.perforceCollectChanges`（靠 watcher 驱动、只在 row 可见后 click）、`smoke.perforceGraph`（renderer Action2 恒有 handler）均不脆弱。锚：`smoke.perforceChangelist.spec.ts`（`waitForPerforceCommands`）；`MainThreadCommands.ts:74`（拒绝点）。
变体（2026-08，helper 已上移 `fixtures/perforceApp.ts` 导出）：`perforceGraphReveal`「reveals a seeded changelist via the _workbench bridge」同窗口不同症状——spec 不直接 fire `perforce.*`，而是 `_workbench.openPerforceGraph` 打开图谱后**编辑器内部** `load()` 立即 `perforce-graph.getChanges`，落在同一未注册窗口时**不抛错而是静默 resolve undefined** → 图谱卡在 "Perforce Graph is unavailable"（`load()` 无重试）→ row 永不出现，5s 超时 "element(s) not found"。首试失败率随时序漂移可高达 4/5。修：`openSeededWorkspace` 在 SCM-count 门控后补 `waitForPerforceCommands(workbench)`。教训：**同类产品内 fire 扩展命令的入口（编辑器 mount 即查询）也踩此竞态**，且症状退化为静默 null 而非显式拒绝，排查时先怀疑「命令 handler 是否已注册」。

**案例 36 — drop 在 Monaco 句柄就位前 fire（拖拽独有薄弱点）**
信号：拖拽 spec received 恒 0（**稳定卡值**非波动）；**键盘型 spec 免疫**（poll `editorTextFocus` 契约键=Monaco `onDidFocusEditorText` 设，天然门控就位），drop 型绕过焦点直接 dispatch 到 host div，故独有此薄弱点。
`smoke.multiFilePromptDrag`「a CR-separated text/uri-list inserts one @mention per file」仅 Ubuntu CI，`expect(count).toBe(3)` received 恒 0——`openSessionWithPrompt` 只等 `acp-prompt-drop-host`（host `div` 立即渲染）就 drop，但 drop 处理里 `el.insertRef()` 依赖内嵌 Monaco 句柄（`if (!ed||!model||!m||!tracker) return`），Monaco 未 mount 完时静默 no-op；Ubuntu 慢启动下 drop 在 Monaco 就位前 fire→0 个 pill，drop fire-once 不幂等（不能放进 poll 反复 fire）。同文件第一个 test（OS drag）因 `setInputFiles` 额外 await 给了缓冲不易翻车；`smoke.promptDragHighlight`（只 `dragover` 不碰句柄）免疫。修：`openSessionWithPrompt` 加 `expect(prompt.locator('.monaco-editor').first()).toBeVisible()` 门控编辑器就位（=编辑器 create 返回+句柄 refs 就位），固定 `waitForTimeout`+硬断言换 `expect.poll`。锚：`smoke.multiFilePromptDrag.spec.ts`；`PromptMonacoEditor.tsx`（`insertRef` no-op 守卫）。

**案例 37 — workspace-swap 的异步 re-pin 窗口吞命令**
信号：命令在 workspace-swap 后紧接 fire→received 恒初值（scan 空/空 edit）；**blind-retry 治标不治本**（重连比 retry 预算慢就仍挂）；Windows CI 的 Electron-as-node spawn 慢把窗口撑到秒级，本地重连 <1 帧几乎测不到。
`smoke.markdownRenameLinks`「rewrites a link in another file when its target is renamed」仅 Windows CI 偶发，`readWorkspaceFileText(a.md)` `toContain('./c.md')` received 恒原文 `./b.md`（15s 超时）——同族 `smoke.markdownMoveStaleDiagnostic` 先前也报同型（先加了 `EMPTY_EDIT_RETRIES` blind-retry，仍复发→治标）。真根因=ext host 在 launch 时把 `workspace.rootPath` 钉死，变更靠 `_onWorkspaceChanged` **异步** `_restart` 重钉：idle `LanguageServicePrewarmContribution` 的 `runWhenIdle`→`activateByEvent('onLanguage:markdown')` 在 CI 抢占下可能**先于** `openWorkspace` 把 ext host 连到初始（空）workspace；随后 `_restart` 重钉，而 rename 的改链接 flush 撞在“`host.stop` 旧 host→spawn 新 host”窗口里 fire→落到仍钉 `root=none` 的旧 host，其 `$findMarkdownFiles` 因 `root` undefined 直接 `return []`→空 edit。排障关键：DBG 打印证明 happy path 恒 `hasConn=false`，强制 `hasConn=true`+2s stop 延迟才暴露；ext host 侧 `console.error` 落子进程 stderr→主日志 `[stderr N]` 不进 page console。复现捷径=强制 `activateByEvent` 先连初始空 workspace+在 `_restart` 的 `host.stop` 后注入 `setTimeout` 撑宽窗口。修=产品侧**同步 arm re-pin barrier**——`onDidChangeWorkspace` 回调改非 async（首 await 前同步记 `_repinning=Promise`，`.finally` 清），新增 `_whenReady()`（drain barrier 再 `start()`），`activateByEvent`/`executeContributedCommand` 改 `await this._whenReady()`，命令必落在重钉后的 host。retry 作为 defense-in-depth 保留。回归单测「blocks a command racing a workspace swap until the host is re-pinned」（fake host `stop` 挂起撑宽窗口）。锚：`ExtensionHostClientService.ts`（`_repinning`/`_whenReady`）；`extensions/markdown/src/mdFsBridge.ts`（`$findMarkdownFiles` `if(!root)return[]`）。

**案例 38 — 等满的时间恰是产品自己的“放弃点”=根本没就位（产品 bug）**
信号：`toBeVisible` 等满 timeout 恒 `element(s) not found` 且该 timeout == 产品自己的"放弃点"→不是慢竞态，是根本没就位；Ubuntu（非仅 Windows）也稳挂。**反模式警示**："产品容忍 Ns、spec 只等 Ms<N,加到 N 对齐"的归因要先验证 N 是"缓冲"还是"放弃点"，两者反义。
`smoke.webviewDiff`「opens a webview diff via _workbench.openWebviewDiff and renders both sides」`@p1 @regression`。**先前误修（84ac521d）已回退**：那版把 iframe `toBeVisible` 从 10s 提到 15s，归因“Windows 慢启动、产品容忍 15s、spec 只等 10s 先放弃”——**被本轮 Ubuntu CI 证伪**：等满 15s 仍 `element(s) not found`，而 15s 恰是 `CustomEditorHost` `setTimeout(...,15000)` 后 `setFailed`（**放弃** provider）的时刻：等到产品自己都放弃了 iframe 仍不出现＝provider 根本没注册。**真根因（产品 bug）**：`installVsixExtension`→`refreshExtensions()`→`_restart('workspace')`＝stop 旧 host(conn A，初始 boot 扫描、**不含**刚装的 vsix)→起新 host(conn B，才扫到)。而 `CustomEditorHost` 只在 mount 时 `activateByEvent(onCustomEditor:<vt>)` **一次**（fire-and-forget）；install→restart 竞态下这唯一一次 activate 若落在 conn A→无 provider；`_restart` 结尾只重放 `STARTUP_ACTIVATION` **不重放** `onCustomEditor:`，而 `CustomEditorHost` 的重试只挂 `onDidChangeProviders`——provider 从未注册→该事件永不 fire→15s `setFailed`。Windows 偶尔过＝activate 恰好落 conn B。**普遍性**：任何已打开 custom editor 遇 host 重启（workspace swap/crash 恢复）都会变空白。修=产品侧 `CustomEditorHost` 订阅 `onDidChangeContributions`，fire 时若 `!opened` 则重新 `activateByEvent`＋`tryOpen` 成功时 `setFailed(false)`；spec 侧回退两处 timeout 15s→10s，保留 `test.slow()`。锚：`CustomEditorHost.tsx`（`activateOwner`/`onDidChangeContributions` 订阅）；同型对照 `smoke.pdfCustomEditorRace`（测 install→open 竞态，天然依赖此修复）。

**案例 39 — workspace 事件监听者无条件 reveal 抢占用户容器选择**
信号：打开工作区后紧接 `click(侧栏图标)`→received 恒默认容器(explorer)；error-context 的 DOM 快照显示 **`Explorer [pressed]`**；多 spec 轮流挂、单跑必过（事件早到无害、晚到覆盖，胜负取决于机器负载）。
`smoke.search` / `smoke.searchScroll` / `smoke.searchScrollRestore` 三 spec `@p1 @regression` 满载/慢机器上不同轮次**轮流**挂，`getByTestId('search-view')` `toBeVisible` 5s 恒 `element(s) not found`——三 spec 路径相同：`openWorkspace(dir)`→`activityBar.click(SEARCH)`→期望 search-view 可见。根因：`WorkspaceExplorerRevealContribution` 监听 `onDidChangeWorkspace`，打开文件夹时**无条件** `openViewContainer('workbench.view.explorer')`；事件**晚到**（点击后）时把刚点的 search 抢回 explorer→SearchView 卸载→testid 消失。**排障弯路**：先误判在 `ViewsService._reload`/`_reconcileActive`，加临时日志实测 DROP 从不出现→排除；顺「还有谁 `openViewContainer('explorer')`」grep 才找到真凶 contribution（**别去 ViewsService 内部找**，`_reconcileActive`/`_loadFromStorage` 是幌子）。诊断 spec 结构需**贴合真实失败**（点击后立即断言，别先 poll 缓冲给事件到达时间）。修产品=reveal **条件化**：`_revealExplorer` 先读 `getActiveViewContainerId(SideBar)`，仅当 `undefined` 或仍是 explorer 时才切（对齐 VSCode）。附带 `ViewsService._reload` 改「以当前选择为基底 merge 持久化值」。回归单测：「preserves a non-Explorer container the user already activated」。锚：`WorkspaceExplorerRevealContribution.ts`（`_revealExplorer` 条件化）；`ActivityBar.tsx`（`handleClick` toggle 语义）。

**案例 40 — search 系再挂：fixture 内存 bucket 泄漏 + 焦点恢复抢容器（两层复合）**
信号：案例 39 修复后仍未根治，同形态+快照 `Explorer [pressed]`；**「加 sleep 变绿」不代表是 fixture 竞速**（可能只是给了第二竞态 settle 时间）。
`smoke.searchScroll` / `smoke.searchScrollRestore` CI Ubuntu 偶发再挂——**复合根因两层**：**第一层=fixture 状态泄漏**——`resetWindow` 只「wipe 磁盘 workspaces/+reload」，但 WORKSPACE-scope storage backend 是 **main 进程内存对象**（`storageMainService._workspace`），reload 不关 folder 也不重建它；前个 search 系 spec 结尾 `click(SEARCH)` 的 200ms debounce persist 把 `{SideBar:search}` 落进该内存 bucket，新 renderer 的 `reconcileFromStorage` 若赢得 reload 后补发 closeWorkspace 的 500ms settle 竞速→读回 search→本 spec `click(SEARCH)` 命中 ActivityBar **toggle** 关闭容器→active=undefined→reveal 合法开回 explorer。命中信号=挂的 spec 前驱是「点过同一侧栏容器的 spec」；复现=whenRestored 后临时 sleep 700ms 撑宽竞速窗口→确定性挂。修=`resetWindow` 在 **reload 前**先 `closeWorkspace()` 拆内存 bucket。**第二层=真产品竞态**——第一层修后仍挂且「sleep 在时全绿」（sleep 不是修复而是给了第二竞态 settle 时间）；按案例 19 的诊断法上 `__VIEWS_DBG__` 内存数组埋点抓到决定性时序：`839 handleClick(search) branch=OPEN → 849 openViewContainer(explorer)` 且**无 revealExplorer 日志**→排除 reveal，顺 `LayoutService.focusView`（唯一通用 `openViewContainer(descriptor.containerId)`）反查到 `WorkspaceFocusRestoreContribution`：`onDidChangeWorkspace` 开 **1500ms restore 窗口**（80ms debounce），无编辑器时 `restoreWorkbenchFocus` 无条件 `focusView(explorer.tree)`→把 10ms 前用户刚点的 search 盖回 explorer（真实用户打开文件夹 1.5s 内点 Search 同样被抢）。修产品=explorer 分支前读 `getActiveViewContainerId(SideBar)`，已切非 explorer→返回 `{target:'kept'}` 不抢。回归单测：「keeps a non-Explorer side bar container the user already switched to」。锚：`packages/e2e-harness/src/fixtures.ts`（`resetWindow` pre-reload closeWorkspace）；`workbenchFocusRestorer.ts`（`'kept'` 分支）。

**案例 41 — 虚拟化 timeline 像素中点落到末条：定位本身按锚索引迭代**
信号：「虚拟化 timeline+像素中点+锚断言」spec，前置守卫 `anchorBefore<末条` 被击穿（received=末条消息号）——像素中点甚至落到末条→距底 <32px 被记 stuck=true，「中部恢复」退化成「贴底恢复」假阳性。
`smoke.agentsVirtualScrollRestore` 仅 Ubuntu CI 偶发，场景 2 前置守卫 `expect(anchorBefore).toBeLessThan(23)` received **24**——案例 34 把断言换成锚定索引相对不变量后，仍遗留「像素中点定位」这个薄弱点：`(scrollHeight-clientHeight)/2` 在虚拟估计坐标系下落点不确定，CI 慢机这次直接落到末条 m24；距底 `<STICK_THRESHOLD_PX`(32px) 被 `handleScroll` 记 `stuck=true`，场景 2 退化成场景 1 的假阳性（若守卫不在就会假绿）。修：定位改按锚定索引割线迭代（设 frac → settle 读锚 → 按 锚≈k·frac 折算下一 frac `frac*=target/idx`，直至锚落进中部带 [6,18]，2~3 次收敛），抽共享 `AcpTimelinePO.scrollToAnchorBand`；同型 `smoke.agentsScrollRestore`（max/2+绝对带 [4,12]，同款潜伏）一并换用。整类扫免疫清单：`agentsStickyScroll` 单张超高卡中点恒在卡内、sessionList/search/mermaid 非虚拟列表，均免疫。锚：`packages/e2e-harness/src/pages/AcpTimelinePO.ts`（`readTopIndex`/`settleTopIndex`/`scrollToAnchorBand`）；`ChatBody.tsx`（`STICK_THRESHOLD_PX=32`）。

**案例 42 — CI en-US locale + 1280×800 窗口暴露水平布局溢出遮挡**
信号：**元素 visible 但 click 超时** + call log 里 `... intercepts pointer events`（hit-target 检查用 `elementFromPoint`，溢出的元素滑到 DOM 序更后的兄弟区域底下即被挡）；本地全绿 CI 挂。
2026-07 标题栏重构（65185fce）后 CI 三个 spec 挂：ai-button `locator.click` 超时 30s、元素 visible 但 call log 报 svg `intercepts pointer events`；本地全绿。根因=CI e2e 环境 = en-US locale + 1280×800 默认窗口，本地 zh-CN（+常最大化）会掩盖标题栏等水平紧凑布局的溢出：command-center 药丸 `flex-shrink:0` + 全 shrink:0 兄弟，CI 的 en-US 更宽文案+1280×800 默认窗口使溢出的 center 右缘滑到 DOM 序更后的 `.right` 区域底下，ai-button 被 svg 遮挡（`elementFromPoint` 命中 svg）；本地 zh-CN 菜单更短仅剩 2px 间隙恰好不触发=本地全绿假象。修：长条形容器留一个可缩元素（`flex-shrink:1 + min-width`）吸收压力，勿全 `flex-shrink:0`；改标题栏/工具栏等水平布局后，用 `--lang=en-US` + 1280 窗口验证遮挡（探针：`elementFromPoint(btn 中心) === btn`、`center.scrollWidth <= center.clientWidth`）。（本案例自 memory `e2e-ci-locale-window-size-vs-local` 迁入。）锚：标题栏 command-center 药丸。

**案例 43 — bootstrap one-shot 焦点恢复抢焦（toBeFocused 恒 inactive）**
信号：`toBeFocused` 恒 inactive（**稳定卡值**）+ 无 workspace 冷启动 spec；仅 Ubuntu CI 挂=慢机 settle 落在测试中段，快机 settle 早于首个动作。命中信号=sharedApp spec 不开 workspace 就驱动/断言焦点（`toBeFocused`/`keyboard.type` 落点敏感）。
`smoke.titlebar`「clicking the command center opens Quick Open」仅 Ubuntu CI 偶发，Escape 关闭 Quick Open 后 `expect(commandCenter).toBeFocused()` 10s 恒 inactive（24 采样全卡值）——案例 27 的 settle 竞态从「容器选择」扩展到「DOM 焦点」：空窗口 reload 后 `viewsService.reconcileFromStorage` 恒走 500ms fallback timer→settle 后 `restoreWorkbenchFocus` 无编辑器分支 `focusView(explorer.tree)` 聚焦 Explorer 树——spec 在窗口内驱动的焦点（click 开的 overlay 触发器、Escape 后 react-aria FocusScope 恢复回的按钮、Monaco/输入框）被抢走且**永不归还**（one-shot）。与案例 39/40 的 workspace-change restore 不同族：本条是 reload 后 bootstrap 一次性 restore，无 kept 语义防护。诊断弯路：本地先跑出 `outline-style "auto"` 假失败=stale build 缺 `:where():focus-visible` 规则（诊断前必先 `pnpm build`），重建后 20/20 绿才转向 CI 特有竞态。修：spec 动作前 `await workbench.waitForBootstrapFocusSettled()`；多 test 同族用 describe 级 `beforeEach` 统一加；整类扫 `toBeFocused` grep 全 specs，凡无 workspace 缓冲的一并加固（`smoke.commandPalette` 6 test/`smoke.quickAccess` 5 test 同族一并 describe 级加固；`smoke.workspaceFocus` 已有门控、`smoke.simpleFileDialog` 有 openWorkspace 缓冲免疫）。锚：`smoke.titlebar.spec.ts`；`workbenchFocusRestorer.ts`（explorer 分支）。

**案例 44 — 裸 launch restore spec 测试中途切 workspace 撞 parcel 竞态**
信号：裸 `electron.launch` 的 restore/persistence spec 在**测试中途** `openWorkspace`（A→B→A=parcel subscribe/unsubscribe churn）→ `Target page ... closed`；boot 时恢复进 workspace 的单次 subscribe 不算高危。
`smoke.editorRestore`「switching workspaces does not leak editors across scopes」仅 CI Windows 偶发 `page.evaluate: Target page, context or browser has been closed`、栈在第二次 `openWorkspace(wsA)`——案例 12（simpleFileDialog「switches the workspace twice」）的同型复发：与另一 worker 并发踩 `@parcel/watcher` windows backend 跨进程 native 竞态（0xC0000005）崩掉自己的 main。当时案例 12/16/26 只扫了 sharedApp/folderDrag/simpleFileDialog 系列，漏了 restore 系裸启动 spec。修：该 test 打 `{tag:'@serial'}`。整类扫：grep `launchWithState|electron.launch` 的文件逐个核对测试体内有无 `openWorkspace`（其余裸启动 spec：layoutPersistence/outputRestore/viewMove/maximizedSecondarySidebarRestore/agentsEmptySessionRestore/agentOnboarding 均无测试中途 `openWorkspace`，不打标）。锚：`smoke.editorRestore.spec.ts`；基准 `smoke.simpleFileDialog.spec.ts:35`。

**案例 45 — Worker teardown 无 fixture 名变体：孤儿子进程握 CDP pipe（已根治）**
信号：「Worker teardown timeout」**无 fixture 名**（卡在 `gracefullyCloseAll`）+测试全过、spec 组合随机=main 死后有东西握着 CDP pipe 不 EOF——优先查父 PID 已死的孤儿子进程（wsl/pwsh/conhost/electron），孤儿命令行就是凶手。
全量 e2e「97 passed 但 N 个 Worker teardown timeout（30s，无 fixture 名变体）」、每轮波动 1~5 个、spec 组合随机（2026-07-25，terminal profiles 功能引入后暴露）——main 端 terminal profile 检测 spawn `wsl.exe -l -q`，wslservice 被 8 并发 worker 竞争时 wsl.exe 挂起；**Node `execFile` 的 timeout SIGTERM 杀不死它**（wsl.exe 卡 RPC 状态，实测挂 8+ 分钟；`taskkill /F` 秒杀）→孤儿持有继承句柄→main 死后 Playwright 的 CDP pipe 不 EOF→`app.close()` 永不 resolve→`gracefullyCloseAll()` 卡满 30s。放大器：`ViewPane` 的 toolbar **无条件挂载**（view 折叠也渲染）→TerminalViewToolbar 的 mount prefetch→Panel 每次显示都触发 `refreshProfiles`；sharedApp 每个测试 reload 窗口→每 worker 4-7 次 wsl spawn。修双层：①产品层 `terminalMainService.runDetectionProbe` 自管理 spawn 替代 `promisify(execFile)`——`stdio:['ignore','pipe','pipe']`（wsl.exe 会等 stdin）+`windowsHide:true`（spawn 默认 false 会闪 console 窗）+超时后 `taskkill /pid /T /F`（对真实用户同样是健壮性修复）；②e2e 层 `INITIAL_SETTINGS` pin `terminal.integrated.useWslProfiles:false`（WSL 检测已有 23 个 main 单测，e2e 不需要机器相关探针）。排查法：无 fixture 名=卡在 `gracefullyCloseAll`（连接未断），有 fixture 名=该 fixture teardown 本身；查系统孤儿（`Get-CimInstance Win32_Process` 找父已死的 wsl/pwsh/conhost/electron），孤儿命令行即凶手；卡住 worker 的 tmp userData `logs/*/main.log` 尾行「before-quit confirm 后无 will-quit」=quit 链被 force-kill 打断。这类超时极易误判为环境 flake 而放过真 bug——**基线对照（stash）是定性关键**。（本案例自 memory `e2e-worker-teardown-wsl-orphans` 迁入；关联 memory `cli-stdin-hang-on-prompt` spawn 交互型 CLI 挂起同类。）锚：`terminalMainService.runDetectionProbe`；e2e `INITIAL_SETTINGS`。

**案例 46 — headless 拖放手势时序不稳 → @flaky（环境，未根治）**
信号：失败仅集中在 DnD 类用例（`smoke.explorerDnD` 等）且重跑能过→环境 flake。
HTML5 DnD 在 Playwright + Electron headless 下手势投递时序不稳（无 GPU/2 核 runner 上时序敏感），非业务回归。workaround：打 `@flaky`，CI 单独趟 `continue-on-error` 不阻塞门禁，但保留覆盖（仍跑、仍上传 trace）；修好根因（显式等待 drop 完成态，见末尾「根治 TODO」）后应摘掉 `@flaky` 让它回归门禁。锚：`smoke.explorerDnD.spec.ts`。

**案例 47 — 机器锁屏时剪贴板用例必败（环境，不打 tag）**
信号：剪贴板用例（`smoke.acpPasteImage` 等）报 `clipboard.readImage().isEmpty()` 为 true（写入后回读为空）+ `powershell -c "Get-Clipboard"` 同时报“拒绝访问”→锁屏环境问题，不当回归。
Windows 锁屏时安全桌面（Winlogon desktop）持有剪贴板，所有进程的 `OpenClipboard` 返回 `ERROR_ACCESS_DENIED`（5）——此时 `Set-Clipboard`、`clip.exe`、Electron `clipboard.writeText` 全部失败，`GetOpenClipboardWindow` 返回 NULL；`LockApp.exe`/`LogonUI.exe` 在跑即锁屏铁证。**解锁屏幕后立即恢复**，与代码无关。workaround：跑 e2e 前确认机器未锁屏。

**案例 48 — chord 用例 defocusEditor 偶发超时（观察中）**
信号：失败点在 `defocusEditor` 的 `expect.poll`（等 `editorFocus` 变 false，5s 超时）+ retry 秒过=点击空白 defocus 与 Monaco focus tracker 在重负载全量趟下偶发时序错位，环境噪声非回归。
`smoke.editorGroupSwitch`「Ctrl+K Ctrl+Left/Right switch focus」首轮偶发卡在 `defocusEditor`（2026-07 本机 Windows 观察到 1 次），根因未定位，retry 即过故暂不打 tag。判定：retry 稳过→时序噪声；同一断言在多轮/多机器稳定失败才当真回归查。

**案例 49 — 固定 sleep 后做全列表相等断言：增量渲染被截半（fullyParallel 暴露）**
信号：列表相等断言（`toEqual` 数组）diff 显示 **received 是 expected 的前缀/子集**（缺尾部条目而非乱序）+ 采样点是「summary/完成信号出现后 `waitForTimeout(N)` 再读 DOM」+ 单独重跑秒过=固定 sleep 采到半渲染列表，负载敏感非回归。
`smoke.searchOrder`「two consecutive searches produce the same file order」在 core 开启 `fullyParallel`（用例级调度，2026-07）后 e2ea 全量趟首挂+retry 也挂：run 2 的行列表缺了几十个文件——search-summary 出现只代表搜索完成，虚拟化树仍在增量渲染，800ms 固定 sleep 在并行组合改变（同机混跑更多冷启动重用例）后不够。与案例 14（滚动采样瞬时值）同族：**测量瞬时值必须 poll 到收敛**。修：去掉 `waitForTimeout(800)`，`expect.poll` 采样行列表至「两次连续采样长度相等」再比较顺序。判定钥匙：received 是前缀子集=渲染未完，真乱序回归的 diff 是同长度但次序不同。锚：`smoke.searchOrder.spec.ts` `runOnce`。

**案例 50 — Allotment 挂载后 preferredSize 是 no-op：异步 reconcile 落后于首次布局**
信号：sash 拖拽/尺寸持久化类 spec，reload 后目标 pane 高度稳定卡在预 reconcile 的等分值（received 恒为等分/初值，与预期差值远超容差，非波动）——命中信号=该组件对多个可变尺寸 pane 用 Allotment 且尺寸来自**异步**加载的持久化状态。
`smoke.viewSizes`「sash-dragged sizes survive a window reload」CI 偶发，reload 后 `paneHeight(TREE_VIEW)` 与拖拽前 `h1` 差值达 212px（5s 超时未收敛）——`main.tsx` 的 `reconcileFromStorage()`（读 WORKSPACE storage）故意放在 React mount **之后** fire-and-forget（避免阻塞首屏），而 Allotment 的 `ResizeObserver` 只要容器有尺寸就立即跑首次布局；CI 更慢的 IPC/主进程往返使 reconcile 更容易在 Allotment 首次布局**之后**才 resolve。`ViewPaneContainer.tsx` 只在 `collapsedKey`/`viewIdsKey` 变化时用 `allotmentRef.current.resize(...)` 做imperative 校正（折叠/展开场景），却漏了「持久化尺寸迟到」这一路径——而**同一仓库里 `WorkbenchLayout.tsx` 早就为顶层 sidebar/panel/secondary 尺寸实现了这个校正**（对照兄弟组件即发现缺口）。根因关键事实：Allotment 已挂载的 `Allotment.Pane` 的 `preferredSize` prop 变更是纯 no-op（只在 pane 首次添加时读一次——1.20.5 源码 `_loop5`：留存 pane 只更新 bookkeeping 的 `preferredSize` 字段，`layoutStrategy` 在 PaneView 构造时冻结），程序化改尺寸必须走 `AllotmentHandle.resize(sizes[])` 命令式 API。修产品：`ViewPaneContainer.tsx` 新增一个 `storedSizesKey`（拼接每个非折叠 view 的 `getViewState(id).size`）driven 的 `useLayoutEffect`，key 变化且与当前渲染尺寸差值 >1px 时调用 `handle.resize(target)`。回归单测：`ViewPaneContainer.test.tsx` 用可控 resolve 的 deferred storage 模拟「ResizeObserver 先跑、reconcile 后到」的顺序，断言 `resize()` 后真实渲染高度（`split-view-view` 的 `style.height`）而非仅服务层 bookkeeping；先临时禁用修复确认测试真失败，再还原验证通过（排除 tautological test）。整类扫描：grep 全仓库 `from 'allotment'` 只有 3 处消费方——`WorkbenchLayout.tsx`（已有校正）、`ViewPaneContainer.tsx`（本次修）、`TerminalView.tsx`（面板间等分且无持久化尺寸，不受此竞态影响，免疫）。锚：`apps/editor/src/renderer/workbench/sidebar/ViewPaneContainer.tsx`（新增 `storedSizesKey` effect）；`apps/editor/src/renderer/workbench/layout/WorkbenchLayout.tsx`（既有同款校正模式）；`apps/editor/src/renderer/main.tsx`（`reconcileFromStorage` fire-and-forget 时机）；node_modules `allotment` 源码（`preferredSize` 挂载后 no-op 的确认点）。

**案例 50b — 上轮修完同 spec 同断言再挂：污染源头没堵（只治了恢复路径）**
信号：案例 50 修完后**同 spec 同断言再次 CI 偶发**（形态完全一致：reload 后卡等分值）——上轮只修了「reconcile 晚到后如何纠正渲染」，没堵「等分值怎么进磁盘」的污染路径。
`smoke.viewSizes` 同一用例 2026-07-27 再挂（212px 差值）——`ViewPaneContainer` 的 `onChange` 无条件 `setViewSizes(...)` 且 `setViewSizes` 无条件 `_schedulePersist()`。**污染窗口在 reload 之后**：reload → 首布局 onChange（等分值）→ 内存记账 + 200ms debounce 计时 → CI 慢机上 reconcile（等 workspace scope hydration + IPC 往返）>200ms 才读盘，而 debounce 先到点把**等分值写盘** → reconcile 读到的是自己刚写的脏值 → `_loadFromStorage` 覆盖内存 + bump → `storedSizesKey` effect 忠实地把脏值纠正到渲染层 → 锁死等分（200ms debounce < reconcile 上限 500ms，窗口真实存在；reconcile <200ms 时读到拖拽真值则不发病，故偶发）。effect 只治了「reconcile 晚到后如何纠正渲染」，没堵「等分值怎么进磁盘」的污染路径。根治（对齐 `WorkbenchLayout` 的 VSCode 语义「persist ONLY user sash drags」）：**收窄落盘权到用户动作**——`IViewDescriptorService.setViewSizes(sizes, {persist?})` 默认只更新内存（layout 记账：首布局/容器 resize/程序化纠正全走这条路，永不落盘），`persist:true` 才调度落盘（sash `onDragEnd`、collapse 时记 remembered size）；`persist:true` 无条件调度（不等 changed——内存值可能已因 onChange 记账相同，而磁盘还是旧值）。配套两路恢复校正缺一不可：① 首个 onChange 一次性 `correctToStoredSizes`（覆盖「reconcile 早于 PaneView 构造」——本地/快机多数路径，用户拖拽先发生则取消）；② `storedSizesKey` effect（覆盖「reconcile 晚于首布局」）；effect 的 prevKey 消费必须移到长度守卫**之后**（守卫 return 前消费会吞掉「reconcile 落地但 Allotment 尚未 onChange」的 key 变更——此后只剩首 onChange 校正单路兜底，多一层幂等防御更稳，diff>1px 守卫保证重复校正 no-op）。回归单测：service 层「setViewSizes persists only when asked」（默认 250ms 后 store 无 key；persist:true 即便内存同值也落盘）+ 组件层「never persists the pre-reconcile first-layout split」（fake timers 越过 debounce+grace 无自动落盘）。坑：用 `save()` 断言「不落盘」是 tautological——`save()` 无条件把内存写盘；守护点是 **debounce 自动落盘不发生**。锚：`ViewDescriptorService.setViewSizes`（persist 参数）；`ViewPaneContainer.tsx`（onChange 记账 / onDragEnd 落盘 / 双路校正）；`WorkbenchLayout.tsx`（对照组语义注释）。

**案例 50c — 50b 修完再挂：磁盘堵住了，内存记账仍被布局噪声覆盖（persisted/mem 双轨制）**
信号：案例 50b 修完后**同 spec 同断言第三次 CI 偶发**（差值 172px）——诊断现场抓到的决定性形态：**mem==DOM==贪心分配值**（`{"treeDom":632,"timelineDom":88,"treeMem":632,"timelineMem":88}`，632=容器 720-88=另一 pane 压 OPEN_MIN 的 distributeEmptySpace 形态），而磁盘是干净的拖拽真值——50b 堵了磁盘污染，**mem（`_viewStates` 布局记账）仍是公共白板，谁都能写**。
`smoke.viewSizes` 同一用例 2026-07 再挂（本地 repeat-each 可复现，8 次 1 flaky 差值 212）。完整致病链：reconcile **早**落地（storedSizesKey effect 被 sizesRef 空守卫跳过、不消费 key）→ Allotment 贪心首布局 fire onChange([632,88]) → 首 onChange correct 把视觉救回 [420,300]（resize 嵌套 fire 同步、外层继续跑）→ 外层 `setViewSizes([632,88])`（50b 后无 persist、纯记账）**把 mem 覆盖成脏值** → 后续任意 version bump（测试里的 reload 后首次交互）触发 render → storedSizesKey/target 从脏 mem 算 → resize([632,88]) **主动把脏值锁死到视觉层**。另一残留路径：correct 是一次性语义，settle 窗口之外的二次扰动（窗口几何 settle 重分配）无人拉回。根治两条：① **persisted 权威源**——`ViewDescriptorService` 新增 `_persistedSizes`（只被 reconcile/persist:true 写，免疫布局噪声），`getPersistedViewSize()` 成为所有「恢复目标」的唯一读源（correctToStoredSizes/storedSizesKey/collapse remembered/expand restore/mount preferredSize 全换读它）；**`save()` 序列化的 size 也走 persisted map**——否则一次无关 persist（move/reorder/collapse 触发）会把当时 mem 里的布局噪声序列化进磁盘（新污染旁路）；② **settle 窗口内持续校验**——600ms 内每次 onChange 都 correct（非一次性），配守卫组合防自激：`sashDraggingRef`（拖拽中暂停）、collapsed 守卫、`correctingRef` 重入守卫、`deficit<0` 跳过（容器装不下 persisted 时 resize→clamp→onChange→correct 会无限同步递归「Maximum call stack size exceeded」，等容器长大的下次报告再纠）。教训：**「记账值」和「权威值」必须分家**——只要恢复路径读的是可被布局帧覆盖的同一份 state，校正逻辑就会在某一帧把噪声当真相锁死；50→50b→50c 三连是同一个设计缺口的三层暴露（恢复缺失→磁盘污染→内存污染）。锚：`ViewDescriptorService.ts`（`_persistedSizes`/`getPersistedViewSize`/save 序列化）；`ViewPaneContainer.tsx`（`correctToStoredSizes` 持续校验+守卫组）；`ViewPaneContainer.test.tsx`（「a later render cannot lock in layout noise」「keeps correcting startup geometry redistribution」两回归用例）。

**案例 51 — 配置刚写入即建 session：异步镜像池 stale 把 wire mcpServers 滤成空（产品竞态）**
信号：ACP spec `updateConfigValue('acp.mcpServers', …)` 后立即建 session（deep link / newSession 命令），echo agent 报告的 mcpServers received 恒 `"[]"`（**稳定卡值**——不是 agent 没回复，是回复内容就是空数组）；本地稳过 CI 偶发。
`smoke.deepLinkAgent`「pid is injected into the universe-editor MCP server env」（该 pid 功能及其用例 2026-07 已删，根因修复仍在 `acpSessionService`）e2ea 偶发，`toContain('"name":"UNIVERSE_EDITOR_MCP_PID"…')` received `"[]"` 10s 超时——echo agent 如实报告了 session/new 收到的空 mcpServers，fire-once 永久错态，poll 再久不变。根因=产品时序缺陷：`acp.mcpServers` 配置写入（同步）→ config-change 监听 fire-and-forget `refreshMcpServerDefinitions()`（内部 `await readProjectMcpJson()` 走一次 fs read）刷新**异步镜像池**；与此同时 `createSession→_connectSession→_resolveSessionWireMcpServers` 也 `await readProjectMcpJson()`（第二次 fs read）后读镜像池做 selection 过滤。两次 `fs.readFile` 落 libuv 线程池**完成顺序不保证 FIFO**，CI 负载下后发的先回→镜像池还是旧值（空）→`resolveMcpServerSelection(空池, null)` enabledNames 空→wire 列表被滤成 `[]`。修产品：`_resolveSessionWireMcpServers` 不读异步镜像，用手头同一份 `projectRaw` 快照 + `_readGlobalMcpDefinitions()` 同步重算 pool——session 创建永远看到当下配置，竞态根除（`smoke.agentsMcpReload` 同模式 spec 一并免疫）。回归单测「a session created right after an acp.mcpServers edit is not filtered by the stale mirror pool」（update 后先 settle 一个宏任务让镜像 refresh 落地，再手动 `mcpServerDefinitions.set([])` 模拟 stale，createSession 断言 wire 非空）。锚：`acpSessionService.ts`（`_resolveSessionWireMcpServers` 内联 pool 重算）；`AcpSessionService.test.ts`（stale mirror 用例）。

**案例 52 — 探针同步读 Monaco 在动态导入落地前抛错，击穿 expect.poll**
信号：`page.evaluate: Error: [MonacoLoader] not initialized; call ensureInitialized() first`，栈过 probe 的同步探针（如 `getVisibleOutputLines`→`findOutputEditor`）；poll 回调抛异常=立即失败**不重试**，冷启动（electronApp fixture）spec 首个 poll 就炸。
`smoke.outputLevelFilter`「hiding levels below Warning…」e2ea 偶发——`showOutputOnAllChannel` 里 `expect.poll(getVisibleOutputLines().length)` 本意是等 Monaco 编辑器异步挂载（注释都写了 "The editor mounts asynchronously"），但探针 `findOutputEditor` 用 `MonacoLoader.get()`，Monaco dynamic import 未完成时**抛错**而非返回空→poll 被异常击穿，等待机制自废武功。修探针：`findOutputEditor` 改 `MonacoLoader.peek()`（未就绪返回 undefined）→`getVisibleOutputLines` 返回 `[]`→poll 继续等到编辑器就位，断言强度不变。整类扫：probe.ts 其余 `MonacoLoader` 调用全是 `await ensureInitialized()`（async 探针），仅此一处同步 `get()`；新写同步探针**禁用 `MonacoLoader.get()`**，一律 `peek()` + 空值兜底。
**2026-07-27 再发**：CI ubuntu run 30234357135 同 spec 两用例（:75/:153）同栈再挂——检查发现 `peek()` API 在 `MonacoLoader.ts:282` 存在已久但 `findOutputEditor` 从未改过去（上轮修复疑似未合入/被 56a9dc1e output 面板重构冲掉）。教训：**案例库记录「已修」后必须核对 main 上修复真实存在**（`git log -S` 验证）；本次已重新落地并整类复扫（全仓 `MonacoLoader.get()` 40+ 处均为产品代码的就绪后回调路径，probe 唯一一处）。锚：`probe.ts`（`findOutputEditor`）；`MonacoLoader.ts`（`get` 抛错 / `peek` 语义）。

**案例 54 — 点击超高 `.view-lines` 容器的几何中心：CI 慢机 relayout 后中心落到容器可见区外被遮挡**
信号：元素 visible/enabled/stable 但 `locator.click` 超时 + call log `<其它元素> intercepts pointer events`；点击目标是 Monaco `.view-lines`（比可见编辑器高很多的内部容器）；**本地全绿、仅 CI 偶发**——本地点击点恰落在 panel 内，CI 慢机在「关闭最后一个编辑器 → panel relayout」后容器中心滑出可见区，命中 DOM 序更后的 Welcome fallback / panel 标签栏。
`smoke.outputContextMenu`「right-click works after a diff editor was created and closed」仅 CI Windows 偶发 `Test timeout of 30000ms exceeded` + `_agentActions_... intercepts pointer events`（Welcome 页 agentActions）——spec 点 `outputLines`（= `[data-testid=part-panel] .view-lines`）的**默认几何中心**。关键事实：Output 的 `.view-lines` 比可见编辑器高（本地量得 y=588 h=190，而 `.monaco-scrollable-element` 视口 y=606 h=172，容器向上溢出视口 18px），Playwright 点击的中心在「溢出容器」上只是个**计算坐标**，是否命中可见文本取决于当时的 panel 高度；CI 慢机 diff editor 关闭后 panel 还在 relayout（更矮/未就位），中心落到 panel 外被 Welcome 挡住 → hit-target 重试 30s 超时。与案例 42 同族（可见元素被兄弟遮挡）但根因不同：42 是 CSS 水平溢出（修产品），本条是**点击目标选错**——不该点超高内部容器的中心。诊断弯路警示：排查中一度误判「Output 只读编辑器右键不弹菜单」，那是**调试操作污染**（同 run 内多次右键/额外点击使菜单消失）；干净 HEAD 下右键 Output 稳定弹出 `[Copy, "", Command Palette F1]`、无 `InstantiationService` 错误——**下「产品不弹菜单」结论前必须 stash 回纯净基线复跑**（skill 判定流程第 1 步）。修：把 click 的 `position` 钳制到「`.monaco-scrollable-element` 可见视口中点」相对 `.view-lines` 的偏移（`{x: 视口中点x, y: scrollable.top - viewLines.top + 视口高/2}`），该点恒映射到可见输出文本，panel 高矮自适应；断言（菜单可见 + 无 disposedErrors）一字未动，强度不变。整类扫：grep 全 specs `button: 'right'` 仅 `smoke.explorerSelection`（点 treeitem 具体可见项，非超高容器中心）与 `smoke.outputContextMenu`（本修），无其它同型。锚：`apps/editor/e2e/specs/smoke.outputContextMenu.spec.ts`（position 钳制）；`workbench/panel/output/LogOutputView.tsx`（超高 `.view-lines` 的来源：Monaco 视口裁剪）。

**案例 53 — workspace restore 窗口抢走编辑器区内焦点（toBeFocused 恒 inactive + 测试中途 openWorkspace）**
信号：`toBeFocused` 恒 inactive（**稳定卡值**，采样全卡）+ spec **测试中途 `openWorkspace`** 后在编辑器区内驱动焦点（Ctrl+F 的 find input、preview 容器等）；仅 CI 慢机挂=各 poll 拉长使 1500ms restore 窗口的 debounce 落点后移，恰好盖在 spec 刚放好的焦点之后。
markdownPreview「Closing find with Escape returns focus...」仅 CI 偶发，`expect(acp-find-input).toBeFocused()` 14 采样恒 inactive——`openWorkspace` 触发 `WorkspaceFocusRestoreContribution` 的 1500ms restore 窗口（80ms debounce，窗口内 group/editor/layout 事件反复 re-schedule）；CI 慢机上 openFileUri→openPreview→Ctrl+F 各步 poll 拉长，restore 落地后移到 find input 聚焦之后→editor 分支无条件 `focusEditorInput` 把焦点抢回 preview，find input one-shot 丢焦永不归还（mount effect 只 focus 一次）。与案例 39/43 同族：39 只给 explorer 分支加了 'kept' 防护，**editor 分支还是裸的**；对真实用户同样成立（打开文件夹 1.5s 内按 Ctrl+F 被抢焦）=真产品竞态，修产品不动 spec。修：`restoreWorkbenchFocus` 顶部加守卫——`document.activeElement` 已在 `[data-testid="part-editorArea"]` 内→返回 `{target:'kept'}` 不抢（editor 分支的目标本就是把焦点放进编辑器区，已达成；正常 workspace 切换后编辑器全关、焦点掉 body，守卫不影响正常 restore）。回归单测「keeps focus the user already placed inside the editor area」。整类：所有「测试中途 openWorkspace 后驱动编辑器区焦点」的 spec 一并免疫，无需逐 spec 加固；残余风险=窗口期内焦点在编辑器区**外**的 overlay（QuickInput 等）理论上仍可能被抢，尚无失败实例未扩大守卫面。锚：`workbenchFocusRestorer.ts`（editorArea 守卫）；`extensions/markdown/e2e/specs/markdownPreview.spec.ts`。

---

**案例 54 — boot 后 openWorkspace 双重启 extension host：LSP provider poll 恒 0 + dying-host 11 个 Disposable 泄漏**
信号：LSP 类 spec（CodeLens/semanticTokens/peek）`expect.poll` 等 `providerCount: 1` **恒 0** 直到超时（首跑挂、retry 秒过），或 teardown 泄漏门禁报 **N 个 Disposable 泄漏**、堆栈 = `MainThreadLanguages._createProvider → new DisposableStore`（泄漏数恰等于扩展 activate 同步注册的 provider 批大小）——两症状同一根因、不会同次运行同时成立；单跑 `--repeat-each` 不复现，`--workers=2` 资源争抢才拉开窗口。
typescript 套件 `tsCodeLens`/`tsSemanticTokens` CI 2 workers 偶发——spec 流程 `waitForRestored → openWorkspace(freshTmpDir) → openFileUri → poll`，boot 后 `openWorkspace` 在**同一回合**触发 extension host **两次重启**：① workspace re-pin（`_onWorkspaceChanged → _repin → _restart('workspace')`）② trust flip revoke（空窗口 trusted=true，fresh tmp dir 不在信任列表→flip→`_onTrustChanged(false)→_restart`）。2 workers（CI 2 核，每 worker 各跑 Electron + host + tsserver）时 host spawn 膨胀到秒级：dying host 的在途 `$registerProvider` 帧在 connection teardown（→ MainThreadLanguages dispose）**之后**才 dispatch → `DisposableMap.set` 无 `_isDisposed` 守卫、value 挂到已死 map 上根链断裂 → 泄漏；host#2/#3 慢启动 → 30s poll 窗口内注册未到达 → providerCount=0。本地单跑无资源争抢、spawn 亚秒级，窗口不存在。三层修复（缺陷根治 + 消除竞态窗口本身）：① `DisposableMap.set` 加 `_isDisposed` 守卫——已 dispose 时 warn + 直接 `value.dispose()` 不存入（**不调 `setParentOfDisposable`**，挂死 parent 正是根链断裂来源；对齐 `DisposableStore.add` 的 warn 但语义选 dispose 而非有意泄漏，因本仓库 `expectNoLeaks` 门禁把 tracker 可见泄漏变测试失败）——`MainThreadCommands/Window/Output` 等所有用 DisposableMap 收 dying-host 迟到帧的类同享兜底；② `MainThreadLanguages.$registerProvider` 入口 `_store.isDisposed` 早退（第一道闸，连带消除 `_codeLensChange` 裸 Map 泄漏面）；③ **harness 新增 `workspaceSeeder` option fixture**——per-test tmp 目录 seed 后作为位置参数随 app 一起启动（`openWindowForFolder`），host 首启即 pin 该 workspace（`_connect` 读 `current.folder.fsPath`），trust 初始化直接 false 无 flip → 每次测试恰好 1 代 host，还省 1-2 次 host+tsserver spawn；配套 `_repin` 同源跳过加固（`conn.workspaceRoot === current.folder.fsPath` 时不重启）把「单代 host」从时序论证变成结构保证。迁移 4 个 spec（tsCodeLens/tsSemanticTokens/peekPreview/peekNavigation）：describe 层 `test.use({ workspaceSeeder: { seed(dir) {...} } })`、删 `openWorkspace`、`openFileUri(launchWorkspace.file('<name>'))`。**Playwright 坑**：`test.use` 传**裸函数**会被当 fixture override 调用（类型层 `TestFixtureValue = Exclude<R, Function> | TestFixture<R>` 直接排除函数值）——seeder 必须包成 `{ seed(dir) {...} }` 对象，与 p4Seeds 裸数组被 tuple 展开同一家族陷阱。修复后 `--workers=2 --repeat-each=8` 两个套件各 16/16 全绿零泄漏，单测试 ~4s（原多次重启路径更慢）。未迁移同构 spec（dirtyDiffPeek、extensions/markdown 5 个）按同模式 follow-up。锚：`packages/platform/src/base/lifecycle.ts`（`DisposableMap.set` 守卫）；`MainThreadLanguages.ts`（早退）；`packages/e2e-harness/src/fixtures.ts`（`workspaceSeeder`/`launchWorkspace`）；`ExtensionHostClientService.ts`（`_repin` 同源跳过）；`HostConnection.ts`（`workspaceRoot`）。

---

**案例 55 — openWorkspace 后立即外部写文件：watcher 跨进程 arm 窗口吞事件，treeitem 等满 timeout 恒不出现（产品竞态）**
信号：`waiting for locator('[role="treeitem"]')` 恒 0 / `toBeVisible` 等满 timeout 元素**从未出现**（不是值错，是事件永远没来）+ spec 时序是 `openWorkspace → 等 seed 文件可见 → fs.writeFile → 等 watcher surface 新文件`；retry（重跑重写文件）救得回；`--repeat-each` 概率挂（4 跑 2 挂）。
`smoke.explorerExternalWatch`「a file created externally appears in the tree automatically」本地 `--repeat-each=4` 必现 flaky——根因=产品 fire-and-forget：`ExplorerTreeService._setRoot → _syncWatch` 是 `void watcher.watch(...)` 不等 ack，而 watch 真正生效要跨三段异步：renderer→main IPC → `WatcherProcessClient` 首启**惰性 spawn utility process**（Windows 下数百 ms 起）→ host 内 `parcel.subscribe()`（`watcherHost.ts` 在 subscribe 完成后才回 ack，语义可靠但慢）。spec 的 seed 文件可见只证明 tree readdir 完成（`_setRoot` 已发生、watch 请求已**发出**），不代表订阅生效；`openWorkspace` 的 await 也只等 workspace 切换不等 watch arm。测试在 request→ack 窗口内 writeFile → **parcel 只报订阅时刻之后的事件**（`startWatching()` 注释明说的语义）→ 事件永久丢失，元素永不出现。修产品（与 `startWatching()` 的 catch-up 补读同思路）：`_syncWatch` 的 `.then(ack)` 里补一次幂等 `refresh(root)`——ack 到达即订阅已 live，窗口期内的外部创建由这次重读兜底；`sameUri(this._root, root)` 守卫防 workspace 快速连切时旧 ack 刷新新根。只刷 root 而非 `_refreshLoadedNodes` 全量：窗口短、深层编辑几乎总伴随 root 级事件，常见路径保持便宜。回归单测「re-reads the root once the watch ack resolves…」：mock `watch()` 挂起，窗口内写文件，ack 后断言 root 被重读且新文件出现。整类扫：全 specs grep「openWorkspace 后 writeFile 并依赖 watcher 事件驱动 UI」**仅本 spec 两个用例**（其余都是 openWorkspace **前**写 seed 文件，首屏 readdir 自然覆盖）；`ExternalChangeWatcher`（编辑器外部变更 reload）消费同一事件流但其场景是文件已打开后（watcher 早已 armed），无 spec 暴露、未动。修复后两例 `--repeat-each=8` 16/16 全绿。锚：`ExplorerTreeService.ts`（`_syncWatch` ack 后补读）；`watcherHost.ts`（ack 时序）；`fileWatcherMainService.ts` / `watcherProcessClient.ts`（跨进程链路）。

---

**案例 56 — extension host activate 赢了 Monaco dynamic import：$registerProvider 在 Monaco 就绪前到达，半建 provider store 孤儿泄漏 + provider 批静默丢失（产品竞态）**
信号：teardown 泄漏门禁报 N 个 Disposable 泄漏、堆栈 = `MainThreadLanguages._createProvider → new DisposableStore`（与案例 54 第二症状同栈），但**测试断言全过、只挂在泄漏**；某与 LSP 无关的提交（bundle/时序扰动）后 CI 恒定失败（ubuntu 恒定、windows 大概率）、本地稳过、retry 救不回；N = 扩展 activate 同步注册的 provider 批大小（typescript=11）。**与 54 的区分**：54 是 dying-host 迟到帧打在 **disposed** 的 MainThreadLanguages（入口早退守卫已挡）；本条帧打在**活着的** MainThreadLanguages，但 **Monaco 尚未 ensureInitialized**。
`smoke.outline`/`smoke.tsStatusBar`（coreTypescriptSharedApp，@serial 趟 `--workers=1`）在 22fc46a4（Monaco ILayoutService 重写——改动本身无错，纯 bundle 变化移动时序）后 ubuntu 4/4 恒挂。根因链：sharedApp reload 后 Monaco 的 dynamic import（几 MB chunk）与「openFileUri → onLanguage:typescript → host activate → 11 帧 `$registerProvider`」竞速；CI 慢机上帧先到 → `_createProvider` 先 `new DisposableStore()`（已入 tracker）再 `lf.registerXxx`，而 `LanguageFeaturesService` 所有注册路径同步 `MonacoLoader.get()` → 抛 `[MonacoLoader] not initialized` → store 永远进不了 `_providers`，成无 parent 孤儿 → 泄漏门禁 11 连报；同时该批 provider **静默丢失**（host 不重试）。断言仍过是因为 openWorkspace 双重启（案例 54）的后代 host 在 Monaco ready 后重新注册成功——泄漏是第一代的尸体。对真实用户同样成立（冷窗口立即打开 .ts → 语言特性静默失效）= 真产品竞态，修产品不动 spec。三处修（`MainThreadLanguages.ts`）：① `$registerProvider`/`$unregisterProvider` 改 async，先 `await MonacoLoader.ensureInitialized()`（二者 await 同一 promise，相对顺序保持），await 后**重查** `_store.isDisposed`（等待期间 host 可能被重启——dying-host 守卫的 async 版）；② `_createProvider` 包 try/catch，注册中途抛则 `store.dispose()` 再 rethrow，孤儿结构性不可能；③ `$publishDiagnostics`/`$clearDiagnostics` 改 `MonacoLoader.peek()` 早退（Monaco 没加载就没有 model，语义等价，不再向 host 抛 RPC error）。回归单测 3 个（waits for Monaco / host died while waiting / throws mid-build does not leak，mock MonacoLoader + 可控 gate）。整类扫：host RPC 触达 Monaco 的入口**仅 MainThreadLanguages**——`languageProviderProxy` 的 `get()` 全在 provider 回调内（Monaco 调用时必已就绪），contributions 按套路 H 已等 `ensureInitialized()`。锚：`MainThreadLanguages.ts`（await 门 + try/catch 兜底）；`LanguageFeaturesService.ts`（同步 `MonacoLoader.get()` 的来源）；`MonacoLoader.ts`（`ensureInitialized`/`peek`）。

---

**案例 57 — 忙等+可信输入的双竞态：setTimeout 提前量赛跑 CDP 输入派发 & dedupe 单赢家断言错具体事件名**
信号：**本地** `--repeat-each=8` 必现 flaky 且两种形态交替：① poll received 恒 0（被测事件根本没录到）② byType/类型聚合断言 received 是**单元素数组但不是期望类型**（如期望含 `keydown` 实收 `["keyup"]`）=「忙等+可信输入」spec 的两个独立薄弱点。
`smoke.interactionPerf`「records a slow interaction when a trusted key lands in a busy main thread」（9106fe25 引入）本地 8 跑 4 flaky。根因①：spec 先 `press('a')` 再 `setTimeout(busy 400ms, 50)`，按键要成为慢交互**必须落在忙等窗口内**，但 CDP 输入派发若 <50ms 到达（本地常态），主线程还空闲、keydown 被立即处理→不快→slowCount 恒 0——50ms 提前量本身就是赛道。修：反转顺序——renderer 里 `console.log(token)` 后**立即**进入忙等，Node 侧 `page.waitForEvent('console')` 收到 token（console 走 IO 线程 mojo flush，主线程阻塞也能送达）再 `keyboard.press`——按键**保证**在已阻塞的主线程上排队，input delay ≈ 忙等时长，确定性慢。根因②：`dedupeByInteraction` 同一 interactionId 只留**最慢**样本，`byType` 只记该样本的 eventType——keydown/keyup 时长差 <8ms 量化粒度，谁最慢是掷硬币，断言具体 `keydown` 错设计（守护的是「eventType 映射不是 undefined」，不是「keydown 一定赢」）。修：断言放宽到 `byType` keys 含任一 `key*` 类型，守护强度不降。锚：`apps/editor/e2e/specs/smoke.interactionPerf.spec.ts`；`interactionPerf.ts`（`dedupeByInteraction` 只留最慢）。

---

**案例 58 — 冷启动首个裸 evaluate 撞 Playwright utility world 销毁（无导航同型）**
信号：`Execution context was destroyed ... navigation` 栈在冷启动 spec 的**第一步** `page.evaluate(whenRestored)`（fixture `waitForProbe` 已成功、之后页面一直活着、无 reload 语义）——与案例 6 同信号但**没有任何导航**：CDP 监听证实无 `framenavigated`、无 crash、URL 不变，~100ms 后 evaluate 即恢复；被销毁的是 Playwright 自己的 `__playwright_utility_world_page` 隔离世界（早期 attach 竞态，utility world 销毁重建时命中在飞的 evaluate）。**挂上外部 CDP 会话后 30 连过**（观察者效应，可据此与真导航区分）；父提交同样挂 → 纯 harness 层时序，与触发关注的代码提交无关。
perforce 全部 4 个 swarm 通知 spec（`swarmReviewNotification{,Focused,Hung,Poller}`）首行裸 `page.evaluate(() => window.__E2E__!.whenRestored())`，本地 ~30-50% 挂、retry 常救回。修：统一换 `evaluateWhenRestored(page)`（案例 6 抽出的硬化 helper，e2e-harness 已导出）——**凡冷启动 spec 的首个 probe evaluate 都不许裸写**。锚：`packages/e2e-harness/src/pages/WorkbenchPO.ts`（`evaluateWhenRestored`）。

---

**案例 59 — 焦点门控断言被并行 worker 的 win.show() 偷焦点**
信号：断言「聚焦时应走应用内 fallback」的 UI（如 in-app toast locator）恒不出现，但探针侧决策已发生；host.log 出现 `notify shown`（期望的是 `notify skipped (window focused, user present)`）= notify 时刻本窗口**根本没聚焦**。
`swarmReviewNotificationFocused` 把 host 轮询从 10s 提速到 1s 后 2/6 挂：notify 从启动后 ~20s 提前到 ~5s，正好落进并行 worker 的 `win.show()` 前台争抢风暴（`win.show()` 默认聚焦，Windows 前台锁只是概率性拦截）；`isFocused()=false` → OS toast 路径 → 应用内 toast 永不渲染。10s 时侥幸是因为 notify 远在争抢平息之后。修：断言前提自己造——`addReview` 前 `await page.bringToFront()` 重新钉住焦点（判别套路沿用 swarm CLAUDE.md「缺失的日志行」：host.log 的 shown/skipped 决策行是第一现场）。锚：`swarmReviewNotificationFocused.spec.ts`；`hostMainService.notify` 门控。

---

**案例 60 — 产品原子写 rename 撞 Windows 瞬时锁：EPERM rename \*.json.tmp（产品修复）**
信号：spec 的 `page.evaluate` 直调服务后报 `EPERM: operation not permitted, rename '<file>.json.<pid>.<ts>.tmp' -> '<file>.json'`（经 reviveWireError 从 main 传回）；retry 救得回（锁是瞬时的）。与案例 17 同族但对偶：17 是 fixture 删目录撞产品**持续写**，本条是产品 rename 撞**并发读/AV 扫描**。
`smoke.extensions`「disabling a built-in extension…」本地 e2ea 偶发——`setExtensionEnablement(false)` 触发 host 重启重扫（读 extensions.json），紧接的 `setExtensionEnablement(true)` 走 `writeJsonAtomic` 裸 `fs.rename`，目标文件正被 rescan/Defender 短暂持有 → Windows rename-over-open-file EPERM。真实用户快速连点启用/禁用同样触发=产品健壮性缺陷，修产品不动 spec：`renameWithRetries`（EPERM/EACCES/EBUSY 重试 10×100ms，非可重试码直接 throw）。回归单测：spy `fs.rename` 前两次 reject 后放行断言落盘、非可重试码不重试、预算耗尽后透传。锚：`installedExtensionsManifest.ts`（`renameWithRetries`）；`__tests__/installedExtensionsManifest.test.ts`。

**案例 61 — beforeAll 起本地 server：固定随机端口 + stdio ignore = 撞占用端口后 15s 盲等（伪 flake 不可诊断）**
信号：报错 `<x> server did not start on http://127.0.0.1:<port>`，栈在 beforeAll 的 waitForServer；retry 救得回（新 worker 重新 import module → 新随机端口）。命中信号=模块级 `PORT = BASE + random(N)` + `spawn(..., { stdio: 'ignore' })`——端口被占（上轮硬杀 worker 留下的孤儿 server 是常见占用者）时 server 早退 EADDRINUSE 但无人看见，只剩盲等超时。
`smoke.extensionsGallery`「installs a signed marketplace extension…」本地 e2ea 偶发——三层修：① 端口改 net `listen(0)` 探测空闲端口（消灭碰撞类）；② env 经**可变对象引用**注入（`launchApp` 在每次 launch 时展开 `config.env`，beforeAll 先于任何 app launch，故 beforeAll 里回填 `env.UNIVERSE_GALLERY_URL` 对所有 launch 可见）；③ `Promise.race([waitForServer, server exit])` + 捕获 stderr——server 早退立即 fail 并带真实错误，不再 15s 盲等。整类扫：全 specs 只有此文件在 beforeAll spawn HTTP server（update spec 用固定 feed 端口但走 `mode: 'default'` 文件内串行 + 已知端口，暂不动）。锚：`smoke.extensionsGallery.spec.ts`（`getFreePort`/`galleryEnv` 回填/race）。

**案例 62 — 颜色探针隐式依赖 TextMate takeover：Monarch 合并 span 使 exact-text 匹配永假（门控缺失 + poll==test 天花板）**
信号：`tokenColor(page, '<word>')` poll received 恒 undefined 直到 `Test timeout of 30000ms exceeded`（poll 30s==test 30s，天花板击穿）；retry 秒过。命中信号=断言依赖某 token 独立成 span，但 Monarch（takeover 前）把同 class 相邻 token 合并渲染（`' after '` 带空格一个 span），exact `textContent === 'after'` 在 takeover 前**结构性匹配不到**——poll 实际在隐式等 TextMate 接管，接管慢/丢失时以不透明的 undefined 超时。
`smoke.textMate`「over-long lines degrade…」本地 e2ea 偶发——修 spec：抽 `waitForTextMateTakeover(page, lang)`（poll `getTokenizationSupportInfo` constructorName === 'TokenizationSupportWithLineLimit'，对齐第一个用例的显式门控），插在颜色断言前——接管失败时在门控处 fail，信号清晰；颜色 poll 降回 15s。整文件同型加固（案例 31 教训）：四个用例首 poll 均 30s==test 天花板，全部补 `test.slow()`。若此后门控本身超时复发=接管在 reload 后丢失（案例 56 族的产品竞态），继续往产品层剥。锚：`smoke.textMate.spec.ts`（`waitForTextMateTakeover`）。

**案例 63 — Worker teardown 30s 预算被多个 shared app 串行 closeApp 吃穿（含 45 之后的复发形态）**
信号：「Worker teardown timeout of 30000ms exceeded」无 fixture 名 + 所有测试 pass + **事后系统无存活孤儿**（dead-parent 扫描干净、各 userData main.log 全部 before-quit→will-quit 齐全）——与案例 45（真孤儿握 pipe）区分：45 的孤儿事后仍在，本条一切最终都死了，只是慢。一个 worker 可持有多个 worker-scoped shared app（每种 shared fixture 类型一个：sharedApp/coreGitApp/coreTextMateApp/iconThemes…），worker teardown 串行 closeApp，任一走强杀路径（10s 等待 + 5s 树枚举 + taskkill + 5s 孤儿清扫 + 3s 等 EOF ≈ 20s+）×2 即吃穿共享 30s 预算。
本地 e2ea 一轮 2 个 worker teardown 超时（206 passed）——三层修（harness）：① `sharedApp`/`electronApp` fixture 加独立 `timeout: 60_000`（Playwright fixture 自带 timeout 与 test/teardown 共享预算解耦，慢而成功的 teardown 不再误报）；② `closeApp` 强杀路径与「强杀后 pipe 仍未 EOF」各输出 `console.warn`（下次复发直接指认哪个 app/是否真有不可达孤儿，不再事后盲扫）；③ `forceKillTree` 的逐 pid `taskkill` 合并为单次多 `/pid` 调用提速。锚：`packages/e2e-harness/src/fixtures.ts`（fixture timeout）；`launch.ts`（closeApp warn/批量 taskkill）。

**案例 64 — 图表 reveal 桥接：编辑器刚挂载时 reveal 与初始 load 双发请求，晚到的 load 把选中清空（产品竞态）**
信号：`smoke.gitGraphReveal` e2ea 双形态交替——首跑 `toHaveClass(/rowSelected/)` received 恒 `"_row_… "`（**行已渲染、选中类稳定缺失**=reveal 生效过又被抹掉，不是没执行）；retry 换成行元素 5s 内不出现（编辑器停在 "Loading…"=初始 git 查询慢于默认 expect 超时，纯窗口太紧）。
根因：`_workbench.openGitGraph` 在 `await openEditor` 后见 `revealCommit` 已注册就直接调——此刻编辑器刚挂载、初始 `load()` 的 getCommits 还在飞行。`revealCommit` 自己再发一发 getCommits 并 `setSelection([hash])`；两发完成顺序倒挂时，晚到的初始 load 续体执行「fresh load invalidates selection」`setSelection([])` 把 reveal 选中抹掉。`revealingRef` 只挡了 `revalidate`，从不挡 `load`。PerforceGraphEditor 同构同病。
修（产品，两侧对称）：`revealCommit` 开头加 `if (result === null) { viewState.pendingReveal = hash; return }`——初始 load 未落地时入队而非竞速，已有的 pendingReveal 消费 effect 在首页数据落地（load 续体跑完）后串行执行 reveal，双请求竞态整体消失。单测复现法：`renderEditor` 的 getCommits 首调挂 deferred、后续即回，reveal 后再 release 首调，断言 selection 仍在。spec 侧：行可见/选中类断言补 `{ timeout: 30_000 }`（对齐同文件 timeline 行的 30s——冷启扩展激活+git 查询在全量并行下轻松超 5s）。锚：`GitGraphEditor.tsx`/`PerforceGraphEditor.tsx`（revealCommit 入队守卫）；`GitGraphEditor.reveal.test.tsx`（「keeps the reveal selection when the initial load resolves after the reveal」）。

**案例 65 — 自启动 spec close 卡死：孤儿/继承句柄握 stdio 管道，close 事件永不 fire（终解=父端 stdio destroy 兜底）**
信号：自启动冷启 spec（viewSizes/viewMove）偶发 `[e2e] closeApp: graceful close still pending after 10000ms` → force-kill 后仍 `CDP pipe still open` → 该 worker 全部测试 pass 但「Worker teardown timeout of 30000ms exceeded」（无 fixture 名）；**事后 dead-parent 扫描常已无孤儿**（元凶稍后自己退了——诊断必须在卡死瞬间 dump，closeApp 的 `dumpSuspectProcesses` 就是为此而加）。改 test.setTimeout 前的形态是 test 本体 7s 跑完、close 卡到 30s test 天花板击穿（e2ea 里表现为 flaky retry 过）。
机制链（读 playwright-core 源码定论）：`ElectronApplication.close()` 的 resolve 系 `launchProcess` 把 `onExit` 挂在 child 的 **`close` 事件**——Node 的 `close` 要等 **stdio 5 根管道全部关闭**，root 退没退无关；任何持有子端句柄的进程活着就永远 pending。三类元凶实锤过：① 手写 seed 漏 `terminal.integrated.useWslProfiles:false` 时 wsl.exe 探测挂起（SIGTERM 杀不掉）；② vendored ACP agent（codex-acp）re-spawn 的孙进程及其 shell-out 探测（`where git`×N、`wmic baseboard`、ConPTY conhost 尸体）——中间父先死，`taskkill /T` 与树 walk 都够不到；③ **并发 spawn 时的继承句柄泄漏**：现场 dump 显示死进程 pipe 被「其它活 app 的进程」意外继承（libuv spawn 窗口句柄短暂 inheritable），根本无孤儿可杀。
修（四层，前三层降低触发率，第四层根治症状）：① 全仓 settings seed 收敛到 harness `INITIAL_SETTINGS`/`seedBaselineUserData`，禁止手写复制（有定制键的如 swarmApp 用 `{...JSON.parse(INITIAL_SETTINGS), ...}` merge）；② 自启动冷启 spec 一律 `test.setTimeout(120_000)` + `[ENABLED_EXTENSIONS_ENV]: ''` core-only；③ `forceKillTree` 单次全表查询+JS 建 PPID 图求闭包；孤儿清扫泛化为「死父 + 指纹」多规则（electron helper/tsserver、node|electron 跑 codex-acp|claude-agent-acp、`where` git|codex|claude、conhost 一律、wmic）；④ **`closeApp` 终极兜底：force-kill + 清扫后 pipe 仍未 EOF 时，直接 `for (const s of proc.stdio) s?.destroy()`**——Node 的 `close` 事件只看**父端**流是否关闭，子端句柄谁握着无所谓，父端流是我们自己的，destroy 即解锁 pending close()，worker teardown 不再超时（实测 destroy 后即便 closePromise 侧仍慢，Playwright transport 也随管道死亡完成 teardown，EXIT=0）。验证：`--repeat-each=5 --workers=4` 多轮，卡死仍偶发（打 warn）但不再产生任何 teardown 超时。锚：`packages/e2e-harness/src/launch.ts`（closeApp/forceKillTree/killOrphanedElectronProcesses/dumpSuspectProcesses）；各 `smoke.*.spec.ts` + `extensions/{ai,perforce}/e2e` 的 seed。

**案例 66 — TextMate 双产品竞态：grammar 注册竞速 Monarch 解析致 model 永久无 tokenization + 主题色表快照乱序落盘错色（62 的门控超时后续；探针 getOrCreate 曾掩盖缺陷）**
信号：形态 B=`waitForTextMateTakeover` 门控通过但 `tokenColor` poll 恒 undefined（整行单 `mtk1` span=model 从未 tokenize）；或门控本身 30s 超时。形态 A=token 有色但**错色**（如 `const` 渲染 `#B267E6` 非 `#569CD6`），mtkN 索引呈现「已加载 22 项色表 +1 位移」= `.mtkN` 样式表停在未加载主题的 7 项残表。两形态本地单跑稳过、`--repeat-each=10 --workers=4` 加压复现。**探针教训**：`getTokenizationSupportInfo` 原用 `TokenizationRegistry.getOrCreate`（强制解析 factory）——门控探针每次 poll 都在**替产品治病**，形态 B 被它间歇掩盖;改只读 `get()` 后测试才诚实（判据：探针语义是「观察产品自身是否就位」→ 必须只读;语义本身是「驱动引擎算值供断言」的诊断探针如 getMarkdownLineTokens 则不在此列）。
形态 B 根因：model 创建时 Monarch factory 还在异步解析（动态 import 慢），TextMate `_rebuildRegistrations` 的 `registerFactory` 替换 factory 会**丢弃进行中的 resolve 且不 fire 事件**（monaco 0.55 语义：`registerFactory` 不碰已解析 support、不通知;只有 `register()`/`setColorMap` 才 fire `changedLanguages`）→ 无人再调 `getOrCreate` → model 永久无 tokenization。旧 warm-up 簿记（initialize 时收集一次 `_languagesToWarm` + 依赖「曾有解析完成」的 flag）在「grammar 注册晚于 model 创建」时双双失效。修（对齐 VSCode `_handleGrammarsExtPoint` 的 createdModes warm-up）：`_rebuildRegistrations` 末尾实时快照活 model 语言（`monaco.editor.getModels()`），∈ 本轮注册语言者逐个 `void getOrCreate(languageId)`;主题切换时对已注册语言调新增的 `TokenizationRegistry.handleChange(languages)` 强制重 tokenize（vscode-textmate 的 ColorMap 冻结在 tokenize 时刻，不重 tokenize 主题色不换）。**反方向第三窗口**（首轮修复后 e2e:smoke 仍 1/64 复发，门控本身 30s 超时=`get()` 恒 null 实锤）：monaco 侧唯一自动触发 `getOrCreate` 的入口是 `languageService.requestRichLanguageFeatures`，但它**按语言 id 去重只触发一次**——那一次若发生在 TextMate 注册前（打了 Monarch），且 rebuild 时恰逢「旧 model 已销毁、新 model 未建」的间隙（shared fixture reload 后 openFileUri 前正是此窗口），rebuild warm-up 空转、新 model 只 `get()`+等事件（`tokenizerSyntaxTokenBackend` 从不主动解析）→ 永久无人解析。修=再挂 `monaco.editor.onDidCreateModel`/`onDidChangeModelLanguage`，model 出生/换语言时对 ∈ 已注册语言者 warm-up（getOrCreate 幂等）——两方向合围后竞态结构性关死。
形态 A 根因（两层叠加）：① `ColorThemeData.tokenColorIndex` 惰性 seeding 以「map 存在」为守卫——外部早调 `getTokenColorIndexId()` 会建空 map 并静默跳过 seeding，后续 `tokenColorMap` 拿到残表;修=独立 `tokenColorIndexSeeded` flag + `clearCaches` 重置。② `monacoThemeBridge.applyTheme(theme)` 捕获主题**快照**，Monaco 懒加载下多个回调乱序到达，最后落盘的 `defineTheme` 可能用未加载主题的 7 项残表（插桩实锤：`encodedTokensColors=21` 之后又来一条 `=6` 且 loaded=true）;修=回调内重读 `themeService.getColorThemeData()`（迟到回调天然收敛到最新状态——**所有「事件触发 + 异步就绪后应用」的桥接都该重读而非捕获**）。验证：修复后 `--repeat-each=10/15 --workers=4` 三轮 145 用例 + e2e:smoke 64 用例全绿（修前 40 中 1-2 挂）。锚：`textMateService.ts`（`_warmUpLiveModels`/`_warmUpLanguage`/onDidCreateModel 钩子/setTheme handleChange）;`colorThemeData.ts`（`ensureTokenColorIndexSeeded`）;`monacoThemeBridge.ts`（回调内重读）;`probe.ts`（getTokenizationSupportInfo 只读）。

**案例 67 — ACP 本地乐观上屏误当 agent 端就位：reload/resume 在握手前 fire（双 id 架构的 e2e 就位信号）**
信号：`page.evaluate` 探针抛 `[E2E] no durable active ACP session`（`reloadActiveAcpSession`），或更隐蔽地 `session/load` 被 echo agent 拒 `session not found`；失败点在「发送 prompt 后立刻做依赖 durable id / agent 端落账的操作」。
根因：异步会话双 id 架构下，`getAcpMessages()` 里的 user 消息是 `_appendMessage` **本地乐观上屏**，不等 `session/new` 握手；`sessionIdOnAgent` 只在 `attachConnection` 后才 set，queued prompt 也在 attach 后才 flush。CI 慢机器上 spawn+握手未竟时，`reloadActiveAcpSession` 读 `sessionIdOnAgent` 得 undefined（本案例形态）；即便 id 刚就位但 prompt 还在队列，echo agent 的 `session/load` 也会因 `!messagedSessions.has(sessionId)` 拒绝（镜像真实 agent：空会话不落盘）。**注意 poll `getAcpSessionStatus()==='idle'` 不是充分信号**——attach 后、queued prompt flush 前存在 idle 窗口（案例 27 同型陷阱）。唯一充分信号 = agent 回复消息出现在 timeline（`getAcpMessages().some(m => m.role === 'agent')`）——它蕴含 attach 完成 + prompt 已派发 + agent 已记 messagedSessions。
修：spec 在 reload 前 `expect.poll(...getAcpMessages()...some(role==='agent'), { timeout: 15000 }).toBe(true)`。本地 5 并发 × `--repeat-each=5` 全绿。锚：`smoke.agentsSelectionAttachment.spec.ts`；`probe.ts`（reloadActiveAcpSession）；`acpSession.ts`（`attachConnection`/`sessionIdOnAgent`）；`echoAgent.cjs`（`session/load` 的 messagedSessions 门）。同型参考 `smoke.agentsScrollRestore.spec.ts` 的「等所有 echo 回复落地」注释。

**案例 68 — 大 csv fixture 四次 p4 print 往返预算不足 vs host 静默 '' 喂进路由判定误走 webview diff：裸 toBeVisible 等满 20s 双假设同修；artifact 上传路径错位静默无现场**
信号：`expect(<diff editor locator>).toBeVisible({ timeout: 20_000 })` 等满 20s 报 `element(s) not found`（`[data-testid="swarm-diff-editor"]` 恒 0）+ **initial 与 retry 完全同形态**（=系统性预算不足或走错分支，非随机竞态）+ 断言对象前存在「fetch 链 → 尺寸阈值路由判定」=「纯慢」与「静默误路由」双假设并存；若 CI artifact 目录同时缺失/为空，先查 upload 路径是否错位（`if-no-files-found: ignore` 会把错位静默吞掉）。
perforce e2e `swarmReview.spec.ts`「routes an oversized Chinese-path csv to the Monaco text diff」在 CI ubuntu（2 核 workers=2）等满 20s 未出现 `swarm-diff-editor`，initial+retry1 同形态（该用例前一天新加、刚通过一次，样本极小）。判别困境：perforce e2e 的 playwright-report/test-results artifact 上传路径少一层 `e2e/`（config 在 `<dir>/e2e/` 下、outputDir 相对 config 解析，而 `ci.yml` 写 `<dir>/test-results` + `if-no-files-found: ignore`）→ 无截图/trace 现场，双假设无法区分只能同修。候选①（测试侧放大器）：该 step 需 4 次完整 1.5MB `p4 print` 往返（bytes probe×2 + text fetch×2；bytes 缓存键 `bytes:<spec>` 与文本键不互通、右侧 `@=<pending change>` 非 immutable 绕过缓存），fake-p4 每次调用=spawn Electron-as-node+同步 parse ~3.1MB state JSON（两份大 csv 塞在 state 里）+1.5MB 管道，CI 争用下可超 20s；retry 在同一负载窗口内重跑故同形态——「initial+retry 同形态」是系统性预算不足而非随机竞态的特征。候选②（产品真隐患，静默误路由）：host 端 `getFileContent`/`getFileContentBytes` 在 `!mgr.active`/print 失败时静默返回 `''`，renderer 把 `''` 当 0 字节喂进 spreadsheet 路由判定（largestSide=0 ≤ 1MB cap）→ 超大 csv 误路由 `_workbench.openWebviewDiff` → `swarm-diff-editor` 永不出现，现象与失败完全一致——空串蒙混过阈值判定是「等满 timeout 恒 not found」的静默产品 bug 形态（对照案例 38 是产品有放弃点，本条是产品走错分支）。修（四管齐下）：① 产品：print 失败结构化错误传播（wire 类型 `SwarmFileContentResult { content, error? }`，「真空文件」= `content:''` 且无 error 键；失败 → renderer setError 走错误页，绝不带空 bytes 进路由判定；失败结果不进持久缓存）；② 产品：oversized csv fall-through 复用 bytes probe 已取回的字节 decode（atob+TextDecoder），省掉 2 次 print；③ 测试：fixture 70k 行（1.5MB）→52k 行（1.13MB，仍过 cap+守卫 throw），断言改「先 `expect.poll(diff.count()+webviewFrame.count()).toBeGreaterThan(0)`（25s）再 `expect(diff).toBeVisible()` + `expect(webviewFrame).toHaveCount(0)`」——误路由立刻以明确形态失败而非 20s 静默超时，断言强度不变；④ CI：upload path 补 `e2e/` 层 + artifact 名换 suite basename。教训：a) 断言对象前有「fetch 链路由判定」时，`element not found 等满 timeout` 的「慢 vs 静默走错分支」双假设要同时排查；把裸 toBeVisible 换成「poll 任一终态出现再断言是哪个」使误路由显性化。b) 静默降级返回空值（`''`/`[]`/0）喂进阈值判定是一类通用产品隐患。c) artifact `if-no-files-found: ignore` 会把路径错位静默吞掉，排查 flake 前先确认失败现场真的在收集。锚：`extensions/perforce/e2e/specs/swarmReview.spec.ts`；`apps/editor/src/renderer/workbench/swarm/SwarmReviewEditor.tsx`（路由判定+错误页）；`extensions/perforce/src/swarm/swarmCommands.ts`（getFileContent/Bytes 错误传播）；`.github/workflows/ci.yml`（artifact 上传路径）。**后记（案例 69 回溯修正）**：本条修后 CI 仍偶发同形态失败——真根因是 fake-p4 的 `process.exit(main())` 在 POSIX 上截断 >64KB stdout（详见案例 69）。当时的两个候选归因（往返预算不足 / 静默空串误路由）都不是主犯：截断产生的是**非空但变短**的 base64，绕过了①的 error 检查照样喂进路由判定。①②的产品加固与④的 artifact 修复仍然有效且必要，但「押双假设同修」漏掉了第三假设：**fixture 自身把数据弄短了**。

**案例 69 — fake CLI 大 stdout 后 process.exit()：POSIX 异步 pipe 写被截断 → 尺寸路由翻转 → providerless webview 静默超时（案例 68 的真根因；平台不对称=「本地 Windows 稳过、CI ubuntu 偶发」的直接来源）**
信号：案例 68 修完后同测试仍偶发同形态——三元 poll（diff+webview 计数）满 25s 恒 0、前三 step 快速通过、initial+retry 同形态、本地 `--repeat-each=8` 全绿；且失败截图**全黑**（Electron+CDP 截图通病，本地复现同样全黑，诊断价值为零）、error-context aria 快照只剩 2 个 `role=alert` 空 div（leak-gate teardown 先 `reactRoot.unmount()`，快照只剩 Monaco 挂 document.body 的全局 aria 容器=伪影，不是真现场）。凡「fixture 子进程单次写 >64KB stdout + 结尾 `process.exit(code)`」+「仅 CI Linux 偶发」都先怀疑本条。
根因链（本地用 `FAKE_P4_TRUNCATE_BYTES` hack 注入截断后复现一字不差，DOM 转储证实 workbench 健在、custom-editor tab 已开）：fake-p4 `print` 一次 `process.stdout.write(1.1MB)` 后 `process.exit(main())`——**POSIX 上 pipe 写是异步的，exit() 丢弃未冲刷的 stdout**（>64KB pipe 缓冲的尾部），截多少取决于 CI 瞬时负载→跨 run flaky、同 run 内 initial+retry 负载相近→同形态；**Windows pipe 写同步**→本地永不复现。两侧各截 ≥0.13MB 后 `largestSide ≤ 1MB` 尺寸路由翻转→oversized csv 走 `_workbench.openWebviewDiff`（viewType `universe.excel`）→ perforce-only 套件无 excel provider→`CustomEditorHost` 等 provider 解析 panel，`webview-frame` iframe 永不渲染→二元 poll 恒 0 静默超时。
修：① fixture 根治：`process.exit(main())` → `process.exitCode = main()`（事件循环自然排空 stdout 后退出；stdin 走 `readFileSync(0)` 无其它存活 handle）+ `process.stdout.on('error', …)` 兜住消费端先死的 broken pipe。② 断言显性化：poll 终态集合补第三元 `custom-editor`（providerless 误路由的真实落点），命中后 `expect(diff).toBeVisible()` + webview/custom 双 `toHaveCount(0)`——误路由从 25s 静默超时变即时明确失败。③ 取证补齐（`packages/e2e-harness/src/forensics.ts` `installFailureForensics`，cold/shared/swarm 三 fixture 接线）：pageerror 收集 + 失败时 `cpSync(<userData>/logs → testInfo.outputPath('userdata-logs'))` 随 test-results artifact 上传，CI 零改动。④ `SwarmReviewEditor` 路由决策日志 `debug`→`info`（默认级别落盘 window-N/，事后可直接对出走了哪条路由）。已全仓 grep：仅 fake-p4 有此模式（fake-swarm 是常驻 server；runner 脚本输出小）。
教训：a) **Node 写的 fake CLI/fixture 大 stdout 后绝不 `process.exit()`**，用 `process.exitCode` 让其自然退出——「Windows 同步写/POSIX 异步写」的平台不对称正是「本地稳过、CI 偶发」这类 flaky 的一等来源。b) 本仓 Electron e2e 的失败截图恒黑、teardown 后 aria 快照必是空壳伪影——别依据它们下结论，真现场在 `<userData>/logs` + pageerror（现已自动收集）。c) poll 终态集合必须枚举**所有**可能的编辑器落点（含误路由目标 custom-editor），漏一个=该分支静默超时。d) 复现优先于推理：给 fixture 加临时故障注入开关（如截断字节数 env）把「CI 才有的环境行为」搬回本地，形态逐字对上才算定案。锚：`extensions/perforce/e2e/fixtures/fake-p4.mjs`（文件尾 exitCode 注释）；`extensions/perforce/e2e/specs/swarmReview.spec.ts`（三元 poll）；`packages/e2e-harness/src/forensics.ts`；`apps/editor/src/renderer/workbench/editor/CustomEditorHost.tsx`（provider 缺失不渲染 frame）。

---

**案例 70 — 焦点交付后异步 payload 触发 keyed remount 换掉被聚焦的树节点：toBeFocused 恒 inactive（同 spec 家族与案例 64 同族，Commit Changes 视图侧）**
信号：`expect(tree).toBeFocused()` received 恒 `data-focused="false"`（**稳定卡值**，63 采样全卡、retry 同形态救不回），但 error-context aria 快照显示树**内容已正确**（数据落地、焦点没落上）=「焦点交付 → 异步数据落地 → keyed remount 把被聚焦的 DOM 节点换掉」竞态；时序特征=断言焦点前一步刚触发了一次**异步**数据推送（本例 ArrowDown 改选中 → 异步拉 commit 变更），慢机上数据必然晚于焦点交付。
`smoke.gitGraphReveal`「focus lands on the commit list on open, arrows move, Enter focuses Commit Changes」CI ubuntu 挂、本地 Windows 稳过——trace 时间线：ArrowDown(41160ms) → rowSelected 断言过(41212) → **Enter(41213)**。Enter 走 `useGraphKeyboardNav.openCommitChanges` → `FocusCommitChangesAction` → `LayoutService.focusView(commitChanges)` 把 DOM 焦点放到 Commit Changes 树的 `[role=tree]` div；与此同时 ArrowDown 的 `applySelection` 还在异步拉 firstHash 的 `getCommitDetails`（2 次 git spawn，必然晚于 Enter）→ resolve 后 `commitChangesViewState.show()` bump `tick` → `CommitChangesView` 的 `<CommitChangesContent key={tick}>` **整体重挂载**，被聚焦的旧树 div 在同一 commit 被换掉、焦点掉 `<body>` 且永不归还（新树无人再 focus）→ `data-focused=false` 恒稳定。与案例 64 同族（同一 spec、同一「graph 选中 → 异步推 Commit Changes」桥）：64 是 reveal 与初始 load 竞态抹**选中**，本条是 remount 抹**焦点**；区别=64 修在 graph 编辑器侧（pendingReveal 入队），本条病灶在共享视图侧，Git/Perforce 两编辑器经同一 `FocusCommitChangesAction` 链路自动同愈。
修（产品侧，spec 断言不动）：`ChangesTree`（Commit/Session Changes 共用抽象）加「同 commit 换树保焦」——模块级 `remountFocusIntent: Set<viewId>`；卸载的 layout cleanup 里若旧树 `contains(document.activeElement)` 则记录 viewId 并 `queueMicrotask` 过期（孤立卸载/关视图不泄漏焦点意图），新实例的 mount layout effect 命中即 `treeRef.current.focus({preventScroll:true})`（`onTreeFocus` 接管落行）。**必须用 layout effect**：microtask 过期先于 passive effect 执行，passive effect 消费不到同 commit 的意图。回归单测（`CommitChangesView.test.tsx`）：「keeps DOM focus on the tree when a show() remount swaps it」（focus 旧树 → show() 换 tick → 断言新树取得焦点，先红后绿）+「an unfocused tree does not steal focus…」（焦点在外部按钮时 remount 不抢焦）。整类扫：8 个 `useViewFocusable` 消费方（Timeline/Search/Scm/Outline/Explorer/Extensions/Session Changes）均原地更新、无 keyed remount，唯一病灶即 Commit Changes 的 `key={tick}`；Session Changes 的 Empty↔树切换跨 commit，microtask 过期保证不被误恢复。验证：相关 52 文件 559 单测 + `pnpm check` + 该 spec `--repeat-each=5` 20/20 + `e2e:smoke` 65/65 全绿。锚：`workbench/changesTree/ChangesTree.tsx`（remountFocusIntent）；`workbench/scm/commitChanges/CommitChangesView.tsx:233`（`key={tick}` remount 源）；`actions/commitChangesActions.ts`（FocusCommitChangesAction）；`services/layout/LayoutService.ts`（focusView 2s 元素轮询）。

---

**案例 71 — workspace-swap restore 竞态擦掉 swap 窗口内新打开的编辑器：poll context key 恒 ""（主症状定案）+ 截图纯黑/aria 只剩 alert 空壳（次症状加留痕兜底）**
信号：`expect.poll(getContextKey('activeEditorLanguageId'))` 20s received 恒 `""` + 失败截图纯黑 + error-context aria 快照只剩 `generic > alert, alert` 两个空壳 + **bootstrap 日志完整走完后静默**（无任何 error/warn）+ 探针活着（evaluate 返回 `""` 不 throw）。与案例 33（bootstrap RPC 被 gate 丢弃）区分：33 探针恒无、日志断在中途；本条探针活、bootstrap 走全。与案例 69 区分：69 的空 aria 快照是 leak-gate teardown 先 unmount 的伪影，本条快照里 #root 已被清空而 teardown 未跑=真死亡。凡「`openWorkspace` 后立刻 `openFileUri`、poll 恒 `""`、日志干净」先怀疑本条。
markdown job（ubuntu，CI run 31295361355）`markdownPreview.spec.ts:205` 与 `gotoSymbol.spec.ts:35` 同轮同形态各挂一次，retry 未救回；同 commit 前后所有 run 该 job 全绿=环境竞态 flake 非回归。**主症状根因（定案）**：`openWorkspace` resolve 后 e2e 立刻 `openFileUri`（trace 实测间隔 52ms），而 storage scope swap 触发的 `WorkspaceRestoreContribution._restore()` 还在 `await storage.get(...)` 上挂着；受争抢的 CI 上读完成晚于 `openEditor` → 新工作区无持久态 → 走 `clearAll()` 分支**把刚打开的编辑器一并擦掉**（该路径只写 debug 日志不落盘）→ `activeEditor=undefined` → ContextKeyContribution 的 autorun 把 `activeEditorLanguageId` 置 `""` 且再无后续变化 → poll 恒 `""` 到超时。本地 Windows 快机上 _restore 恒先于 openFileUri 完成故稳过。单测复现法：deferred `storage.get` mock 把 restore 挂在读上 → openEditor → release 读（回 undefined）→ 断言编辑器仍在（修复前被 clearAll 抹掉，count 0）。**修（产品侧）**：`_restore()` 在读之前快照 baseline 编辑器集合，读完成后把 swap 窗口内新进的 editor 先 `detachEditor`（交还所有权、不 dispose）再 clearAll/restore，最后 re-admit 到 activeGroup 并恢复 active——「读期间新开的属于新 scope，永不擦」。**注意 `EditorInput.matches()` 按 id 判等**（id 默认 = resource.toString()），测试里用 WelcomeEditorInput 会因全实例同 id 摘错编辑器——复现测试必须用每实例唯一 resource 的 input（SwapTestInput）。锚：`apps/editor/src/renderer/contributions/WorkspaceRestoreContribution.ts`（`_restore` newcomer 保留）；`__tests__/WorkspaceRestoreContribution.test.ts`（三个竞态用例）。
**次症状（#root 被清空/纯黑/只剩 Monaco 挂 document.body 的 aria alert 壳）未完全定案**，已排除：render-process-gone（window.log 无记录）、页面 reload（console.log 无第二条 probe installed、探针存活）、WorkbenchErrorBoundary fallback 渲染（aria 快照无其 `data-testid`/Reload 按钮）、React prod 默认 uncaught 输出（console.log 全静默）、代码内直接清 DOM（grep 无命中）。最可信假设：React 19 逃逸所有 ErrorBoundary 的错误 → root 整体 unmount → 默认 `onUncaughtError` 走 `reportError` → window.onerror → `isBenignError`（`errors.ts:33`，吞 ResizeObserver loop / 'no diff result available' / IpcChannelDisposedError）**return true 零留痕**——ext host 重启产生的 IpcChannelDisposedError 恰属 benign，黑屏无任何日志。留痕兜底（诊断增强，非根治）：`main.tsx` 的 `createRoot` 显式传 `onUncaughtError`（rootLogger.error 落盘 + `onUnexpectedError` 走既有上报闸门，不重复造轮子）与 `onRecoverableError`（warn 落盘）——下次复发时 window-N 日志必含 `[react] uncaught error — root unmounted` 及堆栈，可定案；若主修复消除了 clearAll 中途卸载挂载中编辑器组件的场景，级联双错大概率同愈。锚：`apps/editor/src/renderer/main.tsx`（createRoot options）；`apps/editor/src/renderer/errors.ts:33`（isBenignError 静默点）。
教训：a) 「异步读 + 读完按结果 teardown」模式的 restore/reconcile，必须把**读期间的新进状态**摘出来再 teardown，否则慢机上必吃竞态——同族参考案例 27（mount 后 fire-and-forget reconcile 迟到覆盖）、案例 50b/50c（落盘/恢复读脏值）。b) `clearAll()` 这类「无持久态=清空」分支若只写 debug 日志，CI 失败现场毫无痕迹——teardown 类操作的关键分支至少 info 落盘。c) React 19 下逃逸边界的错误会静默 unmount 整棵 root（prod 默认 handler 走 reportError → 可能被 window.onerror 的 benign 过滤器吞掉），`createRoot` 必须显式给 `onUncaughtError` 留痕，否则「黑屏 + 日志干净」无法诊断。d) 验证历史定性：查该 job 最近 N 次 run 全绿、唯本次挂，且失败 commit 改动面与失败 suite 无交集（本例只改 ChangesTree/测试/skill 文档，markdown suite 不挂载 ChangesTree）→ flake 非回归。

---

**案例 72 — CI Windows runner 文件锁窗口内 electron.launch 早夭：Process failed to launch!（ICU 加载失败 / 文件被占用），窗口可长过一次 test retry；修=harness 启动层瞬时重试（launchElectron）**
信号：fixture 或自启动 spec 在 `electron.launch` 处抛 `Error: electron.launch: Process failed to launch!`；call log 里 `<launched> pid=N` 后进程立刻死亡，stderr 是 `Invalid file descriptor to ICU data received.`（exitCode=2147483651=0x80000003 STATUS_BREAKPOINT）或 `The process cannot access the file because it is being used by another process.`（exitCode=1）；**同一时间窗内多个 worker 的 launch 同时挂**（受害者跨 spec、互不相干），窗口外的 launch 全部正常；retry 若落在同一窗口内则同样挂（本例 acpEditorTitle initial+retry1 双挂，activityBar 单挂被 retry 救回）。与案例 28 区分：28 是**本机**环境、CI 正常、不修不打 tag；本条是 **CI Windows runner** 上的瞬时窗口（AV/Defender 扫描 electron dist 或并发进程短暂锁住 electron.exe/icudtl.dat），app 代码完全没跑（进程死在 Chromium 初始化前）。
根因：Playwright 在 CDP 管道建立前进程退出即报 `Process failed to launch!`；此时 Electron 死在加载 ICU 数据/自身二进制的阶段——runner 上另一进程（最可信=Defender 实时扫描）短暂锁住 electron dist 下的文件。锁窗口可持续数十秒，能跨过 Playwright 的单次 test retry，靠 retries 兜不住。
修（harness 层，不动产品、不弱化断言——app 根本没启动过，seeded userData 未被触碰，原地重试同一 userDataDir 安全）：`packages/e2e-harness/src/launch.ts` 新增 `launchElectron(options)` = `_electron.launch` 包装，仅对 `/Process failed to launch/` 重试（3 次、间隔 5s，console.warn 留痕），其它错误原样抛；`launchApp` 内部走它（覆盖 cold/shared/扩展全部 fixture），并从 index.ts 导出给自启动 spec。10 个裸 `_electron.launch` 的自启动 spec（viewSizes/viewMove/terminalRestore/quickOpenRestart/outputRestore/maximizedSecondarySidebarRestore/layoutPersistence/editorRestore/agentsEmptySessionRestore/agentOnboarding）全部改用 `launchElectron`；e2e/CLAUDE.md「自启动 spec」约定同步改为禁止再裸调。验证：`pnpm check` + acpEditorTitle/editorRestore 定向 + `e2e:smoke` 66/66 全绿。
教训：a) 「launch 阶段死、同窗口多 worker 齐挂、窗口外全部正常」= runner 级文件锁，别当产品回归追；b) 环境窗口可长过单次 test retry，靠 Playwright retries 不够，要在更细粒度（launch 调用本身）重试；c) 新增自启动 spec 一律用 `launchElectron`，别裸 `_electron.launch`。锚：`packages/e2e-harness/src/launch.ts`（launchElectron）；`apps/editor/e2e/CLAUDE.md`（自启动 spec 约定）。

**案例 72b — 上条修复后同族复发（同一 CI run 四分片齐挂）：Linux 变体 `spawn ETXTBSY` 不进重试守卫 + Windows 锁窗口超过 3×5s 重试预算；修=守卫扩到 ETXTBSY + 递增退避 + CI Defender 排除根治**
信号：同一 run 里 ubuntu 分片报 `Error: electron.launch: spawn ETXTBSY`（栈就在 launchElectron 的 `electron.launch` 调用行——说明包装生效但正则不匹配直接抛）；windows 分片报 `Process failed to launch!` 且 initial+Playwright retry 双挂（3 次×5s 重试留痕后耗尽）。四个分片跨 OS 同 run 齐挂看似回归，实际是两个独立的启动层环境窗口同时命中——失败点全部收敛在 launch、且 spec 互不相干，即可定性。
根因：a) `spawn ETXTBSY` 是 Linux 上 posix_spawn 撞上并发 fork 短暂持有 electron 二进制写 fd（多 worker 下的经典 Node spawn 竞态），窗口毫秒级，任何重试都能过——但旧守卫 `/Process failed to launch/i` 不匹配它；b) Defender 扫描窗口实测可超过 10s 重试总预算 + 单次 test retry，launch 层固定间隔重试兜不满。
修（三点）：`launch.ts` 守卫改 `TRANSIENT_LAUNCH_ERROR = /Process failed to launch|spawn ETXTBSY/i`；重试改递增退避 `LAUNCH_RETRY_DELAYS_MS = [5s, 10s, 20s]`（总覆盖 ~35s+，fixture timeout 60s 内安全；自启动 spec 受 30s test timeout 截尾，不劣于原状）；`.github/workflows/ci.yml` e2e job checkout 后加 `Add-MpPreference -ExclusionPath "$env:GITHUB_WORKSPACE"`（仅 Windows）从源头消掉 Defender 锁，launch 重试降级为保险。
教训：a) 包装重试的错误匹配正则是白名单——新变体(ETXTBSY/EBUSY/EAGAIN)出现时栈若落在包装内说明只差匹配，扩正则即可，别再包一层；b) 环境窗口长度要拿失败时间线实测（initial+retry 双挂=窗口>单 test 生命周期），重试预算按实测窗口定而不是拍脑袋；c) hosted runner 有 admin 权限，Defender 排除(`Add-MpPreference`)是比重试更根本的手段，两者叠加：排除治本、重试兜底。锚：同上 + `.github/workflows/ci.yml`（e2e job Defender 排除步骤）。
2026-08-09 增补：CI Windows 又现同族新变体 `electron.launch: Electron failed to install correctly. Please delete node_modules/electron...`（playwright 启动前置的可执行性检查在 Defender 锁窗口内跑不动 electron.exe 的报法；栈落在 launchElectron 的 `electron.launch` 行=「只差匹配」直接实证，Playwright test 级 retry 救回标 flaky）。已并入守卫正则 `TRANSIENT_LAUNCH_ERROR`。

---

**案例 72c — 72b 修完 Windows 仍挂：launch 内部游离 promise 的 unhandledRejection 先于重试守卫把测试判死（post-spawn 早夭模式重试从未生效过）**
信号：test 报**裸** `Error: Process failed to launch!`（**无 `electron.launch:` 前缀**）；trace 里错误条目是 `{"type":"error","stack":[]}` 且时间戳**早于** stderr 里的 `[e2e] electron.launch failed (attempt 1/4)` 重试留痕——测试在重试守卫刚 catch 到、还没睡完退避时就已被判死。与 72/72b 区分：那两条的报错带 `electron.launch:` 前缀（走 await 链正常抛出）；本条是 worker 的 unhandledRejection 通道（`workerProcessEntry` 对 unhandledRejection 无条件 fail 当前 test）。
根因：playwright-core `Electron.launch` 内部创建 4 个 `waitForLine` promise（Xserver 错误哨、node debugger 行、DevTools 行、debugger disconnect 行），进程 post-spawn 早夭（Defender 锁 ICU/二进制）时**四个同时 reject 同一个 failError**，但只有 `nodeMatchPromise` 被 await——其余 3 个游离，在 in-process test worker 里触发 unhandledRejection → 立即 fail 当前 test；随后被 await 的那条才进 `launchElectron` 的 catch（守卫本身工作正常但为时已晚）。**spawn 级失败（Linux ETXTBSY）不受影响**——它发生在 `launchProcess` 阶段、`waitForLine` 还没创建，无游离 promise，所以 72b 的重试在 ubuntu 上真实有效（同 run 实证：ubuntu 的 ETXTBSY attempt 1 重试即过）；也解释了 72 时代「activityBar 被 retry 救回」其实是 **Playwright test 级** retry 救的，launch 级重试对 Windows 这种 post-spawn 死亡从未生效过。
修：pnpm patch `playwright-core@1.62.1`（`patches/playwright-core@1.62.1.patch`）——在三个游离 promise 创建后补 `.catch(() => {})` 标记 handled（错误仍经 await/race 原链传给调用方，不吞）；`pnpm-workspace.yaml` patchedDependencies 带注释，升级 playwright 时按该文件头部流程核对（上游若修复则删 patch）。另实证：本 run 里 72b 的 Defender 排除已执行（10:43:43）仍在 10:45:19 出现锁窗口——排除不是完全根治，launch 层重试才是必须生效的兜底。
教训：a) 「重试守卫加了却像没加」先查错误到达通道——对比报错有无 API 前缀 + trace 里 error 条目与重试留痕的时间序，unhandledRejection 通道会绕过一切 await 层守卫；b) Playwright 库模式 server 与 test worker 同进程，server 内部游离 rejection 会直接击穿当前 test，这类修复只能 patch 到库里；c) 同一环境窗口的两种失败模式（spawn 级 vs post-spawn 级）对重试守卫的可达性不同，验证守卫时要分模式看实证，别拿一种模式的成功推断另一种。锚：`patches/playwright-core@1.62.1.patch`；`pnpm-workspace.yaml`（patchedDependencies 注释）；playwright-core `lib/coreBundle.js` `Electron.launch`/`waitForLine`。

---

**案例 73 — EditContext 异步落字赛跑 Enter 提交：ACP prompt 真键盘打字被截断发送（received 是 expected 的前缀）**
信号：断言完整消息的 `toContainEqual` 失败，received 里 user 消息 text 是期望文本的**前缀**（如 `"Explain this s"`，retry 截断点还不同=竞态非确定），echo agent 回声同样截断=发送时模型里就只有这些字。失败点前的操作序列 = `keyboard.type(<text>)` 直接 `keyboard.press('Enter')`，目标是 **Monaco 输入框**（EditContext）。
根因：monaco 0.55 EditContext 下，CDP 派发的字符经 EditContext `textupdate` **异步**落进 model，而 Enter 的 keydown 走 keybinding 同步读 model 提交——CI 慢机上尾部字符还在途，发送即截断（HTML input 无此问题，quick input/find widget 不受影响）。
修：type 后、Enter 前补 `expect.poll(() => getAcpPromptText()).toBe(<全文>)`（`smoke.agentsSelectionAttachment.spec.ts`）；`smoke.agentsPromptHistory.spec.ts` 第三用例的裸 type 后同款补齐（防迟到字符污染 popover 交互）。同族加固先例：`agentsPromptHistory` 的 `typeAndSend`、`agentsMcpDraft` 的 `typeDraft`（delay 20 + 双 poll）——本条正是「同文件族有的步骤已加固、有的还裸」的遗留薄弱点形态。
教训：真键盘往 **Monaco**（EditContext）输入后要做结构性操作（Enter 提交/ArrowUp 弹层/光标移动依赖内容时），必须先 poll 模型内容到位；对 HTML input 则无需。新写 ACP prompt 相关 spec 直接抄 `typeAndSend` 模式。锚：`smoke.agentsSelectionAttachment.spec.ts`（poll 后 Enter）；`smoke.agentsPromptHistory.spec.ts`（typeAndSend）；`smoke.agentsMcpDraft.spec.ts`（typeDraft）。

---

**案例 74 — palette 打字后立即 Enter 接受了「旧过滤列表」的第一项：QuickInputPanel useDeferredValue 竞态（产品 bug，慢机高概率）**
信号：palette 用例 `type('<命令名>') → Enter → waitForHidden` 后，断言命令效果的 poll 恒卡「命令没执行」的初值（本例 `getActiveEditorUri()` 恒 `Untitled-2`），但 `editorFocus` 等焦点断言正常 true（palette 关闭后焦点还原到原编辑器）；**双平台同 run 同形态 initial+retry 全灭**（CI 慢是持续条件，不是独立随机事件）；本地 `--repeat-each` 低概率复现（1/5）；**在 type 与 Enter 之间插入任何 evaluate 往返做插桩后不再复现＝海森堡效应，即竞态窗口就在 type→Enter 之间**。pageerrors 里的 `Canceled at Delayer.cancel/dispose` 是无关噪音。
根因：`QuickInputPanel` 的过滤列表基于 `useDeferredValue(filterText)` 渲染（低优先级），Enter handler 读的是**已提交渲染**闭包里的 `sortedFiltered[focusedIdx]`。慢机上 type 完最后一个字符后 deferred 重渲染尚未 commit，Enter 读到的是**旧过滤文本**的列表——word mode 无分隔符时按 MRU+字母序排，`Focus Next Group` 打到一半的 "Focus" 匹配集第一项是 `Focus Above Group`（水平双组布局下 no-op）→ 执行了错误命令 → palette 关、焦点还原原组、断言恒卡。这是真产品 bug：真实用户快速打字+Enter 同样会执行错命令。
修（产品层，勿在 spec 里遮）：把过滤+排序管线抽成纯函数 `buildDisplayList`，Enter/accept 路径检测 `filtersLocally && deferredFilterText !== filterText`（deferred 未追上）时，用**实时** filterText 同步重算列表并 accept 其第一可选项（与渲染稳定后的结果一致，对齐 VSCode「接受当前输入的最佳匹配」语义）；deferred 已同步时维持原 `sortedFiltered[focusedIdx]`（尊重箭头导航）。所有 quick pick（命令面板/编辑器选择/keybindings 等）统一受益。
教训：a) 「双平台同 run 同形态、initial+retry 全灭、本地稳过」优先怀疑**持续条件型竞态**（慢机必然触发）而非独立随机 flake；b) 插桩后不再复现（海森堡）本身就是定位证据——窗口就在被插桩的两步之间；c) `useDeferredValue` 驱动的列表,任何「读已渲染列表做 accept」的路径都要校验 deferred 是否已追上实时输入，否则接受的是旧世界的选择；d) e2e 抓到的不一定是断言不鲁棒——本例断言完全正确，抓到的是产品竞态,修产品不改 spec。锚：`packages/workbench-ui/src/feedback/quickInput/QuickInputPanel.tsx`（buildDisplayList + Enter 分支）；`apps/editor/e2e/specs/smoke.editorGroupSwitch.spec.ts`（原样保留的用例）。

---

## 根治 TODO

- `@parcel/watcher` Windows 多 worker 竞态的长期根治（升级 / 换 watcher / 进一步隔离），替代长期 `--workers=1`（案例 12/16/26/44 的 `@serial` 都是它的 workaround）。
- DnD 用例稳定化（显式等待 drop 完成态），稳定后摘 `@flaky`（案例 46）。
