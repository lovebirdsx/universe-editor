# 案例库（fix-ci-e2e-flake）

> 速记索引见 `../SKILL.md` 的「速记」节；速记 N 与案例 N 互相引用。新经验先追加到本文案例库，再到 SKILL.md 补一条速记。

> 每条：现象+根因 → 修法+锚点。教训已并入对应速记；此处保留判别信号与锚点路径。新经验往下追加。

**案例 1（速记 1/2）**：`smoke.notification` toast 全局 `toHaveCount(0)` CI 偶发、count 1↔2 波动——ext host 崩溃发背景 Warning toast 污染全局 count。修：`.filter({hasText:'This is a test notification.'})` 收敛再 count。锚：`ExtensionHostClientService._handleCrash`；`NotificationsToast.tsx`。

**案例 2（速记 8）**：同轮挂 bell badge `"1"` + `smoke.output` `panelVisible` false——同噪音源 `_handleCrash`（未读 toast→unreadCount；崩溃 error→`ErrorLogAutoRevealContribution._hasRevealed` 一次性置真）污染全局单值/一次性状态，spec 层无法 filter。修：`ExtensionsContribution._boot` 开头 `isE2E` 短路 return（先 grep 确认无 spec 依赖 ext host/SCM）。锚：`isE2E=window[E2E_PROBE_ENABLED_KEY]`（`shared/e2e/contract.ts`）。

**案例 3（速记 9/10）**：`smoke.outline` `@p1` 硬失败（超时非拿 `[]`）——全局 test `timeout:30_000` 拦腰截断 outline poll 累计 ~115s（`OutlineService` retry 180s），CI tsserver 冷启动+4 Electron 抢 2 核。修：冷启动 spec `test.slow()`；`expect.timeout=CI?10_000:5_000`；`workers=CI?2:4`；editorResolver 改“先 poll 目标项可见再 Enter”。锚：`playwright.config.ts`；`OutlineService.ts`（`PULL_RETRY_BUDGET_MS`）。

**案例 4（速记 12）**：`smoke.outline` Windows+Ubuntu 都总挂、received 恒 `[]`——outline 唯一依赖 ts LSP，ts 扩展硬依赖 `vendor/typescript-language-server`（非 pnpm workspace，CI 只 `pnpm install`+`build` 从未装它）。修：e2e job 加 `npm --prefix vendor/typescript-language-server ci`（不能调 `vendor-install.mjs`——它遍历 submodule 而 e2e job 无 `submodules: recursive`）。锚：`.github/workflows/ci.yml`；`scripts/release/vendor-install.mjs`。

**案例 5（速记 13）**：`smoke.peekNavigation` Ubuntu 偶发、received 稳定 `…/a.md`——`expect.poll` 盲按 `keyboard.press('Enter')`，peek 打开前焦点在编辑器 TEXTAREA，Enter 在链接处插换行破坏 link→peek 永不打开。修：加探针 `isReferencePeekFocused()`（`document.activeElement?.closest('.ref-tree')`），只在聚焦时按。锚：`PeekNavigationContribution.ts`（`.ref-tree`）。

**案例 6（速记 5/14）**：`smoke.disposableLeak` Ubuntu 偶发 `Execution context was destroyed`、栈在 `WorkbenchPO.waitForRestartRestore()` 末步裸 `evaluate(whenRestored)`——`restart`→`win.reload()` 导航未 commit 时裸 evaluate 与上下文切换重合被销毁。修：抽 `_evaluateWhenRestored()`（捕获→重等→重试 ≤3）两处复用。锚：`WorkbenchPO.ts`；`windowActions.ts`（`RestartEditorAction`）。

**案例 7（速记 15）**：`smoke.gotoSymbol` Windows 偶发、`activeEditorLanguageId` poll received 恒 `""`——首步硬编码 `{timeout:5000}` 盖回 CI 分档（config 已 CI 10s）。修：删这 4 个 spec 的 `{timeout:5000}` 继承 config 默认。锚：`smoke.{gotoSymbol,peekNavigation,markdownLsp,markdownPreview}.spec.ts`；`playwright.config.ts:11`。

**案例 8（速记 16）**：`smoke.editorTabDnD` 本地 Windows 偶发 `EBUSY rmdir`、栈在 `finally` 的 `fs.rm(tmpDir)`——app 未关（`await use(app)` 后才 `closeApp`），pinned 句柄+watcher 占目录，`force:true` 不重试 EBUSY。修：`fs.rm` 加 `maxRetries:10,retryDelay:200`。锚：`electronApp.ts`。

**案例 9（速记 17）**：`smoke.commandPalette` `@p0` 前置 `expect.poll(getContextKey('editorFocus')).toBe(true)` received 恒 false——`focusActiveEditorGroup` fire-once 依赖 Monaco `FileEditorRegistry.register`（model 异步后），冷启动命令在 register 前 fire→no-op，poll 不重 fire。修：把 fire 放进 poll 谓词，抽 PO helper `focusActiveEditorGroup()`。锚：`editorActions.ts`；`FileEditor.tsx`（`applyModel` register）。

**案例 10（速记 18）**：`smoke.aiCommitMessage.spec.ts` 仅 Windows CI `Test timeout of 30000ms`、received 恒 0——与兄弟 `@p0` 逐行相同但漏抄 `test.setTimeout(120_000)`，poll 窗口 30_000==全局 timeout→摆设，Windows 慢击穿。修：加 `test.setTimeout(120_000)`、poll→60_000。锚：基准 `aiCommitMessage.generate.spec.ts`。

**案例 11（速记 8/19）**：案例 10 续集，形态变 poll 自己 60s 窗口超时、`getScmSourceControlCount()` 恒 0——git `activate` fire-once，`openWorkspace` 依赖 host relaunch 重新 activate，但 `_connect` 在 `await host.start` 期间 `this._trusted` 未赋值→启动期 swap 落此窗口事件被丢弃→host pin 空 workspace。修产品：`_onWorkspaceChanged` 开头 `await Promise.allSettled([_startingTrusted,_startingRestricted])` 再读 tier+回归单测。锚：`ExtensionHostClientService.ts`（`_onWorkspaceChanged`）。

**案例 12（速记 20）**：`smoke.simpleFileDialog`「OK switches workspace」本地全量偶发 `Target page...has been closed`——main 退出码 `0xC0000005` native 段错误；三组对照证 `@parcel/watcher` windows backend 多进程并发重订阅（切 workspace）native 竞态，进程内串行化无效。修：`@serial` 隔离到 `--workers=1`（`pnpm e2e`/`ci.yml` 各拆并行趟+串行趟）。锚：`smoke.simpleFileDialog.spec.ts`；`fileWatcherMainService._subscribe`。

**案例 13（速记 21/51）**：仅 Windows CI「Worker teardown timeout」+所有测试 pass——①`SessionShutdownParticipant._maybeVeto` 有 running session 时弹 headless 无人应答 modal→`app.close()` 永不 resolve；②超时 SIGKILL 只杀 main PID，Windows 子进程孤儿占句柄。修：Fix A `_maybeVeto` 算出 running 后 `isE2E` 短路 `return false`；Fix B `closeApp` 超时 `taskkill /pid <pid> /T /F`。锚：`SessionShutdownParticipant.ts`；`e2e/fixtures/electronApp.ts`（`forceKillTree`）。

**案例 14（速记 22）**：`smoke.agentsScrollbarStable` 仅 Windows `maxJump≈1.27`（阈值 0.25）、采样单调爬升到稳定——每段滚动只等固定 2 帧 raf 读 `scrollHeight`，@tanstack `ResizeObserver` 异步测量未完，采到过渡瞬时值。判定：`--workers=1 --repeat-each=5` 5/5 过、`--workers=4` 全挂=并发放大。修：换 `settle()`（scrollHeight 连续 4 帧不变或耗尽 120 帧）。锚：`ChatBody.tsx`（`estimateRow`/`useVirtualizer`）。

**案例 15（速记 23/24）**：①`smoke.agentsScrollRestore` `finalFrac=1`（期望 <0.85）——`poll(readFrac).toBeGreaterThan(0.15)` 被 RAF 过冲帧 frac=1 立刻满足；②`smoke.vscodeKeybindings` 末步裸 `whenRestored` `Execution context destroyed`=案例 6 同型；sibling `agentsVirtualScrollRestore` scene1 贴底 distance 卡 ~342=缺 echo-settle 门控。修：①断言改锚定消息索引落绝对中部带 [4,12]、poll 连续 ≥600ms 在带内；②导出 `evaluateWhenRestored(page)` 复用；sibling 补 echo-settle+两帧 scrollHeight 门控。锚：`ChatBody.tsx`（`captureAnchor`/`scrollToBottomStable`）；`WorkbenchPO.ts`。

**案例 16（速记 20）**：`smoke.folderDragNewWindow`+`smoke.outline` 本地 `--workers=4` 偶发 `Target page...has been closed`——撞案例 12 同款 parcel watcher 跨进程崩溃（`openWorkspace`→`ExplorerTreeService._setRoot`→`fileWatcher.watch`→parcel `subscribe`；folderDrag 一次 test 两次 subscribe 高危）。`--workers=1` 12/12 过=并发放大。修：两 spec `@serial`。锚：`ExplorerTreeService._setRoot`→`fileWatcherMainService._subscribe`。

**案例 17（速记 9/16）**：`smoke.multiFileDragEditor` 本地全量偶发、断言已过、报 `ENOTEMPTY rmdir workspaces` 栈在 `sharedApp.seedUserData`——`openWorkspace` 改主进程状态违反 `sharedApp` 契约，`resetWindow` 删 workspaces 瞬间主进程又写 `.tmp`（storage debounced 原子写）→父 rmdir 撞 ENOTEMPTY，`rmSync` 内建 maxRetries 只覆盖叶子救不回。修：`seedUserData` 改 async，手写 `for(attempt<10)`+`await sleep(200)` 循环重试整个 `rmSync`。锚：`sharedApp.ts`（`seedUserData`）；`src/main/storage.ts`（`writeAll` tmp→rename）。

**案例 18（速记 12/25）**：`smoke.dirtyDiffPeek` 仅 CI Windows、poll received 恒 false（test 级已 120s）——CI Windows `os.tmpdir()` 8.3 短路径喂 openWorkspace，`git --show-toplevel` 返长路径→`relative(长,短)` 破碎→`git show` exit 128→`getHeadContent` 恒 null→regions 空。不对称：兄弟 aiCommitMessage 只验 SCM count>0 能过。修：`realpathSync.native(mkdtempSync(...))` 规范长形。锚：`extensions/git/src/repository.ts`（`getHeadContent` `relative(this.root,absPath)`）；`DirtyDiffContribution._getHead`（缓存 null）。

**案例 19（速记 26/27/28）**：sessionChanges「diff 跟随修改自动更新」新 spec `--workers=4 --repeat-each=6` 批量挂、received 恒 agent 值——三层叠加：①`sessionDiffAgent.cjs` 写盘共享 tmp（sessionId `sd-1` 每进程重复）被覆写→加 `${process.pid}-${randomBytes(4)}`；②`setActiveEditorText` 裸 evaluate model 未就位 no-op→放进 `expect.poll`；③迟到 fs 事件 `_refreshChangedDiffEditors` 无条件读盘盖回 live 编辑→live-model 优先（`peek` 有值用 `model.getValue()`，别用 isDirty）。诊断坑：改既有共用探针污染全断言、`console.info` heisenbug→用独立 probe+`window.__DIFF_DBG__` 数组。锚：`sessionDiffAgent.cjs`；`ExternalChangeWatcher._refreshChangedDiffEditors`；`SessionChangesDiffSyncContribution._sync`。

**案例 20（速记 29）**：`smoke.mermaidPreviewSwitch` 全量新暴露、切回预览 scrollTop 恒 0——测试裸 `dispatchEvent(new Event('scroll'))` 不带 wheel，被 `useMarkdownPreviewScrollRestore` 的 `restoringRef` guard 吞（只有 wheel/pointer/key 触发 `stop()` 退出 restore）；提交 `f7405724` 新增 reveal-line 分支+mermaid 同步渲染拉长 600ms 定时器激活既有脆弱点。判定：`--workers=1 --repeat-each=5` 确定性全挂+`git stash`+build 回 HEAD 仍挂。修：测试滚动前补真实 `WheelEvent`（产品零改动）。弯路：改产品用位置差异判接管会把 scroll-anchoring 漂移误判、打破切回。锚：`useMarkdownPreviewScrollRestore.ts`（`restoringRef`+`stop()`+`RESTORE_WINDOW_MS=600`）。

**案例 21（速记 30）**：`smoke.historyNavigation` `@p0` 本地全量偶发、goBack 后多一个 ghost pinned tab——`sharedApp` `window.reload()` 不拆主进程 workspace，上个 spec `openWorkspace` 的文件夹被 `WorkspaceRestoreContribution._restore` 灌回本测试 active group。修：`resetWindow` 在 `whenRestored` 后加 `closeWorkspace()`（切 no-workspace scope→`clearAll()`）+新探针。锚：`sharedApp.ts`（`resetWindow`）；`workspaceMainService.ts`（`closeFolder`）。

**案例 22（速记 31）**：`smoke.markdownMoveStaleDiagnostic` 本地全量偶发（~1/5）、移动 B 后 A 链接没落盘、received 恒旧值——`closeAllEditors` 同步返回但 model dispose 异步，move 触发 bulk edit 时 `peek(A)` 返回即将 dispose 的孤儿 model→走 live 分支只改内存、孤儿 dispose 后丢失。无扰动探针（`__peekModelAlive`）抓到孤儿存活+磁盘旧值。修产品：`FileBulkEditService.apply` 加 `persistToDisk`（始终读写盘+`setValue` 同步残留 model），`_flush` 传 true。锚：`fileBulkEditService.ts`（`apply` persistToDisk）。

**案例 23（速记 32/33）**：一次启动优化提交同时打挂 4 spec（jsonOutline/outlineKeyboard 恒空、markdownEditing 命令被拒、markdownPreview languageId 恒空）——`git show <opt-commit>` 自述「workbench mounts before Monaco」，自补部分探针漏改同类。三类根因：(a) `OutlineService._recomputeSymbols` 拉取缺 `.catch()`→worker 冷启动 reject 断 retry 链（产品 bug）；(b) `runCommand(toggleTask)` 未注册时抛异常击穿 `expect.poll`（Playwright poll 对回调抛异常不重试）；(c) markdownPreview pure-preview `openWorkspace` 冷启动 10s poll 不够。修：(a) `.then().catch()` 双分支 `_maybeRetry`+注入 ILoggerService；(b) `runEdit` try/catch 仅 `/extension host may only execute/` 时 return undefined；(c) poll `{timeout:20000}`。守护测试：OutlineService REJECT 保活用例。锚：`OutlineService.ts`；`smoke.markdownEditing.spec.ts`（`runEdit`）。

**案例 24（速记 34）**：案例 23 续集，补 `.catch()` 后 jsonOutline/outlineKeyboard 仍 Ubuntu 稳定挂 `[]`——`_maybeRetry` 只在 pull settle 后排下一次，JSON provider 底层 worker RPC/动态 import 冷启动永久 pending→then/catch 都不触发→链死。不对称：Windows✓/Ubuntu headless✗；TS/markdown outline 走 LSP 有自带超时不吃此路径。修：抽 `_pullWithTimeout`，`provideDocumentSymbols` race `PULL_TIMEOUT_MS=5000` timeout-reject 保证必 settle。守护测试：provider HANGS（`new Promise(()=>{})`）用例。锚：`OutlineService.ts`（`_pullWithTimeout`）。

**案例 25（速记 15）**：`smoke.markdownLsp` 仅 CI Windows、首 `activeEditorLanguageId` poll received 恒 `""`——`openWorkspace`-then-`openFileUri` 冷启动，首 poll 吃 config CI 默认 10s 在 Windows 不够（后续 poll 都已带 10s，唯首 poll 漏改，同案例 23c 未对齐兄弟）。修：首 poll 加 `{timeout:20000}`。锚：基准 `smoke.markdownPreview.spec.ts:1197/1238`。

**案例 26（速记 20/35）**：`smoke.output`（纯 renderer，自己不 openWorkspace）本地全量偶发 `Target page ... has been closed`（**非** `Execution context destroyed`，故不被 `sharedApp.ts:105` catch 吞），栈在 `resetWindow` 的 `runCommand('clearHistory')`——根因同案例 12/16 parcel `0xC0000005`，但传播链是 sharedApp 模型：`resetWindow` 每 test 前无条件 `closeWorkspace()`→若同 worker 前个 test 开过 workspace，reload restore 它、reset `unwatch`→parcel `unsubscribe`，多 worker 并发 subscribe/unsubscribe 崩，output 只是恰好在 reset 撞上濒死进程的受害者（崩点飘忽、谁排在前谁背锅）。修：隔离加害者而非受害者——把主趟里所有 `sharedApp ∩ openWorkspace` 的 test 打 `{tag:'@serial'}`（主趟遂无 sharedApp test 进 workspace 状态→`closeFolder` 的 `if(_current===null)return` 让 closeWorkspace 变 no-op→主趟永不 parcel op，受害者自然不崩，覆盖不变）。锚：`sharedApp.ts`（`resetWindow`→`closeWorkspace`）；`fileWatcherMainService._teardown`→`unsubscribe`。

**案例 27（速记 1/17/36）**：`smoke.activityBar` `@p0`「switches between Explorer and Search」`--repeat-each=16` 偶发 2 挂、`data-active-view-container` 期望 `search` received 恒 `explorer`（13 采样全 explorer、卡满 5s 不恢复）——`click(SEARCH)` 是 fire-once 把 SideBar 切离默认，但 renderer 在 **mount 后 fire-and-forget** 跑 `viewsService.reconcileFromStorage()`（含 `INITIAL_LOAD_TIMEOUT_MS=500` fallback timer），无持久 views 时以破坏性 `_loadFromStorage`→`_seedDefaults()` 收尾回默认 Explorer。`sharedApp.resetWindow` 只 await `whenRestored`（=mount 时机，早于 reconcile），故迟到的 reconcile 在 click 后覆盖回 Explorer。免疫反例：`smoke.workspace` 断言 openWorkspace 后的**默认 Explorer**（与 reveal + reconcile seed 三方一致），迟到不翻车。修：spec body 首行 `await workbench.waitForBootstrapFocusSettled()`（probe 在同一 reconcile `Promise.all` 的 `.finally` resolve）。锚：`main.tsx:751`；`ViewsService.ts`（`reconcileFromStorage`/`_seedDefaults`）。

**案例 28（速记 37）**：本机 Windows `pnpm e2e` 里 `smoke.editorRestore`/`smoke.outputRestore`/`smoke.layoutPersistence`/`smoke.agentOnboarding`/`smoke.agentsEmptySessionRestore` 等**裸 `electron.launch`** 的 restore/relaunch 类 `@p1` 偶发/稳定报 `electron.launch: Process failed to launch!`（exitCode=9）——两类根因叠加：①**环境**：本机对 Electron 二次启动+CDP 连接的限制，`git stash` 清改动复跑仍挂即证与业务无关，走 fixture 的 `@p0` 全过、CI 亦过→**别当回归追,以 CI 为准**；②**真 bug**：Claude Code shell 设 `ELECTRON_RUN_AS_NODE=1`→Electron 当纯 Node 跑不认 `--remote-debugging-port`→子进程继承后报 `bad option`。修：所有自定义 `electron.launch` 的 `env` 先解构去掉该变量再展开（`fixtures/electronApp.ts`/`sharedApp.ts` 及各 spec 内 `launchWithState` 已统一）；本地全量偶发 launch 争用时 `--workers=2` 可稳。附：ext host 启动崩溃通知污染精确 count 断言→`smoke.notification` 触发前 `clearAll` 清噪声+文案 `filter` 替代 `toHaveCount(1)`。锚：`e2e/fixtures/electronApp.ts`（`inheritedEnv` 解构）。

**案例 29（速记 38）**：`smoke.search` `@regression` 仅 Windows CI 稳定失败，active editor 已正确打开目标文件，但预期 `file:///C:/Users/RUNNER%7E1/...`、received `file:///C:/Users/RUNNER~1/...`，retry 不改变差异——Windows runner 的 temp root 含 8.3 `~` 路段；Node `pathToFileURL` 与平台 `URI.file` 的序列化规则不同。修：断言使用 `URI.file(target).toString()`，保持和 E2E probe 的 `EditorInput.id` 同一 URI 表示域。锚：`renderer/e2e/probe.ts`（`getActiveEditorUri`）；`platform/base/uri.ts`（`URI.file`）。

**案例 30（速记 39）**：`smoke.perforceChangelist`「perforce.moveToReconcile lands the file in Changes to Reconcile」`@p1 @regression` 本机 `--workers=1 --repeat-each=N` **确定性**失败(0/5)、received 恒 `["default"]`(default 组 `hideWhenEmpty:false` 永远可见，真正失败是 reconcile 组恒空被隐藏)——三层复合根因：**①盘符大小写**：`pathsInChangelist('default')` 返回内核 `norm(clientFile)`=**小写盘符** `c:/`，fake-p4 `targetsFromArgs` 用**大小写敏感**精确匹配磁盘路径(大写 `C:/`)→匹配空；sibling 传显式 `perforce.file()`(大写)故过。真实 p4 Windows 大小写不敏感=fixture 失真。修：fake-p4 匹配改走 `normPath`+`toDepotFile` 身份比较。**②fixture 架构失真**：fake-p4 把 `state.json` 写在**被 watch 的 workspace 目录内**(`.p4fake/`)，每次 `saveState` 触发 workspace watcher→额外 `refreshReconcilePaths` 与被测操作交织；真实 p4 状态在服务器不在工作区。修：state 移到独立 `mkdtempSync`。**③真产品竞态**(剥完前两层仍 4/8 挂)：`refresh`(有单飞)与 `refreshReconcilePaths`(无互斥)并发读改共享 `_reconcileFiles`/`_openedPaths`；watcher 触发的"文件仍 opened→scan 结果被过滤成空"的 pass 若**最后完成**，其 `mergeReconcile` 把文件删回→组恒空(诊断日志实锤:`_setReconcileFiles n=1` 后被 `n=0` clobber)。真实使用(p4 edit→改文件触发 watcher→点 Move to Reconcile)同样踩=真 bug。修：加串行链 `_runSerial`，refresh 类操作全走它，每 pass 见前一 pass 已提交态；回归单测「serializes overlapping reconcile passes」。锚：`extensions/perforce/src/client.ts`(`_runSerial`)；`e2e/fixtures/fake-p4.mjs`(`normPath`)；`fixtures/perforceApp.ts`(state 外置)。

**案例 31（速记 15）**：案例 25 第三次复发——`smoke.markdownEditing`「operates on non-final lines and CRLF documents (regression)」仅 CI Windows，首 `activeEditorLanguageId` poll received 恒 `""`（10s 超时）——同 `openWorkspace`-then-`openFileUri` 冷启动，首 poll 无显式 timeout 继承 config CI 默认 10s；紧随的 `hasCommand` poll 反而都带了 15000ms，首 poll 是漏改薄弱点。本文件 4 个 test 全同型。修：4 处首 poll 统一 `{timeout:20000}`（`replace_all`）。教训=修一处必整体扫同文件同型兄弟一并加固。锚：基准 `smoke.markdownPreview.spec.ts`。

**案例 32（速记 10/18）**：案例 10 复发——`smoke.layoutPersistence`「sidebar width is restored from storage on launch」仅 Ubuntu CI `Test timeout of 30000ms exceeded`（test 天花板击穿）——裸 `electron.launch` 冷启动重前置（seed workspace state→hydration→reconcile 落地→Allotment DOM 收敛），但**漏抄 `test.slow()`**（兄弟 markdown 系列全有，此 spec 独漏），且两 poll 各硬编码 `timeout:5000` 盖回 config CI 分档 10s（案例 7 反模式）；Ubuntu 2 核+2 workers 抢占下累计 >30s。修：`test.slow()`+两 poll 抬到 `15_000`。整体评估同款：`vscodeKeybindings` 两 test 有 `timeout:30_000` poll 但无 test 级保护（poll 窗口==全局 timeout→摆设，速记 10），一并补 `test.slow()`；`editorRestore`/`outputRestore`/`agentOnboarding`/`agentsEmptySessionRestore`/`viewMove` poll 窗口 ≤8s 单发不击穿 30s 暂不动。锚：`smoke.layoutPersistence.spec.ts`；`playwright.config.ts:6`/`:11`。

**案例 33（速记 40）**：同一 Ubuntu shard 随机报 1 failed + 5 flaky，受害者横跨 `swarmReview/gotoSymbol/markdownPreview/windows/workspaceSettings`；全部未进入 test body，首窗口或第二窗口卡 `waitForFunction(__E2E__)`，截图纯黑，retry 多数恢复。trace 显示 launch/firstWindow 很快，随后 probe 恒无；提交边界是最后绿色 commit 后新增 `ElectronProtocol._frameAlive`。根因：main 在 `loadURL` 前建 protocol，`did-start-navigation` 关 gate；renderer ES module 在 `dom-ready` 前执行 `initializeRendererNls()` 发 IPC，`ChannelServer` 同步生成响应却被 `send()` 静默丢弃，renderer 的无超时 Promise 永久 pending，probe/React 均不会安装。修产品：`onIncoming` 路由到 `ElectronProtocol.acceptMessage`，先 `_frameAlive=true` 再 `Emitter.fire`，保证同一调用栈里的响应可发送；保留 `render-process-gone` 后无入站时的 dead-frame 防反馈保护。守护单测钉住「main-frame navigation 后、dom-ready 前收到首个 IPC 即恢复」。不要加 fixture timeout 掩盖永久死锁。锚：`apps/editor/src/main/ipc/electronProtocol.ts`；`electronProtocol.test.ts`。

**案例 34（速记 23）**：案例 15 遗漏的 sibling——`smoke.agentsVirtualScrollRestore`「stays bottom-pinned and restores mid position ... when virtualized」仅 Ubuntu CI 偶发，场景 2 末尾 `expect(finalFrac).toBeLessThan(0.85)` received `0.97`。兄弟主 spec 早已按案例 15 改成锚定消息索引，此虚拟化 sibling 场景 2 仍裸用 `frac=scrollTop/max`+3s 窗口是遗留薄弱点（速记 5）。诊断：加 `readTopIndex`（解析 `data-slot-key` 尾数）+临时打印，`--repeat-each` 实测发现**恢复完全忠实**（anchor=11→恢复≈10-11 frac≈0.42；anchor=22→恢复=22 frac≈0.92），**非产品 bug**；flake 真因=像素中点在虚拟模式**双峰**落到 m11 或 m22（上方行 estimate 高度≠真实），落 m22 时忠实恢复 frac≈0.92 触发 `<0.85` 误判。绝对中部带无法同时覆盖两者（带宽到能含 m22 就不再守护“跳到底”）。修：改**相对不变量**——`settleIndex()`（连续 4 帧同值）记切走前 anchor，恢复后 poll 连续 ≥600ms 落 `[anchor-2, anchor+2]`，仍守护重置到顶与跳到底。锚：`smoke.agentsVirtualScrollRestore.spec.ts`；基准 `smoke.agentsScrollRestore.spec.ts`；`acpSession.ts`（`m${++_msgCounter}`）。

**案例 35（速记 41）**：`smoke.perforceChangelist`「a default-changelist file row exposes "Move to Changelist" via right-click」本机 Windows 偶发，`page.evaluate` 报 `extension host may only execute _workbench.* commands ... not "perforce.edit"`——扩展命令冷启动激活竞态：perforce `activate()` **早期** `PerforceClient.create`→`scm.createSourceControl` 就建好 source control，spec 的 `getScmSourceControlCount()>0` 门控随即满足；但 contributed 命令 handler（`perforce.edit` 等）在**同一 `activate()` 后段**才同步批量注册。两者之间 fire `perforce.edit`→renderer 转发 host→host 无 handler 转回 renderer→`MainThreadCommands.$executeCommand` 拒（非 `_workbench.*`）。修：加 `waitForPerforceCommands(workbench)` helper，`expect.poll` 轮询只读的 `perforce.refresh` 直到不再抛该错串（handler 一次性注册，一个就绪则全就绪），插到每个 fire `perforce.*` 的 test 的 `workbench.view.scm` 之后（5 处）。同目录 `smoke.perforceCollectChanges`（靠 watcher 驱动、只在 row 可见后 click）、`smoke.perforceGraph`（renderer Action2 恒有 handler）均不脆弱。锚：`smoke.perforceChangelist.spec.ts`（`waitForPerforceCommands`）；`MainThreadCommands.ts:74`（拒绝点）。

**案例 36（速记 42）**：`smoke.multiFilePromptDrag`「a CR-separated text/uri-list inserts one @mention per file」仅 Ubuntu CI，`expect(count).toBe(3)` received 恒 0（稳定卡值非波动）——`openSessionWithPrompt` 只等 `acp-prompt-drop-host`（host `div` 立即渲染）就 drop，但 drop 处理里 `el.insertRef()` 依赖内嵌 Monaco 句柄（`if (!ed||!model||!m||!tracker) return`），Monaco 未 mount 完时静默 no-op；Ubuntu 慢启动下 drop 在 Monaco 就位前 fire→0 个 pill，drop fire-once 不幂等。同文件第一个 test（OS drag）因 `setInputFiles` 额外 await 给了缓冲不易翻车；第二个无缓冲稳定挂。键盘型兄弟（poll `editorTextFocus` 天然门控）、`smoke.promptDragHighlight`（只 `dragover` 不碰句柄）均免疫。修：`openSessionWithPrompt` 加 `expect(prompt.locator('.monaco-editor').first()).toBeVisible()` 门控编辑器就位，`waitForTimeout(400)`+硬断言换 `expect.poll`。锚：`smoke.multiFilePromptDrag.spec.ts`；`PromptMonacoEditor.tsx`（`insertRef` no-op 守卫）。

**案例 37（速记 43）**：`smoke.markdownRenameLinks`「rewrites a link in another file when its target is renamed」仅 Windows CI 偶发，`readWorkspaceFileText(a.md)` `toContain('./c.md')` received 恒原文 `./b.md`（15s 超时）——同族 `smoke.markdownMoveStaleDiagnostic` 先前也报同型（先加了 `EMPTY_EDIT_RETRIES` blind-retry，仍复发→治标）。真根因=**workspace-swap 的异步 re-pin 竞态**：idle `LanguageServicePrewarmContribution` 的 `runWhenIdle`→`activateByEvent('onLanguage:markdown')` 在 CI 抢占下可能**先于** `openWorkspace` 把 ext host 连到初始（空）workspace（`hasConn=true` 分支）；随后 `_onWorkspaceChanged` 异步 `_restart` 重钉，而 rename 的改链接 flush 撞在“`host.stop` 旧 host→spawn 新 host”窗口里 fire→落到仍钉 `root=none` 的旧 host，其 `$findMarkdownFiles` 因 `root` undefined 直接 `return []`→空 edit。本地重连 <1 帧测不到，Windows Electron-as-node spawn 慢撑到秒级。排障关键：DBG 打印证明 happy path 恒 `hasConn=false`，强制 `hasConn=true`+2s stop 延迟才暴露；ext host 侧 `console.error` 落子进程 stderr→主日志 `[stderr N]` 不进 page console。修：`ExtensionHostClientService` 加**同步 arm 的 re-pin barrier**——`onDidChangeWorkspace` 回调改非 async（首 await 前同步记 `_repinning=Promise`，`.finally` 清），新增 `_whenReady()`（drain barrier 再 `start()`），`activateByEvent`/`executeContributedCommand` 改 `await this._whenReady()`。retry 作为 defense-in-depth 保留。回归单测「blocks a command racing a workspace swap until the host is re-pinned」（fake host `stop` 挂起撑宽窗口）。锚：`ExtensionHostClientService.ts`（`_repinning`/`_whenReady`）；`extensions/markdown/src/mdFsBridge.ts`（`$findMarkdownFiles` `if(!root)return[]`）。

**案例 38（速记 44）**：`smoke.webviewDiff`「opens a webview diff via _workbench.openWebviewDiff and renders both sides」`@p1 @regression`。**先前误修（84ac521d）已回退**：那版把 iframe `toBeVisible` 从 10s 提到 15s，归因“Windows 慢启动、产品容忍 15s、spec 只等 10s 先放弃”。**被本轮 Ubuntu CI 证伪**——等满 15s 仍 `element(s) not found`，而 15s 恰是 `CustomEditorHost` `setTimeout(...,15000)` 后 `setFailed`（**放弃** provider）的时刻：等到产品自己都放弃了 iframe 仍不出现＝provider 根本没注册，不是慢竞态；且这次是 Ubuntu（无 Windows 慢 spawn 借口）。**真根因（产品 bug）**：`installVsixExtension`→`refreshExtensions()`→`_restart('workspace')`＝stop 旧 host(conn A，初始 boot 扫描、**不含**刚装的 vsix)→起新 host(conn B，才扫到)。而 `CustomEditorHost` 只在 mount 时 `activateByEvent(onCustomEditor:<vt>)` **一次**（fire-and-forget）；install→restart 竞态下这唯一一次 activate 若落在 conn A→无 provider；`_restart` 结尾只重放 `STARTUP_ACTIVATION` **不重放** `onCustomEditor:`，而 `CustomEditorHost` 的重试只挂 `onDidChangeProviders`——provider 从未注册→该事件永不 fire→15s `setFailed`。Windows 偶尔过＝activate 恰好落 conn B。**普遍性**：任何已打开 custom editor 遇 host 重启（workspace swap/crash 恢复）都会变空白。修=产品侧 `CustomEditorHost` 订阅 `onDidChangeContributions`，fire 时若 `!opened` 则重新 `activateByEvent`＋`tryOpen` 成功时 `setFailed(false)`；spec 侧回退两处 timeout 15s→10s，保留 `test.slow()`。锚：`CustomEditorHost.tsx`（`activateOwner`/`onDidChangeContributions` 订阅）；同型对照 `smoke.pdfCustomEditorRace`（测 install→open 竞态，天然依赖此修复）。

**案例 39（速记 45）**：`smoke.search` / `smoke.searchScroll` / `smoke.searchScrollRestore` 三 spec `@p1 @regression` 满载/慢机器上不同轮次**轮流**挂，`getByTestId('search-view')` `toBeVisible` 5s 恒 `element(s) not found`、单跑必过（与速记 36/案例 27 同族但触发源不同）。三 spec 路径相同：`openWorkspace(dir)`→`activityBar.click(SEARCH)`→期望 search-view 可见。error-context 的 DOM 快照显示 **`Explorer [pressed]`**（=真实 activeId 是 explorer）。根因：`WorkspaceExplorerRevealContribution` 监听 `onDidChangeWorkspace`，打开文件夹时**无条件** `openViewContainer('workbench.view.explorer')`；事件**晚到**（点击后）时把刚点的 search 抢回 explorer→SearchView 卸载→testid 消失。**排障弯路**：先误判在 `ViewsService._reload`/`_reconcileActive`，加临时日志实测 DROP 从不出现→排除；顺「还有谁 `openViewContainer('explorer')`」grep 才找到真凶。诊断 spec 结构需**贴合真实失败**（点击后立即断言，别先 poll 缓冲给事件到达时间）。修产品=reveal **条件化**：`_revealExplorer` 先读 `getActiveViewContainerId(SideBar)`，仅当 `undefined` 或仍是 explorer 时才切。附带 `ViewsService._reload` 改「以当前选择为基底 merge 持久化值」。回归单测：「preserves a non-Explorer container the user already activated」。锚：`WorkspaceExplorerRevealContribution.ts`（`_revealExplorer` 条件化）；`ActivityBar.tsx`（`handleClick` toggle 语义）。

**案例 40（速记 46/47，续案例 39）**：`smoke.searchScroll` / `smoke.searchScrollRestore` CI Ubuntu 偶发再挂（案例 39 修复后仍未根治），同形态+快照 `Explorer [pressed]`。**复合根因两层（速记 39 层层剥）**：**第一层=fixture 状态泄漏**——`resetWindow` 只「wipe 磁盘 workspaces/+reload」，但 WORKSPACE-scope storage backend 是 **main 进程内存对象**（`storageMainService._workspace`），reload 不关 folder 也不重建它；前个 search 系 spec 结尾 `click(SEARCH)` 的 200ms debounce persist 把 `{SideBar:search}` 落进该内存 bucket，新 renderer 的 `reconcileFromStorage` 若赢得 reload 后补发 closeWorkspace 的 500ms settle 竞速→读回 search→本 spec `click(SEARCH)` 命中 ActivityBar **toggle** 关闭容器→active=undefined→reveal 合法开回 explorer。复现=whenRestored 后临时 sleep 700ms 撑宽窗口→确定性挂；修=`resetWindow` 在 **reload 前**先 `closeWorkspace()` 拆内存 bucket。**第二层=真产品竞态**——第一层修后仍挂且「sleep 在时全绿」（sleep 不是修复而是给了第二竞态 settle 时间）；按速记 26 上 `__VIEWS_DBG__` 内存数组埋点抓到决定性时序：`839 handleClick(search) branch=OPEN → 849 openViewContainer(explorer)` 且**无 revealExplorer 日志**→排除 reveal，顺 `LayoutService.focusView`（唯一通用 `openViewContainer(descriptor.containerId)`）反查到 `WorkspaceFocusRestoreContribution`：`onDidChangeWorkspace` 开 1500ms restore 窗口+80ms debounce，无编辑器时 `restoreWorkbenchFocus` 无条件 `focusView(explorer.tree)`→把 10ms 前用户刚点的 search 盖回 explorer（真实用户打开文件夹 1.5s 内点 Search 同样被抢）。修产品=`restoreWorkbenchFocus` explorer 分支前读 `getActiveViewContainerId(SideBar)`，已切非 explorer→返回 `{target:'kept'}` 不抢（与案例 39 reveal 条件化同语义）。回归单测：「keeps a non-Explorer side bar container the user already switched to」。锚：`packages/e2e-harness/src/fixtures.ts`（`resetWindow` pre-reload closeWorkspace）；`workbenchFocusRestorer.ts`（`'kept'` 分支）。

**案例 41（速记 23④/48）**：`smoke.agentsVirtualScrollRestore` 仅 Ubuntu CI 偶发，场景 2 前置守卫 `expect(anchorBefore).toBeLessThan(23)` received **24**——案例 34 把断言换成锚定索引相对不变量后，仍遗留「像素中点定位」这个薄弱点：`(scrollHeight-clientHeight)/2` 在虚拟估计坐标系下落点不确定，CI 慢机这次直接落到末条 m24；距底 <32px(`STICK_THRESHOLD_PX`) 被 `handleScroll` 记 `stuck=true`，场景 2 退化成场景 1 的假阳性（若守卫不在就会假绿）。修：定位改按锚定索引割线迭代（设 frac→settle 读锚→`frac*=target/idx` 折算，直至锚落进中部带 [6,18]），抽共享 `AcpTimelinePO.scrollToAnchorBand`；同型 `smoke.agentsScrollRestore`（max/2+绝对带 [4,12]，同款潜伏）一并换用。锚：`packages/e2e-harness/src/pages/AcpTimelinePO.ts`；`ChatBody.tsx`（`STICK_THRESHOLD_PX=32`）。

**案例 42（速记 49）**：2026-07 标题栏重构（65185fce）后 CI 三个 spec 挂：ai-button `locator.click` 超时 30s、元素 visible 但 call log 报 svg `intercepts pointer events`；本地全绿。根因=command-center 药丸 `flex-shrink:0` + 全 shrink:0 兄弟，CI 的 en-US 更宽文案+1280×800 默认窗口使溢出的 center 右缘滑到 DOM 序更后的 `.right` 区域底下，ai-button 被 svg 遮挡（`elementFromPoint` 命中 svg）；本地 zh-CN 菜单更短仅剩 2px 间隙恰好不触发。修：长条形容器留可缩元素吸压；验证用 `--lang=en-US` + 探针 `elementFromPoint(btn 中心) === btn` / `center.scrollWidth <= center.clientWidth`。（本案例自 memory `e2e-ci-locale-window-size-vs-local` 迁入。）锚：标题栏 command-center 药丸。

**案例 43（速记 50）**：`smoke.titlebar`「clicking the command center opens Quick Open」仅 Ubuntu CI 偶发，Escape 关闭 Quick Open 后 `expect(commandCenter).toBeFocused()` 10s 恒 inactive（24 采样全卡值）——bb36ad15 新增的焦点环断言。根因=bootstrap one-shot 焦点恢复抢焦：sharedApp `resetWindow` pre-close workspace 后 reload 进空窗口，`viewsService.reconcileFromStorage` 无 workspace 恒走 500ms fallback timer，settle 后 `restoreWorkbenchFocus` 无编辑器分支 `focusView(explorer.tree)` 聚焦 Explorer 树；测试链路 click→quick input→Escape→react-aria FocusScope rAF 恢复焦点到按钮，CI 慢机上链路累计 >500ms，bootstrap restore 落在恢复之后把焦点抢到 Explorer 且永不归还（one-shot）→恒 inactive。仅 Ubuntu=2 核慢机 settle 落测试中段；Windows CI/本地快机 settle 早于 click。诊断弯路：本地先跑出 `outline-style "auto"` 假失败=stale build 缺 `:where():focus-visible` 规则（速记 11：诊断前必先 `pnpm build`），重建后 20/20 绿才转向 CI 特有竞态。修：spec 动作前 `await workbench.waitForBootstrapFocusSettled()`；整类扫 `toBeFocused` 命中 `smoke.commandPalette`（6 test）/`smoke.quickAccess`（5 test）同族一并 describe 级 `beforeEach` 加固；`smoke.workspaceFocus` 已有门控、`smoke.simpleFileDialog` 有 openWorkspace 缓冲免疫。锚：`smoke.titlebar.spec.ts`；`workbenchFocusRestorer.ts`（explorer 分支）。

**案例 44（速记 20）**：`smoke.editorRestore`「switching workspaces does not leak editors across scopes」仅 CI Windows 偶发 `page.evaluate: Target page, context or browser has been closed`、栈在第二次 `openWorkspace(wsA)`——案例 12（simpleFileDialog「switches the workspace twice」）的同型复发：**裸 `electron.launch` 的 restore spec 在测试中途切工作区**（A→B→A 两次 `openWorkspace`=parcel watcher subscribe/unsubscribe 连发），与另一 worker 并发踩 `@parcel/watcher` windows backend 跨进程 native 竞态（0xC0000005）崩掉自己的 main。当时案例 12/16/26 只扫了 sharedApp/folderDrag/simpleFileDialog 系列，漏了 restore 系裸启动 spec。修：该 test 打 `{tag:'@serial'}`。整类扫：其余裸启动 spec（layoutPersistence/outputRestore/viewMove/maximizedSecondarySidebarRestore/agentsEmptySessionRestore/agentOnboarding）grep 均无测试中途 `openWorkspace`（仅 boot 时恢复进 workspace 的单次 subscribe，非高危 churn），不打标。锚：`smoke.editorRestore.spec.ts`；基准 `smoke.simpleFileDialog.spec.ts:35`。

**案例 45（速记 21/51）**：全量 e2e「97 passed 但 N 个 Worker teardown timeout（30s，无 fixture 名变体）」、每轮波动 1~5 个、spec 组合随机（2026-07-25，terminal profiles 功能引入后暴露）——main 端 terminal profile 检测 spawn `wsl.exe -l -q`，wslservice 被 8 并发 worker 竞争时 wsl.exe 挂起；**Node `execFile` 的 timeout SIGTERM 杀不死它**（wsl.exe 卡 RPC 状态，实测挂 8+ 分钟；`taskkill /F` 秒杀）→孤儿持有继承句柄→main 死后 Playwright 的 CDP pipe 不 EOF→`app.close()` 永不 resolve→`gracefullyCloseAll()` 卡满 30s。放大器：`ViewPane` 的 toolbar **无条件挂载**（view 折叠也渲染）→TerminalViewToolbar 的 mount prefetch→Panel 每次显示都触发 `refreshProfiles`；sharedApp 每个测试 reload 窗口→每 worker 4-7 次 wsl spawn。修双层：①产品层 `terminalMainService.runDetectionProbe` 自管理 spawn 替代 `promisify(execFile)`——`stdio:['ignore','pipe','pipe']`（wsl.exe 会等 stdin）+`windowsHide:true`（spawn 默认 false 会闪 console 窗）+超时后 `taskkill /pid /T /F`（对真实用户同样是健壮性修复）；②e2e 层 `INITIAL_SETTINGS` pin `terminal.integrated.useWslProfiles:false`（WSL 检测已有 23 个 main 单测，e2e 不需要机器相关探针）。排查法：无 fixture 名=卡在 `gracefullyCloseAll`（连接未断），有 fixture 名=该 fixture teardown 本身；查系统孤儿（父 PID 已死的 wsl/pwsh/conhost/electron），孤儿命令行即凶手；卡住 worker 的 tmp userData `logs/*/main.log` 尾行「before-quit confirm 后无 will-quit」=quit 链被打断。（本案例自 memory `e2e-worker-teardown-wsl-orphans` 迁入；关联 memory `cli-stdin-hang-on-prompt` spawn 交互型 CLI 挂起同类。）锚：`terminalMainService.runDetectionProbe`；e2e `INITIAL_SETTINGS`。
