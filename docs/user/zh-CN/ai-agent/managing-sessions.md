# 管理会话

随着用得多了，你会积累不少 Agent [会话](../reference/glossary.md#会话agent-会话)。这一页讲怎么恢复、切换、清理会话，以及会话标题、聊天字体、会话内导航与查找。

## 目录

- [恢复历史会话](#恢复历史会话)
- [刷新与切换](#刷新与切换)
- [清除会话历史](#清除会话历史)
- [会话可见范围](#会话可见范围)
- [会话标题](#会话标题)
- [聊天字体大小](#聊天字体大小)
- [时间线导航](#时间线导航)
- [在会话中查找](#在会话中查找)

## 恢复历史会话

- 快捷键：`Ctrl+Shift+H`（恢复 Agent 会话…）。
- 从列表里挑一个（显示标题、所在目录、相对时间）。
- 属于**其它工作区**的会话，会以**只读预览**方式打开，避免误连到错误的目录。

<!-- 截图：恢复会话列表 -->

## 刷新与切换

- **刷新列表**：命令"刷新 Agent 会话列表"。
- **跨窗口切换会话**：`Alt+S`（切换会话…）。

<!-- 截图：切换会话（跨窗口） -->

## 清除会话历史

- 命令："清除 Agent 会话历史"，会有确认弹窗。
- 它**只清除本地的历史索引**；Agent 侧保存的对话仍然保留，直到 Agent 自己清理。

## 会话可见范围

会话列表显示哪些会话可以配置——通过设置项 `acp.sessions.historyScope`，可选：

- 当前工作区（`workspace`）
- 当前工作树（`worktree`）
- 全部（`all`）

## 会话标题

会话标题会自动生成。你也可以为它单独指定一个"会话标题模型"，详见 [模型选择与花费](./models-and-cost.md)。

## 聊天字体大小

以下快捷键**仅在聊天获得焦点时**生效：

| 操作 | 快捷键 |
| --- | --- |
| 增大聊天字体 | `Ctrl+=` |
| 减小聊天字体 | `Ctrl+-` |
| 重置聊天字体 | `Ctrl+0` |

## 时间线导航

会话聊天记录称为"时间线"。以下按键**仅在聊天获得焦点时**生效：

| 操作 | 快捷键 | 命令标题 |
| --- | --- | --- |
| 上一项 / 下一项 | `Alt+↑` `Alt+↓`（或 `Alt+K` `Alt+J`） | 聚焦上一个 / 下一个时间线项 |
| 顶部 / 底部项 | `Alt+A` / `Alt+E` | 聚焦时间线顶部项 / 底部项 |
| 滚动 上 / 下 | `Ctrl+Alt+↑` / `Ctrl+Alt+↓` | 向上 / 向下滚动时间线 |
| 翻页 上 / 下 | `Ctrl+Alt+PageUp` / `Ctrl+Alt+PageDown` | 时间线向上 / 向下翻页 |
| 折叠当前项 | `Alt+F` | 切换时间线项折叠状态 |
| 全部折叠循环 | `Ctrl+Alt+F` | 循环切换时间线折叠状态（全部） |
| 跳转到计划 | `Alt+P` | 跳转到计划 |

## 在会话中查找

- `Ctrl+F` 打开查找条（在会话中查找）。
- `F3` 下一个匹配项 / `Shift+F3` 上一个匹配项 / `Esc` 关闭。

<!-- 截图：会话内查找条 -->

## 下一步

- [Agent 概览](./overview.md)

## 相关阅读

- [模型选择与花费](./models-and-cost.md)（会话标题模型）
- [你的第一次 Agent 会话](./first-session.md)
