{
  "name": "@universe-editor/remote-server",
  "version": "0.0.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "bundle": "node esbuild.config.mjs --bundle",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run",
    "lint": "eslint src --cache --cache-strategy content",
    "lint:fix": "eslint --fix src",
    "clean": "node -e \"const {rmSync}=require('node:fs');for(const d of ['dist','dist-bundle'])rmSync(d,{recursive:true,force:true})\""
  },
  "dependencies": {
    "@lydell/node-pty": "catalog:",
    "@parcel/watcher": "catalog:",
    "@universe-editor/node-services": "workspace:*",
    "@universe-editor/platform": "workspace:*",
    "@universe-editor/temp-root": "workspace:*",
    "@vscode/ripgrep": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@universe-editor/config-ts": "workspace:*",
    "@universe-editor/extension-api": "workspace:*",
    "@universe-editor/extension-host": "workspace:*",
    "@universe-editor/extension-packaging": "workspace:*",
    "esbuild": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
